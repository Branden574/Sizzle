# Sizzle — technical debt register

Durable tracker (TODO comments don't count). Add an entry when work uncovers real debt;
close entries by linking the fixing commit/PR. Severity: P1 fix soon · P2 scheduled ·
P3 opportunistic. Sourced initially from the 2026-08-06 autonomy audit
(`autonomy-audit.md` holds the full context + evidence for each).

| ID | Sev | Subsystem | Problem | Fix sketch | Status |
|----|-----|-----------|---------|------------|--------|
| TD-1 | P1 | web UI | Conditional React hooks in 3 shipped components — RecipeSheet.tsx:75-91 (7 hooks after early return), CommentsSheet.tsx:41, CookSheet.tsx:357. Latent "rendered more hooks" crash class; the 9 remaining `npm run lint` errors. | Hoist hooks above early returns; verify all three sheets on the iOS Simulator; then add `lint` to ci.yml. | open |
| TD-2 | P1 | api deps | hono 4.12.26 in advisory range — ReDoS in the CORS middleware mounted app-wide (GHSA-8j4g-w8fx-2239). In-range fix 4.13.0. | `npm update hono` → typecheck → smoke CORS from getsizzle.app + native origins → owner-authorized deploy. | ✅ closed 2026-08-07 — hono 4.13.0 shipped (commit 7843842); lockfile = 4.13.0 (> 4.12.33 advisory range), `npm audit` no longer flags hono core |
| TD-3 | P1 | api security | `POST /hashtags/:tag/moderate` (hashtags.ts:224) is an admin action gated only by requireAuth+requireAdmin — bypasses the requireAdminUnlock second factor mounted on the /admin router. Contradicts SYSTEM_RISK_MAP:177-180. | Move under /admin or add requireAdminUnlock inline; confirm the dashboard client sends the unlock header on this call. | ✅ closed 2026-08-07 — requireAdminUnlock now inline on POST /hashtags/:tag/moderate (commit 7843842); verified hashtags.ts:230 (route + comment cite the 2026-08-06 audit) |
| TD-4 | P1 | crons | 4 of 5 crons have zero failure signal (only finalize-videos has the indirect stuckVideoBacklog gauge). | `cron_runs` heartbeat table + per-cron last-success age on /health + alert threshold. | ✅ closed 2026-08-07 — cron_runs heartbeat + /health cronAges + cron-stale problems live (commit 7843842); verified all 5 jobs upserting last_success_at in prod (health.ts:33-60, internal.ts recordCronRun) |
| TD-5 | P1 | observability | /health always 200 "ok" (DB-probe failure swallowed → null); Sentry events carry no release/OTA version; internal.ts failure paths console.error only despite Sentry being live. | Honest degraded/503; `release` field in both envelopes; captureException on the 4 cron failure branches. | 🟡 mostly done 2026-08-07 — honest 503/degraded ✓ (health.ts:72,117), API Sentry `release` ✓ (sentry.ts:44), cron captureException ✓ (internal.ts). REMAINING: web Sentry envelope still carries no `release` tag (apps/web/src/lib/sentry.ts) |
| TD-6 | P1 | money tests | Stripe/RevenueCat webhook + entitlement flows have none of the SYSTEM_RISK_MAP "required tests" (duplicate delivery, refund-after-earnings, chargeback-after-payout, account-switch entitlement). | In-process Hono + local Supabase harness with signed canned fixtures; commit it (stop discarding per-change harnesses). | open |
| TD-7 | P1 | db security | The 2026-07-29 RLS/grant-matrix verification is manual prose; grant drift already caused one real bypass. | Commit a runnable grant-matrix assertion (SQL, read-only) + wire into CHANGE_SAFETY_CHECKLIST after any grant-touching migration. | open — **partial cousin shipped 2026-08-17 (security sweep):** `scripts/ops/anon-boundary-probe.mjs` is a credential-free, read-only, black-box probe of the *anon* side of the boundary (22 sensitive tables must yield 0 rows, 12 `profiles` PII columns must be column-denied, `creator_earnings` must not execute, with a positive control). It is NOT the SQL grant-matrix assertion this entry asks for — it cannot see the `authenticated` role (where SYSTEM_RISK_MAP open findings #1/#4 live) or distinguish "RLS holding" from "table empty" — so TD-7 stays open. **Strengthened 2026-08-21 (daily sweep, see TD-26):** the probe now covers **all 49** migration-declared tables (was 22 — 26 were silently unprobed), asserts the *predicate* of every policy that lets anon read (`hashtags` blocked/sensitive/non-active all return 0 rows), and its table classification is CI-enforced against `supabase/migrations`, so a new table fails the build until it is classified. Live result: the entire anon-facing schema is clean. This closes the anon half of the drift question properly; the `authenticated`-role half and the empty-table ambiguity are still exactly why TD-7 remains open. |
| TD-8 | P2 | deploys | No verify-deploy tooling; API exposes no commit SHA; deploy.md still documents Railway/Fly. | Stamp VERCEL_GIT_COMMIT_SHA into /health + web version.json; scripts/verify-deploy.mjs (poll READY + probe + SHA compare); rewrite deploy.md. | ✅ closed 2026-08-23 — all four parts done. 2026-08-07: `scripts/verify-deploy.mjs` ✓ and `/health` `commit` SHA ✓ (health.ts:81). 2026-08-23 (daily sweep), the two remainders: **(1) web `version.json` now emitted** by a `versionManifest()` rollup plugin in `apps/web/vite.config.ts` (`{version, commit, builtAt}`, commit from `VERCEL_GIT_COMMIT_SHA`/`GITHUB_SHA`, null on an unstamped local/archive build), served `no-store` via `apps/web/vercel.json` — a cached copy would report the PREVIOUS build as live, defeating the check. The app never imports it, so it carries no runtime risk. **(2) `docs/deploy.md` rewritten** against verified files, with a dated supersession banner: two reverse-named Vercel projects (`sizzle` = API, `sizzle-api` = frontend), the Build-Output-API serverless function (nodejs22.x, maxDuration 30s) rather than `node dist/index.js`, real required/optional env vars incl. the two boot-time guards, the five cron schedules with their `/health` stale limits, the mandatory verify-deploy step, and the true `/health` shape. **A real bug was found and fixed in the same pass:** verify-deploy's **web** branch printed the probe status and asserted *nothing* — a READY frontend serving HTTP 500, or an alias pointing at a previous build, both exited 0 as "verified". It now fails on a non-ok probe and compares `/version.json`'s commit to HEAD (`checkWebVersion()`), giving the frontend the same served-surface proof `/health` already gives the API. Regression-guarded by **7 new tests** in `tests/invariants/ops-tooling.test.mjs` (match · short-SHA prefix · alias mismatch FAILS · missing file FAILS · unreachable FAILS · unstamped archive deploy reports-not-fails · the build+header contract). Verified real guards, not tautologies: flipping the `no-store` header to `public, max-age=3600` fails the suite, restoring it passes (27/27). Emission verified on a real build twice — plain build → `commit: null`, and driving Vite's build API with `VERCEL_GIT_COMMIT_SHA` set → the SHA stamped through. |
| TD-9 | P2 | ota | OTA upload is a raw manual CLI command; nothing can fail a native-coupled bundle; no min-update-version anywhere. | `ota:release` wrapper: safety:diff vs last-binary tag hard-fails on NATIVE, then build + upload with --min-update-version from a committed compat table. | open |
| TD-10 | P2 | api types | Supabase client untyped (no Database generic, no generated types) — column renames compile clean and fail at runtime. | `supabase gen types` → thread generic through supabaseAdmin; regenerate on migration PRs. | open |
| TD-11 | P2 | api abuse | No per-route limits on GET /me/export (8 unbounded scans), POST /uploads/video/:id/poster (paid moderation call), follow/block mutations. | Targeted rateLimit buckets with the existing middleware. | open |
| TD-12 | P2 | uploads | Own-folder URL check is plain startsWith — `..`/percent-encoded traversal can address another user's object within the project host. | Parse with new URL(), reject %/?/#/.., require normalized pathname to keep the prefix. | open |
| TD-13 | P2 | docs | PROGRESS.md 6 builds behind; README describes pre-launch prototype; security.md/HOSTING.md/launch-blockers.md/premium-launch-plan.md/cloudflare-stream-setup.md/audit-2026-07-01.md carry resolved "MUST fix" claims with no closure banners; app-store-deployment.md wrongly says the Firebase plist is committed; SYSTEM_RISK_MAP "96"→**97** migrations (the entry said 95 when filed on 2026-08-06; re-counted 2026-08-21 — the number moves with every migration, so the catch-up pass should re-count rather than trust this figure); ARCHITECTURE "18"→19 route groups (re-counted 2026-08-23: `apps/api/src/app.ts` mounts exactly **19**). | One docs catch-up pass with dated supersession banners. | open — **narrowed 2026-08-23 (daily sweep):** `docs/deploy.md` left this entry's scope entirely (rewritten under TD-8, with a dated supersession banner), and the two lines that *pointed at* it were corrected in the same pass so the docs don't contradict each other: `PROGRESS.md:108` no longer advertises a "Railway/Fly API + static web", and `docs/security.md` H-2 no longer says the global IP limit "requires trusted-proxy config in production" — that is **resolved in code** (`apps/api/src/middleware/rateLimit.ts:7-18` prefers `x-vercel-forwarded-for`, else the right-most XFF hop, never the client-supplied left-most). Everything else in this entry is untouched and still real; the remaining items were deliberately **not** cherry-picked, because piecemeal edits across eight docs is what produced this drift — they want the single coherent pass with banners. Note `autonomy-audit.md` row 11 still repeats the "deploy.md still says Railway/Fly" claim and is now stale, but it lives under `docs/engineering/**` (Level C) so the sweep left it for Branden. |
| TD-14 | P3 | deps hygiene | @capacitor/cli in `dependencies` (`apps/web/package.json:33`) rather than devDependencies — a build-time CLI declared as a runtime dep. **Severity lowered P2→P3 on 2026-08-16: the security driver is gone.** The entry was opened because the placement dragged tar (critical advisory) into prod-scope audits; as of this sweep `npm audit --omit=dev` reports **`found 0 vulnerabilities`**, so no advisory currently reaches prod scope through it. What remains is hygiene — the misplacement is still real and would re-expose the same class the next time a transitive advisory lands. | Move to devDependencies + `npm audit fix` (NOT --force) + verify `cap sync` and both builds. | open — **the predicted re-exposure fired 2026-09-03** (daily sweep): the new `@xmldom/xmldom` advisory (GHSA-6gmq-8vp8-gcm6, moderate) reached **prod-scope audit** (`npm audit --omit=dev`, was 0 since 08-16) solely through this misplacement — chain `@xmldom/xmldom` ← `plist` ← `@capacitor/cli` in `dependencies`. Cleared same-day by merging Dependabot PR #7 (0.9.12, lockfile-only), so prod scope is back to 0, but the entry's thesis is now demonstrated rather than hypothetical: the move to `devDependencies` retires this recurring class. |
| TD-15 | P2 | native | Contradictory claims about isPluginAvailable() for runtime-registered plugins (instagramStory.ts:24-27 vs NativeCameraRecorder.tsx:21) — one is wrong; segment recording gating may be affected. | One simulator log check; correct whichever comment/gate is wrong. | open |
| TD-16 | P3 | media | video_assets has no ready_at/error_at — transcode latency unmeasurable; abandon windows were chosen blind. | Additive migration + set in finalizeProviderAsset. | open |
| TD-17 | P3 | repo | `.github/modernize/java-upgrade/` — unowned Java-tool residue in a TS monorepo. | Confirm origin with Branden (rule 16), delete. | open |
| TD-18 | P3 | majors | Deliberate one-at-a-time major queue: Vite 5→8 (first — unblocks esbuild advisories), zustand 5, zod 4, React 19, TS 7, @hono/node-server 2. | Each is its own researched project per DEPENDENCY policy in renovate.json. | open — Dependabot **security** PR #6 (esbuild 0.24.2→0.28.2 + grouped vite bump) is open & CI-green but held for owner: it's the first (Vite) item of this queue and needs a local build + iOS-Simulator smoke; not an unattended merge (2026-08-09 sweep, risk summary on PR #6). **PR #3** (`@hono/node-server` 1.19.17→2.0.10) is the queue's last item and, as of 2026-08-13, **carries no security driver at all** — TD-19 closed when GitHub narrowed the advisory range off 1.19.17, so #3 is now a purely optional major with no urgency. **2026-09-03 (daily sweep):** npm's advisory DB now surfaces vite's three direct advisories in `npm audit` (GHSA-4w7w-66w2-5vf9 + GHSA-v6wh-96g9-6wx3 MODERATE, GHSA-fx2h-pf6j-xcff **HIGH** `server.fs.deny` bypass on Windows) — previously it listed vite only as esbuild's dependent. **Not new exposure**: all three have been open Dependabot alerts since 08-08 (recorded in the 08-31 weekly), all dev-server scope, all fixes semver-major; noted so the changed audit output isn't misread as a new incident. |
| TD-20 | P1 | secrets | **Leaked Supabase PAT in public git history.** GitHub secret-scanning alert #1 (open, `publicly_leaked:true`) — a `sbp_…` Supabase Personal Access Token committed on 2026-07-15 in `.codex/config.toml` at commit `16355f66` ("Stop the app making claims that aren't true"), which is on `origin/main`. Recurrence of the CLAUDE.md hard-rule-1 PAT-leak class. **Contained going forward:** `.codex/` is now gitignored (`.gitignore:58`) so it can't re-leak, the file is untracked at HEAD, and the leaked value is absent from every current working-tree `.toml/.json/.env` (local config already replaced). **Not verified:** whether the leaked token is actually revoked on Supabase's side — the alert being open means it may still be live. | **Owner (Level D):** (1) revoke that PAT in Supabase → Account → Access Tokens if not already; (2) resolve/dismiss GitHub secret-scanning alert #1 as "revoked". History rewrite is optional and unnecessary once revoked (and would be a destructive force-push — owner decision only). | ✅ closed 2026-08-10 — owner revoked the PAT; GitHub secret-scanning alert #1 resolved as `revoked` (resolved_at 2026-08-10T14:52:32Z), open-alerts list verified empty via the API (2026-08-10 sweep). Containment from 08-09 still holds (`.codex/` gitignored, leaked value absent from working tree). History rewrite deliberately not done — unnecessary once revoked, and a destructive force-push on shared `main`. |
| TD-19 | P3 | api deps | `@hono/node-server` 1.19.17 (was 1.19.14; patched up in the 2026-08-08 Dependabot triage) — serve-static path traversal on Windows via encoded backslash (`npm audit` MODERATE; surfaced 2026-08-07 sweep, now the top prod-scope-adjacent item after hono core cleared). **Non-exploitable on two independent counts, re-verified 2026-08-10 sweep:** (1) the vulnerable code is the `serve-static` middleware, which is **never imported anywhere** in the API (`grep -rn serveStatic apps/api` → 0 matches); only `getRequestListener` — a different, unaffected export — is used, and it IS the prod Vercel adapter (`api/index.ts:28`). (2) production runs on Vercel **Linux**; the advisory is **Windows-only** (`%5C`→`\`). *(Corrects the earlier "not in the prod path / uses hono/vercel adapter" note — there is no `hono/vercel` usage; node-server's `getRequestListener` is the prod request adapter. Conclusion unchanged: non-exploitable.)* | Bump when a 1.x fix publishes, or fold into the `@hono/node-server` 2 major already queued in TD-18. No prod action needed. | ✅ closed 2026-08-13 — **the advisory no longer covers the installed version.** GitHub narrowed GHSA-frvp-7c67-39w9's range on `updated_at 2026-08-12T18:26:51Z` (after the 08-12 sweep ran) to `< 1.19.15` **and** `>= 2.0.0, < 2.0.5`; installed/lockfile version is **1.19.17**, outside both. Verified: `npm audit` now reports 2 advisories (was 3) with **zero** hono mentions; the `@hono/node-server` Dependabot alerts (#1/#16) are gone from `dependabot/alerts` at **every** state, not merely dismissed. The other published node-server advisory, GHSA-9mqv-5hh9-4cgg (WebSocket handshake DoS), is `>= 2.0.0, <= 2.0.9` — 1.x never affected. So 1.19.17 is clean on both published advisories on version grounds alone; the prior two-fold non-exploitability argument (re-verified this sweep: `serveStatic` 0 hits, `upgradeWebSocket` 0 hits, `getRequestListener` is the prod adapter at `api/index.ts:28`, prod is Vercel Linux) is now belt-and-braces rather than the load-bearing reason. **PR #3 is therefore no longer a security PR** — it is a plain optional major, tracked only by TD-18. |
| TD-21 | P2 | ops tooling | **The daily sweep's DB-state check has a single point of failure and no fallback.** On 2026-08-16 the Supabase MCP returned `Unauthorized. Please provide a valid access token … via the --access-token flag or SUPABASE_ACCESS_TOKEN` on every call (`execute_sql`, `get_advisors`), so sweep items 5 (parked `pending_media_deletions`, `video_assets` stuck >6h, `cron_runs` repeated failures) and 5b (Supabase security advisors) could not run. Sweeps 08-11…08-14 used it successfully, so this is new. Root cause **not determined**: the MCP reads its credential from the local-only `.mcp.json`, and reading that file is (correctly) permission-blocked for the unattended agent, so the token cannot be inspected from inside a sweep. No repo script offers an alternative read-only path — `scripts/` holds only `gen-apple-secret`, `safety-diff`, `secrets-check`, `verify-deploy` and `ops/*.sh`. Partial cover exists but is **not** equivalent: `/health` independently proved `stuckVideoBacklog` 0 (same table, 15-min threshold ⇒ subsumes the >6h question) and all five `cronAges` fresh with `problems:[]` (⇒ no cron failing longer than its stale threshold). **Genuinely unchecked this sweep: `pending_media_deletions` attempts≥10, and the Supabase advisor baseline.** | Three options, cheapest first. (a) **Owner/Level D:** re-authorize the Supabase MCP (verify/rotate the PAT in `.mcp.json`) — restores items 5/5b exactly as before. (c) **Owner, one-line config, no new credential:** the **claude.ai Supabase connector** (`mcp__claude_ai_Supabase__*`) is already connected in the sweep session — 2026-08-17 confirmed it is *not* in the auth-required list, and `list_projects` failed only with "requested permissions … not granted" — but `.claude/settings.json` allowlists just the `.mcp.json`-backed `mcp__supabase__{execute_sql,list_tables,get_logs,get_advisors}`. Adding the connector's `list_projects`/`execute_sql`/`get_advisors` to that allowlist gives sweeps an **independent second path** (authenticated via Branden's claude.ai account, not the PAT), which is the real SPOF fix. The unattended agent deliberately did **not** self-grant this — widening its own permission set is an owner call. (b) **Level B, service-role fallback:** `scripts/ops/db-state.mjs` running the three sweep queries off the API's service-role env — still valid, but note the agent cannot verify it against a real DB unattended (`Read(./apps/api/.env*)` is denied by design), so it needs one attended run before it counts as a working fallback, and it puts a service-role key in the sweep's hands where (c) does not. | open — raised 2026-08-16; **still Unauthorized 2026-08-17** (`execute_sql` same error), items 5/5b unchecked a second day; option (c) identified 2026-08-17. **Weekly security sweep 2026-08-17 (same day, later):** both paths still blocked (`get_advisors` Unauthorized; connector `list_projects` still "permissions not granted"), so the advisor baseline and the `pending_media_deletions` check remain uncovered. Partial mitigation shipped: `scripts/ops/anon-boundary-probe.mjs` (see TD-7) covers the *anon* half of the weekly RLS spot-check with no credential at all and passed clean against prod. It does not replace advisors or the parked-deletions query. **Daily sweep 2026-08-18: day 4, both paths still blocked** (`.mcp.json` PAT `execute_sql` → same Unauthorized; connector `list_projects` → still "permissions not granted"); anon-boundary probe re-run clean as the partial substitute. **Daily sweep 2026-08-19: day 5, both paths still blocked (identical errors); anon-boundary probe re-run clean. Half the gap is now permanently closed in-lane:** `/health` gained a `parkedMediaDeletions` gauge (`health.ts`, service-role count of `pending_media_deletions` where `attempts >= 10`), so the sweep no longer needs a DB path to answer that question — it reads it from the same probe it already runs. Chosen over TD-21 option (b) because it adds **no** new credential anywhere and reuses the service-role the API already holds; report-only, deliberately excluded from `problems` so a parked row can never 503 the API and trip uptime alerting (regression-guarded by `tests/invariants/security-invariants.test.mjs` → "parked media deletions stay visible on /health"). Note this was never *unalarmed* — `drainMediaDeletions` already `captureException`s every run while any row is parked (`internal.ts`) — it was merely invisible to the sweep. **Remaining gap after this: the Supabase security-advisor baseline only** (item 5b), which still needs option (a) or (c). **Daily sweep 2026-08-20: day 6, both paths still blocked** (`execute_sql` → identical Unauthorized; connector `list_projects` → still not allowlisted). **First sweep where item 5 is fully answered without a DB path** — the gauge shipped on 08-19 read `parkedMediaDeletions: 0` live today, `stuckVideoBacklog: 0` (15-min threshold ⇒ strictly subsumes the ">6h non-ready" question), and all five `cronAges` inside `CRON_STALE_SECONDS` with `problems: []` (⇒ no cron failing repeatedly). Anon-boundary probe re-run clean. So TD-21's cost is now exactly one item — **the advisor baseline (5b)** — and it is the only reason this entry stays open. **Daily sweep 2026-08-21: day 7, both paths still blocked** (`get_advisors` → identical `Unauthorized`; claude.ai connector `list_projects` → still not granted). Two things narrowed the remaining gap without a credential. (1) A third path was tested and **ruled out**: PostgREST's OpenAPI root (`GET /rest/v1/` with the public publishable key) would have enumerated the exposed schema, but production returns **401** to anon — good posture, dead end for us. (2) The anon-boundary probe was instead pointed at the schema the *repo* declares, and now covers all 49 migration-declared tables (TD-26). That materially substitutes for the advisor's `rls_disabled_in_public` / `policy_exists_rls_disabled` lints **from the anon side across the whole schema**, which is the half of item 5b that could actually hurt users. Genuinely still unverified: the advisor's `authenticated`-side view, `function_search_path_mutable`, the `pg_trgm`/deny-all INFO baseline, and `password_hibp_enabled`. **Daily sweep 2026-08-23: day 8, both paths still blocked** — `mcp__supabase__get_advisors` → identical `Unauthorized … --access-token flag or SUPABASE_ACCESS_TOKEN`; the claude.ai connector's `list_projects` → still not permitted (not self-granted; widening the agent's own permission set is an owner call). Item 5 answered without a DB path as usual (`parkedMediaDeletions` 0, `stuckVideoBacklog` 0, five fresh `cronAges` with `problems: []`), and the whole-schema anon-boundary probe re-run clean (47 tables 0 rows · 3 `hashtags` predicate checks · 12 `profiles` PII columns denied · `creator_earnings` not executable · positive control readable). Residual gap unchanged from day 7. **Daily sweep 2026-08-24: day 9, both paths still blocked** — `mcp__supabase__get_advisors` → identical `Unauthorized … --access-token flag or SUPABASE_ACCESS_TOKEN`; claude.ai connector `list_projects` → still "permissions not granted" (not self-granted, as before). Item 5 answered without a DB path (`parkedMediaDeletions` 0 · `stuckVideoBacklog` 0 · five fresh `cronAges` with `problems: []`), anon-boundary probe re-run clean (47 tables 0 rows · 3 `hashtags` predicate checks · 12 `profiles` PII columns denied · `creator_earnings` not executable · positive control readable). Residual gap unchanged. **Weekly security sweep 2026-08-24 (same day, later): second consecutive weekly sweep with the advisor baseline dark** — both paths re-tried and still blocked (`get_advisors` → identical `Unauthorized`; connector `list_projects` → still not granted, not self-granted). The weekly-specific consequence: the 2026-08-10 auth hardening (`password_hibp_enabled` + 10-char server floor) has now gone two weekly regression checks without verification. Anon-boundary probe re-run clean (49/49) as the substitute; full detail in the 2026-08-24 weekly entry in `docs/operations/incidents/LOG.md`. **Daily sweep 2026-08-25: day 10, both paths still blocked** — `mcp__supabase__get_advisors`/`execute_sql` → identical `Unauthorized … --access-token flag or SUPABASE_ACCESS_TOKEN`; claude.ai connector `list_projects` → still permission-gated (not self-granted, as before). Item 5 answered without a DB path (`parkedMediaDeletions` 0 · `stuckVideoBacklog` 0 · five fresh `cronAges` with `problems: []`), anon-boundary probe re-run clean (47 tables 0 rows · 3 `hashtags` predicate checks · 12 `profiles` PII columns denied · `creator_earnings` not executable · positive control readable). Residual gap unchanged. **Daily sweep 2026-08-27: day 12, both paths still blocked** (no sweep ran 08-26) — `mcp__supabase__get_advisors` → identical `Unauthorized … --access-token flag or SUPABASE_ACCESS_TOKEN`; claude.ai connector `list_projects` → still permission-gated (not self-granted, as before). Item 5 answered without a DB path (`parkedMediaDeletions` 0 · `stuckVideoBacklog` 0 · five fresh `cronAges` with `problems: []`), anon-boundary probe re-run clean (47 tables 0 rows · 3 `hashtags` predicate checks · 12 `profiles` PII columns denied · `creator_earnings` not executable · positive control readable). Residual gap unchanged. **Daily sweep 2026-08-31: day 16, both paths still blocked** (no sweeps ran 08-28…08-30) — `mcp__supabase__get_advisors` → identical `Unauthorized … --access-token flag or SUPABASE_ACCESS_TOKEN`; claude.ai connector `list_projects` → still permission-gated (not self-granted, as before). Item 5 answered without a DB path (`parkedMediaDeletions` 0 · `stuckVideoBacklog` 0 · five fresh `cronAges` with `problems: []`), anon-boundary probe re-run clean (47 tables 0 rows · 3 `hashtags` predicate checks · 12 `profiles` PII columns denied · `creator_earnings` not executable · positive control readable). Residual gap unchanged. **Weekly security sweep 2026-08-31 (same day, later): third consecutive weekly sweep with the advisor baseline dark** — both paths re-tried and still blocked (`get_advisors` → identical `Unauthorized … SUPABASE_ACCESS_TOKEN`; claude.ai connector `list_projects` → still permission-gated, not self-granted). The 2026-08-10 auth hardening (`password_hibp_enabled` + 10-char server floor) has now gone **three** weekly regression checks without verification. Anon-boundary probe re-run clean (49/49) as the substitute; full detail in the 2026-08-31 weekly entry in `docs/operations/incidents/LOG.md`. **Daily sweep 2026-09-02: day 18, both paths still blocked** (no sweep ran 09-01) — `mcp__supabase__get_advisors` → identical `Unauthorized … --access-token flag or SUPABASE_ACCESS_TOKEN`; claude.ai connector `list_projects` → still permission-gated (not self-granted, as before). Item 5 answered without a DB path (`parkedMediaDeletions` 0 · `stuckVideoBacklog` 0 · five fresh `cronAges` with `problems: []`), anon-boundary probe re-run clean (47 tables 0 rows · 3 `hashtags` predicate checks · 12 `profiles` PII columns denied · `creator_earnings` not executable · positive control readable). Residual gap unchanged. **Daily sweep 2026-09-03: day 19, both paths still blocked** — `mcp__supabase__execute_sql`/`get_advisors` → identical `Unauthorized … --access-token flag or SUPABASE_ACCESS_TOKEN`; claude.ai connector → still permission-gated (`list_projects` → "requested permissions … not granted"; not self-granted, as before). Item 5 answered without a DB path (`parkedMediaDeletions` 0 · `stuckVideoBacklog` 0 · five fresh `cronAges` with `problems: []`), anon-boundary probe re-run clean (47 tables 0 rows · 3 `hashtags` predicate checks · 12 `profiles` PII columns denied · `creator_earnings` not executable · positive control readable). Residual gap unchanged. **Daily sweep 2026-09-04: day 20, both paths still blocked** — `mcp__supabase__get_advisors` → identical `Unauthorized … --access-token flag or SUPABASE_ACCESS_TOKEN`; claude.ai connector `list_projects` → still permission-gated ("requested permissions … not granted"; not self-granted, as before). Item 5 answered without a DB path (`parkedMediaDeletions` 0 · `stuckVideoBacklog` 0 · five fresh `cronAges` with `problems: []`), anon-boundary probe re-run clean (47 tables 0 rows · 3 `hashtags` predicate checks · 12 `profiles` PII columns denied · `creator_earnings` not executable · positive control readable). Residual gap unchanged. **Daily sweep 2026-09-05: day 21, both paths still blocked** — `mcp__supabase__get_advisors` → identical `Unauthorized … --access-token flag or SUPABASE_ACCESS_TOKEN`; claude.ai connector `list_projects` → still permission-gated ("requested permissions … not granted"; not self-granted, as before). Item 5 answered without a DB path (`parkedMediaDeletions` 0 · `stuckVideoBacklog` 0 · five fresh `cronAges` with `problems: []`), anon-boundary probe re-run clean (47 tables 0 rows · 3 `hashtags` predicate checks · 12 `profiles` PII columns denied · `creator_earnings` not executable · positive control readable). Residual gap unchanged — **three full weeks** of the advisor baseline (item 5b) being the single reason this entry stays open, which is itself the argument for option (a)/(c) getting an owner slot. **Daily sweep 2026-09-06: day 22, both paths still blocked** — `mcp__supabase__get_advisors` → identical `Unauthorized … --access-token flag or SUPABASE_ACCESS_TOKEN`; claude.ai connector `list_projects` → still permission-gated ("requested permissions … not granted"; not self-granted, as before). Item 5 answered without a DB path (`parkedMediaDeletions` 0 · `stuckVideoBacklog` 0 · five fresh `cronAges` with `problems: []`), anon-boundary probe re-run clean (47 tables 0 rows · 3 `hashtags` predicate checks · 12 `profiles` PII columns denied · `creator_earnings` not executable · positive control readable). Residual gap unchanged. **Daily sweep 2026-09-24: both paths still blocked — and, new this sweep, *every documented substitute is dark too*.** `mcp__supabase__get_advisors` → identical `Unauthorized … --access-token flag or SUPABASE_ACCESS_TOKEN`. The three fallbacks that have carried items 5/5b since 2026-08-17 all failed this sweep **for a different reason than the credential** — the open SEV-1 (`docs/operations/incidents/2026-09-21-supabase-project-unreachable.md`) has withdrawn the project's DNS records. (1) `/health`'s `stuckVideoBacklog` and `parkedMediaDeletions` gauges both return **`null`** and `cronAges` is **`null`**, because the service-role DB probe fails first — so the 08-19 gauge that "permanently closed half the gap" answers nothing during an outage. (2) `scripts/ops/anon-boundary-probe.mjs` — the TD-7/TD-26 credential-free substitute for the advisor RLS lints — **dies before its first assertion**: `TypeError: fetch failed … getaddrinfo ENOTFOUND gsxoaurmsgqascxukony.supabase.co`. **The structural point, verified rather than inferred:** TD-21's mitigations bought path diversity in *credential* terms (service-role gauge · anon black-box probe · MCP PAT) but **not in *dependency* terms — all three terminate at the same Supabase hostname**, so one DNS withdrawal takes the primary and every fallback simultaneously. Items 5 **and** 5b are therefore both fully unanswerable, the first time that has been true since the gauge shipped on 2026-08-19 (every prior blocked day still answered item 5). This does not change the close condition — option (a) PAT rotation or (c) connector grant — but note **neither would have helped today**; only the SEV-1 restore re-lights these checks. The one item-5b channel that is *not* Supabase-dependent was run and is clean: GitHub secret-scanning, **0 open alerts**. |
| TD-22 | P3 | api auth | **`CRON_SECRET` bearer check is not constant-time.** `apps/api/src/routes/internal.ts:42` gates all five crons with a plain `!==` string compare of the `authorization` header against `Bearer <CRON_SECRET>`, whereas the three webhooks (`monetize.ts` Stripe + RevenueCat, `uploads.ts` Cloudflare) all use `timingSafeEqual` with a length pre-check. Surfaced by the 2026-08-17 security sweep's route-auth pass. **Practically non-exploitable** — recovering a long random secret one byte at a time needs sub-microsecond timing resolution that Vercel serverless + network jitter does not offer — so this is consistency/hardening, not a live hole. | Mirror the RevenueCat pattern: length check + `timingSafeEqual(Buffer.from(auth), Buffer.from(expected))`; keep the `!expected` fail-closed branch. **Level C** (auth gate on the cron path — a mistake here silently 401s every cron, incl. finalize-videos): ship attended, verify with one authorized cron call + `/health cronAges` advancing afterwards. Not shipped unattended for exactly that blast radius. | open |
| TD-24 | P3 | ops tooling | **`verify-deploy.mjs` hard-crashes on a stale Vercel CLI token instead of refreshing it.** The script reads the raw token out of `~/Library/Application Support/com.vercel.cli/auth.json` (`scripts/verify-deploy.mjs:35-37`); when that token has gone stale the Vercel API returns 403 and `latestFor()` throws an unhandled `Error: vercel api 403`, killing the run with a stack trace before either project is checked (hit for the first time on the 2026-08-19 daily sweep, immediately after a successful push). The script's own docstring already names the recovery — "run `vercel whoami` first if the token has gone stale (it refreshes in place)" — and that is exactly what worked: one `vercel whoami` (returned `branden574`) then a clean re-run verifying both projects READY. So this is a robustness gap, not a broken tool: the *fix is known and documented*, it just isn't automated, and an unattended sweep that stops at the stack trace would report a push as unverified when it actually deployed fine. | Catch the 403 in `latestFor()`, run `vercel whoami` once (it refreshes the token in place), re-read `auth.json` and retry once before failing; keep any other status a hard error. Low risk — verification-only tooling, no production effect, and the failure mode it removes is a false "unverified deploy". | ✅ closed 2026-08-20 — fixed as specified, **and the fix proved itself on its first real run**: the smoke run of the rewritten script hit `vercel api 403` again on the very first API call (so this recurs daily, not once), auto-refreshed and completed — `sizzle` READY / probe 200 / serving `9b0086f` == HEAD / health `ok`, `sizzle-api` READY / `getsizzle.app` 200. The old code would have died there with a stack trace before checking either project. Implementation: `createVercelClient()` in `scripts/verify-deploy.mjs` owns the token and retries once on **401 or 403** (403 is what was observed; 401 is the same class) after `vercel whoami` re-mints `auth.json`; refresh is capped at **once per process** so a genuinely dead token fails fast instead of spinning `whoami` on every 10s poll tick; every non-auth status stays a hard error; a failed `whoami` raises a `vercel login` hint rather than an opaque spawn error. Regression-guarded by 5 new tests in `tests/invariants/ops-tooling.test.mjs` (refresh-and-retry with the RE-READ token · 401 same class · at-most-once · 404/429/500 never refresh · failed-refresh message) — a real guard, not a tautology: `HEAD:scripts/verify-deploy.mjs` has **0** exports and self-executes its poll loop at import, so these tests cannot even load against the old file. |
| TD-23 | P3 | ops tooling | **The local watchdog summons on host-side network blips.** `scripts/ops/watchdog.sh` treats a single `curl` returning `000` (connection/DNS-layer failure) as an incident and summons a full unattended session. Three times now (2026-08-10 07:47, 2026-08-17 15:53, 2026-08-21 06:48 PDT — **3 of the 4 real summons to date**) the trigger was a momentary host-side network failure on the Mac while production was up the whole time. **Two variants, one signature:** 08-10 and 08-17 were resolver failures (`curl: (6) Could not resolve host`); **08-21 was a TCP-connect failure with DNS working** (`curl: (7) Failed to connect to sizzle-chi.vercel.app port 443 after 139 ms`) — so the class is "host-side network blip", not specifically DNS. In every case both surfaces failed in the same second on *different* Vercel anycast IPs and recovered untouched within ~25 s. Off-Mac corroboration each time: on 08-17 the GitHub `Uptime` run logged HTTP 200 for both surfaces 20 s before the local probe failed; on 08-21 the scheduled run at 13:32:37Z was green, a `workflow_dispatch` run at 13:51:32Z (3 min after the blip) was green, and neither Vercel project had deployed in 22 h. Each false summon burns a paid session and the 60-min cooldown. The true-outage alert path is already covered independently by `.github/workflows/uptime.yml` (3 probes 45 s apart, GitHub-side), so a short local confirm delay costs nothing in coverage. | In `watchdog.sh`: when a probe returns `000` (not a 5xx — a 5xx is a real origin answer), `sleep 20` and re-probe once before appending to `PROBLEMS`; optionally also require the latest `Uptime` run to be non-success before summoning on `000`. **Keying on `000` is confirmed to cover both observed variants** — `curl` reports `000` for the (6) resolver failure *and* the (7) connect failure, so no extra branch is needed for the 08-21 case. Test with `SIZZLE_WATCHDOG_TEST_URL=https://does-not-exist.invalid/health` + `SIZZLE_WATCHDOG_TEST_PROMPT` and confirm one retry then no summon-on-DNS-blip vs. still-summons on a real 5xx. **Attended (Level C by blast radius, not by path list):** launchd executes the repo file directly, so an edit is live at the next 5-min tick with no CI/deploy gate, and a bug would suppress *real* summons. | open — filed 2026-08-17 by the 15:53 false-alarm session; **third occurrence 2026-08-21 06:48 → recommend P3→P2** (3 of 4 real summons are now false alarms, and the remedy as specified would have suppressed all three). Still deliberately not shipped unattended, for the reason in the fix column. **Fourth occurrence 2026-08-28 12:50:10 PDT** — same `curl (7)` connect-failure variant (TCP connect to sizzle-chi.vercel.app failed in **162 ms**; frontend `000000` in the same tick) → **4 of 5 real summons now false**. Off-Mac bracketing green again: scheduled `Uptime` run 19:39:37Z success ~10.5 min before, `workflow_dispatch` run 19:51:19Z success ~1 min after the blip; the summoned session's own probes 49 s after the failed tick got 200/200 with both surfaces serving HEAD `9272d63` (API `/health` commit + web `version.json`), and both Vercel projects' latest production deployments `● Ready` at 1 d old (nothing shipped near the alert). The filed remedy (20 s retry on `000`) would have suppressed this summon too; the P3→P2 recommendation stands. **Fifth occurrence 2026-09-06 13:01:29 PDT (20:01:29Z)** — same `curl (7)` connect-failure variant (TCP connect to sizzle-chi.vercel.app failed in **353 ms**; frontend `000000` in the same tick) → **5 of 6 real summons now false**. Off-Mac bracketing green on both sides: scheduled `Uptime` run 34056354169 at 19:54:49Z success ~6.7 min before, `workflow_dispatch` run 34056788916 at 20:03:28Z success ~2 min after with `attempt 1: HTTP 200` for *both* surfaces (no retries), and **all 40 Uptime runs today green**. The summoned session's own probe ~45 s after the failed tick got 200/200, `/health` `status:ok problems:[]` at commit `9452ee4` (= origin `main`), and both Vercel projects' latest production deployments were `● Ready` and **5 h old** — nothing shipped near the alert, so no rollback was warranted. The filed 20 s-retry remedy would have suppressed this one too. **The cost case is now the argument:** a false summon burns a paid session *and* the 60-min cooldown, during which a genuine outage cannot summon anyone. |
| TD-26 | P2 | db security | **The anon-boundary probe covered less than half the schema, and nothing noticed.** Found 2026-08-21 (daily sweep) while trying to substitute for the blocked Supabase advisors (TD-21). `scripts/ops/anon-boundary-probe.mjs` asserted against a **hand-written list of 22 tables** while `supabase/migrations` declares **49** — so 26 tables (`collections`, `collection_recipes`, `saves`, `downloads`, `reposts`, `reactions`, `comment_likes`, `follows`, `follow_requests`, `conversations`, `user_mutes`, `user_hashtag_preferences`, `creator_products`, `creator_reviews`, `creator_tiers`, `live_sessions`, `cook_events`, `cook_logs`, `rate_limits`, `save_nudges`, `content_hashtags`, `hashtag_metrics`, `hashtag_trend_snapshots`, `hashtags`, `recipe_impressions`, `recipe_views`) were **never probed at all**. The list was accurate when written on 08-17 and decayed silently as it aged; nothing tied it to the schema, so a migration adding a table would keep it invisible indefinitely. This is the tooling-decay class, not a live breach — **all 26 were probed during the fix and production is clean**. | ✅ closed 2026-08-21 — same-sweep fix, three parts. (1) Classification extracted to `scripts/ops/anon-boundary-tables.mjs`, expanded to **47** must-yield-no-rows tables + `profiles` (column-scoped) + `hashtags` (readable by design), each classified by reading its migration's grants/policies rather than by copying what prod answered. (2) **`hashtags` is anon-readable by design** — policy `hashtags_public_read` (`20260718120000`, tightened by `20260718122000`) grants anon `select using (status = 'active' and not is_blocked and not is_sensitive)` — so the probe now asserts the *predicate* live: `is_blocked=eq.true`, `is_sensitive=eq.true` and `status=neq.active` must each return 0 rows. That keeps "readable by design" from degrading into "readable, unchecked". (3) The decay itself is now CI-enforced: `security-invariants.test.mjs` → "the anon-boundary probe classifies every table the migrations declare" parses every `create table` out of the migrations and fails on any unclassified table, any classified-but-undeclared table, any table in both buckets, and any anon-readable table with no predicate check. **Verified a real guard, not a tautology:** removing `saves` from the list makes it fail, restoring it makes it pass (20/20 invariants green). **Live result:** all 49 tables clean — 47 yield 0 rows to anon, the 3 `hashtags` predicate checks pass, 12 `profiles` PII columns still `401 42501`, `creator_earnings` still not executable, positive control readable. Honest limit: still anon-only and still black-box, so TD-7 (SQL grant matrix, `authenticated` role) stays open. | ✅ closed 2026-08-21 |
| TD-25 | P3 | deps automation | **`renovate.json` is dead config, and no engine proposes routine (non-security) dependency updates.** Verified 2026-08-20: `renovate.json` exists at the repo root (written in the 2026-08-06 audit, "patch automerge only; native/money never"), but Renovate has **never opened a PR** — all 6 PRs in repo history are `dependabot/*` or the original `codex/*` branch, 0 match `renovate`. Installing the Renovate GitHub App was listed as owner action #2 in `autonomy-audit.md` and was never done. Meanwhile there is **no `.github/dependabot.yml`**, so Dependabot runs in GitHub's default **security-updates-only** mode: it opens a PR when an advisory lands and otherwise never runs. (That also explains the "Dependabot Updates" workflow showing no run since 2026-08-10 — it is advisory-driven, not scheduled, and the open alert set has been the same 7 since 2026-08-08. Not dead automation.) **Consequence:** the daily sweep's triage rule 3 ("CI green + patch/minor + not native/payment/auth ⇒ merge") has had nothing to act on since it was written and structurally never will — routine patch/minor upgrades are simply never proposed, so the tree drifts until something becomes a *security* problem. Also observed: the 2026-08-10 Dependabot security job for `vite` failed with `dependency_file_not_supported` (run 31406439748); coverage was not lost (PR #6 already carries vite+esbuild) but it shows the updater does not cleanly handle this workspace layout for that dep. | Pick one engine and make it real, then delete the other's config so the repo stops describing automation it doesn't have. Either (a) install the Renovate GitHub App and let the existing `renovate.json` take effect, or (b) add `.github/dependabot.yml` with a weekly `npm` schedule for `/`, `/apps/web`, `/apps/api` and delete `renovate.json`. **Owner call:** (a) is a GitHub App install (Level D); (b) touches `.github/**` (Level C). Neither is shippable unattended. | open — filed 2026-08-20 by the daily sweep. **2026-09-03: the triage rule fired for the first time** — Dependabot **security** PR #7 (`@xmldom/xmldom` 0.9.10→0.9.12: patch, lockfile-only, CI green, non-native/non-payment/non-auth) was squash-merged unattended per sweep rule 3. This doesn't change the entry's thesis: the lane works when an advisory-driven PR appears, but routine (non-security) patch/minor updates are still never proposed by anything. |
| TD-27 | P2 | ops tooling | **The unattended sweep cannot observe the current state of `main`, so it reasons about a stale working tree.** `.claude/settings.json` allowlists `git status/diff/log/add/commit/push/show/stash` but **not `git fetch` or `git pull`** — both prompt for approval, which an unattended session cannot grant. Whenever a sweep advances *remote* `main` without a local fast-forward — now routine, since the 2026-09-03 sweep merged Dependabot PR #7 with `gh pr merge` and then pushed its docs through the GitHub git-data API — local `main` stays behind and **every later sweep reads stale files**. Verified 2026-09-04: local HEAD `d4c5395` vs origin `d3e526f` (2 commits behind), and the stale `package-lock.json` made `npm audit` re-report `@xmldom/xmldom` GHSA-6gmq-8vp8-gcm6 as a live moderate advisory **that had been fixed upstream the previous day**. It was caught only because `gh run list --workflow ci.yml` showed a `Bump @xmldom/xmldom … (#7)` run absent from local history; without that cross-check this sweep would have spent its one in-lane ship re-doing yesterday's work and logged a false "new advisory cleared" claim. The 09-03 entry in `LOG.md` recorded the same mechanics as a one-off "for the next sweep's awareness" — one day later it produced a near-miss, so it is recurring, not incidental. The staleness silently degrades every repo-derived sweep check (flag drift, lint baselines, file-existence spot checks, `npm audit`). | Add `Bash(git fetch:*)` to `permissions.allow` in `.claude/settings.json`. `git fetch` is read-only — it updates remote-tracking refs and cannot alter the working tree, the index, or local branches — so it carries none of the risk that keeps `git pull`/`git merge` out (an unattended fast-forward over uncommitted work is a separate question and should stay denied). **Owner call:** an agent may not widen its own permission set, so this is not shippable unattended. Until it lands, the workaround is mandatory and must run **before** sweep items 4 and 6: compare `git rev-parse HEAD` with `gh api repos/Branden574/Sizzle/commits`, and when they differ, read every file the sweep reasons about via `gh api …/contents/<path>?ref=<origin-head>` rather than the working copy, and build any doc edit on that content. | open — filed 2026-09-04 by the daily sweep that hit the near-miss above. **Recurred immediately on 2026-09-05** (local `d4c5395` vs origin `91aba12`, **3** commits behind — the drift grew, it did not settle), so the mandatory workaround was **automated in-lane rather than hand-run a third time**: `scripts/ops/origin-drift.mjs` compares `git rev-parse HEAD` against `gh api …/commits/main`, and on drift classifies each changed file by *which sweep check it corrupts* (lockfile → item 4 `npm audit`; `technical-debt.md`/`LOG.md` → the sweep's own doc appends; `scripts/`+`tests/` → re-fixing finished work), mirrors origin's copies into `.codex/origin-<sha>/`, prints the exact `npm audit --package-lock-only` command for the origin tree, and exits **3** (0 = in sync, 1 = tool failure). It is read-only by construction — `git rev-parse` plus `gh api` GETs, writes only under the gitignored `.codex/` — and it is wired in as **step 0 of `scripts/ops/sweep-prompt.md`**, which is the part that matters: the workaround previously existed only as prose a future sweep had to remember. Proven on the live stale tree this sweep (correctly named all 3 drifted files and their corrupted checks; its printed audit command reproduced the same 2-advisory result as the hand-built tree). Guarded by 6 new tests in `tests/invariants/ops-tooling.test.mjs`, verified as real guards by mutation: breaking the lockfile matcher fails 2, and removing `origin-drift` from the sweep prompt fails the wiring test (38/38 green restored). **The entry stays open and P2** — this lowers the odds of a *missed* check, but it cannot make local `main` current, so the sweep still cannot run repo-derived checks against a tree that simply is the truth. The one-line owner fix (`Bash(git fetch:*)`) is unchanged and still the actual close condition. **2026-09-06 — the 09-05 automation is INERT, and the entry is worse than it looked. Recommend P2→P1.** Third consecutive recurrence, and the drift is still growing (2 → 3 → **4** commits: local `d4c5395` vs origin `f64e139`). The new finding is the mechanism: `scripts/ops/daily-sweep.sh:23` builds the agent's prompt with `cat scripts/ops/sweep-prompt.md` — read from the **local working tree**. Because local `main` is frozen at `d4c5395`, which predates the `f64e139` commit that added step 0, **today's sweep was launched with a prompt containing no step 0**, and `node scripts/ops/origin-drift.mjs` would have died `MODULE_NOT_FOUND` because the script did not exist locally either. So the fix shipped on 09-05 to make the stale-tree workaround unskippable *cannot execute*, and — the general form — **any sweep-authored improvement to the sweep's own tooling is dead on arrival**: it lands on origin and can never reach the runner that needs it. The staleness withholds the very tool built to detect the staleness. Today's sweep performed the workaround correctly only because the agent recalled it from project memory rather than from its instructions; memory is not a control, and the next sweep may not have it. **In-lane mitigation applied (checkout repair, not a code change):** the three `f64e139` ops files — `scripts/ops/origin-drift.mjs`, `scripts/ops/sweep-prompt.md`, `tests/invariants/ops-tooling.test.mjs` — were materialised into the local checkout from `gh api …/contents/<path>?ref=f64e139` and verified **byte-identical to origin** with `diff` (empty), so the next sweep is launched *with* step 0 and *with* a runnable drift tool; `npm run test:invariants` = **38/38** green against them. Nothing was committed for this, because the content already *is* origin's — it shows as locally dirty only relative to the stale `d4c5395` HEAD, which is the bug, not a change. **This mitigation is one-shot and self-erasing** — it repairs the next run, not the mechanism, and the next ops-tooling commit will be inert on arrival exactly as this one was. Only `Bash(git fetch:*)` closes it. **2026-09-23 (session 42) — the phantom-advisory failure mode reproduced, now with the delta measured.** Local `main` is **56 commits** behind (`d4c5395` vs `8eb371c`). `npm audit --package-lock-only` on the working tree returns **4 vulnerabilities (2 moderate, 2 high)**; the same command on `.codex/origin-8eb371c/` returns **3 (2 moderate, 1 high)**. The extra is **`@xmldom/xmldom` 0.9.10, HIGH** — fixed on origin **20 days earlier** (2026-09-03, Dependabot PR #7 → 0.9.12), and `gh api …/compare/d4c5395...8eb371c` confirms that bump is the *only* `package-lock.json` change across all 56 commits. So the 2026-09-04 near-miss that opened this entry is not historical: it is the steady state, on the same package, and the cost of leaving the entry open is now a number rather than a description. The step-0 tool did its job (flagged the lockfile as check-corrupting before any audit ran) — which is the distinction that matters: the *detection* works, the *staleness* does not go away. **2026-09-24 (daily sweep) — same delta, same package, measured a second consecutive day; the phantom is now 21 days stale.** Local `main` is still frozen at `d4c5395`; origin is `ded164d`. `npm audit --package-lock-only` on the working tree returns **4 vulnerabilities (2 moderate, 2 high)** across `@xmldom/xmldom`, `esbuild`, `hono`, `vite`, against **3 (2 moderate, 1 high)** on `.codex/origin-ded164d/`. The extra is the same **`@xmldom/xmldom` 0.9.10 HIGH** that Dependabot PR #7 fixed on origin on **2026-09-03** — so a sweep auditing its own checkout would today report a HIGH advisory that has been fixed for three weeks, and this is now the measured steady state on three separate sweeps (09-04 near-miss, 09-23, 09-24) rather than an incident. Step 0 (`scripts/ops/origin-drift.mjs`) again flagged the lockfile as check-corrupting before any audit ran, and the audit was taken against the mirror as instructed — the control works; the staleness it compensates for does not shrink. Close condition unchanged: `Bash(git fetch:*)`. |
| TD-28 | P2 | crons / observability | **Three of the five `/internal/*` crons return HTTP 200 while the database is unreachable — the query error is destructured away.** Found 2026-09-21 (5th watchdog session of the Supabase SEV-1), confirmed at source and filed 2026-09-21 by the 8th. `supabase-js` resolves query failures as `{ data: null, error }` rather than throwing, and three of the five crons read only `data`: `internal.ts:67` (`const { data: pending }`), `:103` (`staleUploads`), `:110` (`staleProcessing`), `:251` (`const { data: due }`), `:280` (`const { data: saves }`), `:296-298` (`finishes`/`nudged`/`recipes`) — that is, `finalize-videos`, `publish-scheduled` and `save-nudges`. **Corrected 2026-09-24 (62nd watchdog session): the two rollups are NOT affected, and already implement this entry's own prescribed remedy.** `rollup-watch-ratios` (`:337-344`) and `rollup-hashtag-trends` (`:354-361`) each destructure `const { data, error }` and, on error, `console.error` + `captureException` + `return c.json({ ok: false, … }, 500)`. The original filing cited `:344`/`:361` as examples of the defect; they are in fact the counter-example. With the DB down, `data` is `null`, the handler reads that as *"nothing to do"*, and returns a zero-count `c.json(...)` — **HTTP 200**. `recordCronRun` (`:18-33`) cannot correct it either: its heartbeat upsert fails too, and its `catch` only `console.error`s — deliberately, and correctly, since "a heartbeat write failure must never fail the cron that just did its real work" (`:15-16`). Net effect: during an 11h total database outage, Vercel Cron's own success/failure view showed **three green crons and two red ones**. Observed live across sessions 3-6 of this incident (`publish-scheduled` and `finalize-videos` logging 200 repeatedly) while `/health` was 503 `database-unreachable` and `GET /feed/for-you?limit=3` was 500 `db_error`. **Honest scope limit — this is narrower than "crons have no failure signal":** because `last_success_at` never updates, `/health.cronAges` does grow stale and would eventually cross `CRON_STALE_SECONDS`, so TD-4's heartbeat is not defeated in the long run. The defect is at the **HTTP-status layer** — Vercel Cron's per-invocation result, anything keyed on cron exit status, and the handlers' own zero-count summaries, which read identically to a genuinely idle tick. (In this particular outage `/health.cronAges` was `null` anyway — the health DB probe failed first. **"No signal at all" was too strong, corrected 2026-09-24:** the two rollups have been returning 500 *and* firing `captureException` every 15 and 30 minutes for the entire outage, so Vercel Cron's failure view for those two paths — and Sentry — are a real, already-wired signal that simply has no alert attached to it. That is a materially different and cheaper gap than "the cron layer is blind".) | Destructure and check `error` on the **three** affected handlers' data queries (`finalize-videos`, `publish-scheduled`, `save-nudges`); on error `captureException` with the job name and return **500** so Vercel Cron records a real failure. The two rollups at `:337-344`/`:354-361` are the in-repo reference implementation of exactly this shape — copy them, and do **not** churn them; the fix is ~40% smaller than originally filed. **Scope note (2026-09-23, 48th watchdog session): this fix does NOT also close TD-34, despite both entries citing `internal.ts:67`.** After a restore the rescue SELECT succeeds and merely returns empty, so there is no `error` for this guard to catch while the unbounded abandon UPDATEs still flip the stranded cohort. TD-34 needs its own symmetric lower bound — shipping this entry must not be read as covering it. Leave `recordCronRun` best-effort exactly as-is — that part is correct by design. ~6 lines per call site. **Level B** (`routes/internal.ts` is not on the autonomy-policy security-sensitive list, and this touches no auth gate — unlike TD-22, which is Level C by blast radius because it changes the `CRON_SECRET` compare). Unit-testable with a mocked `supabaseAdmin` returning `{ data: null, error }` — no live DB needed for the logic — but the happy path must still be confirmed by one authorized cron tick after recovery, plus `/health cronAges` advancing. | open — filed 2026-09-21. **Deliberately not shipped during the SEV-1**, and four consecutive sessions reached the same call: the change cannot be verified on a real surface while the database does not resolve (CLAUDE.md hard rule 4), and an unverifiable API deploy mid-incident also perturbs the very cron/health signals being used to watch for recovery. Ship as a Level B PR **after** the database is restored. |
| TD-29 | P2 | crons / video pipeline | **`finalize-videos` permanently abandons in-flight video assets once an outage exceeds 6 hours.** Found 2026-09-22 (13th watchdog session of the Supabase SEV-1) by asking what the outage's *duration* destroys, rather than re-deriving that the DB is down. `internal.ts:61-74` selects the sweep's work with `.in('status', ['pending','uploading','processing'])` **and `.gte('created_at', sixHoursAgo)`** — a hard floor. The outage began `2026-09-21T18:23Z` and has run 16h37m, so every asset that was mid-transcode when the database vanished is now **older than the finalizer's own lookback window**: on restore the row falls outside the query and is never picked up again. **Nothing else re-drives it** — Stream webhooks are deliberately skipped (`:54`, "we skip Stream webhooks" is the documented design) and the client poll caps at ~10 min (CLAUDE.md), long since gone. By the cron's own docstring (`:49-57`) such a post is left "stuck `pending` forever — unplayable AND never thumbnail-moderated", which is precisely the gap this cron exists to close. **This is the one part of the incident that does NOT self-heal at any restore time** — unlike the Stripe webhook backlog, which lands on idempotent handlers if restore beats `2026-09-24T18:23Z`. The other four crons were audited in the same pass and are all safe: `publish-scheduled` (`:248-263`) has no push fan-out and drains a backlog at ≤200/min; `save-nudges` dedupes on `save_nudges(user_id,recipe_id)` forever and its 7-21 day window is 14 days wide, and its `21:00Z` run failed at the first query so there is no partial state; both rollups (`:336-344`, `:353-361`) recompute from source. **Honest limits:** volume is **unknown** (counting needs the DB — it could be zero, but the outage began 11:23 AM PDT Monday, a plausible upload hour); the fragility is **pre-existing**, not outage-caused — this is simply the first event long enough to cross it; and no moderation *bypass* was verified — a stuck `pending` asset should not reach viewers, so do not escalate this to a moderation incident without checking the read path. | Two parts. **(1) One-off backfill after restore** (not time-critical — the rows persist): count with `select id, status, created_at from video_assets where provider='cloudflare' and status in ('pending','uploading','processing') and created_at < now() - interval '6 hours'`, then re-drive those ids through `finalizeProviderAsset` once. **(2) Resilience fix so duration alone cannot strand user content** — the 6h floor exists to bound the scan, so do not simply widen it forever (that changes cron cost and scan shape every minute to solve a once-per-outage problem). Better shape: keep the hot window, and add a separate low-frequency reaper for older non-terminal assets, or track an explicit `last_checked_at` so oldest-first progress is durable. **Level B** (`routes/internal.ts` is not on the autonomy-policy security-sensitive list and no auth gate is touched), but the happy path needs a live DB to verify. | open — filed 2026-09-22 by the 13th watchdog session. **Deliberately not shipped during the SEV-1**, same reasoning as TD-28: unverifiable against a database whose hostname has no DNS record (CLAUDE.md hard rule 4), and an unverifiable API deploy mid-incident perturbs the very cron/health signals being watched for recovery. The backfill step and its count query are in §4 of `docs/operations/incidents/2026-09-21-supabase-project-unreachable.md` so the restore does not silently leave orphans behind. |
| TD-30 | P3 | payments / messaging | **`sendWelcomeDm` is the only non-idempotent write in the Stripe webhook handler, so a redelivered `customer.subscription.created` sends a duplicate welcome DM and a duplicate push.** Found 2026-09-22 (17th watchdog session of the Supabase SEV-1) while verifying that the incident's own recommended recovery — replaying the queued Stripe backlog — is safe to run. `monetize.ts:98-110`: the `conversations` upsert dedupes on `user_a,user_b` (`:104`), but the message write at `:107` is a plain `insert` into `public.messages` with no dedupe, and the table has no unique constraint to backstop it (`supabase/migrations/20260625040000_direct_messages.sql:20`; the `unique (user_a, user_b)` at `:15` is on `conversations`, not `messages`). The single caller is the `customer.subscription.created` branch when the subscription is active (`:1132-1134`), and `notify(...)` at `:109` re-fires the push alongside it. Every *financial* write in the same handler is durably guarded and was re-verified in the same pass — `invoice.paid` on the unique `provider_ref` index (`:1151`), subscription rows upserting on `subscriber_id,creator_id` (`:1129`), unlocks/purchases upserting with `ignoreDuplicates` (`:1095`, `:1099`), the tips flip status-filtered (`:1092`), and the dispute re-pay gated by `hasTransferInGroup(repayGroup)` *before* the transfer (`:1083`, whose hour-bucketed idempotency key at `:1088` is deliberate and documented at `:1076-1081`) — so this is **not** a money defect and was not escalated. **Pre-existing, not outage-caused:** Stripe webhook delivery is at-least-once by design, so any redelivery has always been able to trigger it; the outage matters only because restore will flood a just-woken database with a queued backlog, which is exactly the condition that produces a partial-success-then-error invocation. Volume unknown (counting needs the DB). | Make the welcome DM idempotent per (conversation, subscription) rather than relying on the handler running once. Cheapest durable shape: a partial unique index keyed to the welcome message — e.g. carry the `stripe_subscription_id` on the inserted row and add `unique (conversation_id, stripe_subscription_id)`, then `upsert(..., { ignoreDuplicates: true })` like the sibling writes already do — which also makes the `notify` at `:109` a no-op on replay if it is moved behind the insert's result. A guard table is overkill; the sibling handlers in this same file already establish the upsert-on-conflict idiom. **Level C** — it touches `apps/api/src/routes/monetize.ts` (payments, on the security-sensitive path list) and adds DDL under `supabase/migrations/**`, so it needs owner sign-off and a migration, even though the defect itself is cosmetic. Testable with a mocked `supabaseAdmin` replaying one `customer.subscription.created` twice and asserting a single `messages` row. | open — filed 2026-09-22 by the 17th watchdog session. **Deliberately not shipped during the SEV-1**, same reasoning as TD-28/TD-29: unverifiable against a database whose hostname has no DNS record (CLAUDE.md hard rule 4), it needs a migration (Level C), and an unverifiable deploy mid-incident perturbs the signals being watched for recovery. **Not a blocker for the restore** — the mitigation is procedural and already written into §2 of `docs/operations/incidents/2026-09-21-supabase-project-unreachable.md`: replay with the `delivery_success=false` filter and let the automatic retries drain first, which avoids re-running anything already delivered. |
| TD-31 | P3 | api deps | **`hono` 4.13.0 is inside the `<= 4.13.4` range of three advisories that appear in no existing doc.** Surfaced by `npm audit` and first triaged 2026-09-22 (daily sweep) — a fresh recurrence of the TD-2 class (hono core), which closed 2026-08-07. Triaged by reading the code, not the rating: (1) [GHSA-gqvv-2mrq-wpjv](https://github.com/advisories/GHSA-gqvv-2mrq-wpjv) (`toSSG()` writes outside the output directory) is **unreachable** — `grep -rn toSSG apps/api/src` = 0 matches, there is no SSG path; (2) [GHSA-g6gw-c38x-mqfc](https://github.com/advisories/GHSA-g6gw-c38x-mqfc) (unbounded dot-notation nesting in `parseBody()` → memory exhaustion) is **unreachable** — `grep -rn parseBody apps/api/src` = 0 matches; (3) [GHSA-crvj-82cr-hjcx](https://github.com/advisories/GHSA-crvj-82cr-hjcx) (query parser reads parameters **after** the URL fragment → cache-key / proxy interpretation differential) is **the only one with a surface**: the API sets publicly cacheable headers on query-param routes behind Vercel’s CDN — `seo.ts:224,347` (`s-maxage=3600`), `feed.ts:280` (`s-maxage=30`), `hashtags.ts:37,72` (`max-age`). Shape is **public-content cache poisoning, not an auth or PII bypass** — no gated route is publicly cached, and `feed.ts:280` sets the public header only when `userId` is absent, so personalized feeds are never shared-cached. | Merge Dependabot **PR #8** (`hono` 4.13.0→4.13.5 — CI-green, non-major, clears all three) as the **first post-restore deploy**, when `/health` can actually confirm it. | open — filed 2026-09-22 by the daily sweep. **Deliberately not merged unattended**, for two reasons recorded on PR #8, the first decisive: (a) merging deploys the API, and the sweep ship rule requires `scripts/verify-deploy.mjs` afterwards, whose success criterion is a 200 `/health` — which returns 503 `database-unreachable` regardless of this bump, so the change is **unverifiable by construction** during the open SEV-1 (hard rule 4); (b) precedent — TD-2 / `autonomy-audit.md:34` classified the prior hono bump as "owner-authorized deploy (money-path dep)" because hono is mounted app-wide and carries the Stripe webhook and auth middleware, which is more specific than the sweep’s generic "patch + not payment/auth ⇒ merge" lane. |
| TD-32 | P2 | ops tooling | **The daily maintenance sweep did not run for 14 days and produced no signal of any kind about its own absence.** Verified 2026-09-22, not inferred: `gh api …/commits?since=2026-09-06&until=2026-09-21T18:00Z` returns **zero** commits, and a completed sweep appends a `LOG.md` section and pushes on **every** run by design — so no sweep completed 09-07 through 09-20 inclusive. It fired normally on 09-22 (the 08:13 PDT `com.sizzle.daily-sweep` launchd slot) and `~/.sizzle-ops/paused` is absent, so the schedule itself is intact. **Cause undetermined from inside the session.** Two candidate mechanisms, both in `scripts/ops/daily-sweep.sh`: the pause flag at `:8`, which exits 0 after writing one line to `~/Library/Logs/sizzle-sweep.log` and notifying **nobody**; and the all-attempts-failed path at `:31-34`, which does fire an `osascript` desktop notification — but that is local-only and evaporates on an unattended or sleeping Mac. The decisive evidence is `~/Library/Logs/sizzle-sweep.log`, which sits outside the session’s allowed working directory and is blocked by the sandbox. **Concrete cost already realised:** Dependabot PR #8 carried a security driver from 2026-09-10 and went untriaged for 12 days because no sweep ran to see it (TD-31). Structurally this is the §7 alerting finding of the 09-21 SEV-1 one layer up — **a maintenance job whose non-execution is silent is indistinguishable from one that ran and found nothing.** | **Owner (one grep, outside the sandbox):** search `~/Library/Logs/sizzle-sweep.log` for `paused` / `ALERT` / `sweep starting` across 09-07…09-20 — that file distinguishes the two candidates outright. **Then close the observability hole**, which needs no new infrastructure: the sweep’s output *is* a git commit, so a staleness check is a cheap one-liner (compare the newest `ops: daily sweep` commit date against today and shout when the gap exceeds ~2 days). Worth routing that through a channel that survives a sleeping Mac, unlike `osascript`. | open — filed 2026-09-22 by the first sweep to run after the gap. Level C+ (touches ops scheduling and needs evidence the sandbox cannot reach), so not shippable unattended. **2026-09-24 — the schedule has now held three consecutive days; the *observability* half is untouched.** `gh api …/commits?since=2026-09-06` shows an `ops: daily sweep` commit for **09-22, 09-23 and 09-24**, so the 14-day dark gap has not recurred and the `com.sizzle.daily-sweep` launchd slot is demonstrably firing. **That is evidence about the schedule, not about the hole:** had today's sweep silently not run, nothing in the repo or in any channel Branden reads would have said so — which is precisely the defect this entry describes, and it is unchanged. The cheap fix named in the fix column (compare the newest `ops: daily sweep` commit date against today, shout past ~2 days) remains unwritten and unowned. Note the owner grep of `~/Library/Logs/sizzle-sweep.log` for 09-07…09-20 is also still outstanding, and that evidence ages out with log rotation. |
| TD-33 | P2 | secrets / ops tooling | **The mandated pre-push secret gate scans nothing on the only push path unattended sessions can use.** Found 2026-09-22 (daily sweep) by noticing that the required `npm run secrets:check` reported `clean (0 file(s) scanned, staged)` before a real docs push. Two independent reasons it cannot see API-pushed content: (1) `scripts/secrets-check.mjs:46-47` defaults to `git diff --cached`, and the TD-27 workaround pushes through the GitHub git-data API without ever staging anything, so the file list is empty and the script exits 0 with a reassuring "clean"; (2) it reads bodies from the **working tree** (`:55 readFileSync(file)`), whereas the blobs actually uploaded are built under `.codex/staged-*/`, which is gitignored and untracked — so **even `--all` would not scan them.** The gate is therefore structurally blind to exactly the content it is supposed to guard, and its output is indistinguishable from a real pass. **Not currently an exposure:** CI runs the same scanner with `--all` (`Secret scan (all tracked files)`, verified green on `cee5b0d`), so Layer 3 still fires — but it fires **after** the push, and on a PUBLIC repo post-push detection of a live credential is the TD-20 outcome (`publicly_leaked:true`), not a save. This is a gap in Layer 2 of the defence CLAUDE.md hard rule 1 exists to provide. | Make the check content-based rather than index-based on the API path: have the push script scan the exact blob strings it is about to upload (the same `PATTERNS` array, imported rather than re-implemented) and abort non-zero on a hit. Cheapest correct shape is to export `PATTERNS`/`scanText` from `scripts/secrets-check.mjs` and call it from the pusher, so the two layers cannot drift. Optionally also accept `--stdin`/`--files` so any future out-of-tree push path is covered by construction. | open — filed 2026-09-22 by the daily sweep that hit it. **This sweep compensated manually**: the staged content was scanned out-of-band for credential-shaped strings (prefix + real-length tail, plus JWT and PRIVATE KEY blocks) and was clean, and CI's `--all` scan passed post-push — so `cee5b0d`/`5c791fc` are not at risk. The fix touches `scripts/` (Level B) but was deliberately not shipped mid-SEV-1, consistent with TD-28, TD-29 and TD-30 all staying parked: it is tooling on the live push path, and this session would have to use the very script it was editing to ship the edit. |
| TD-34 | P2 | video pipeline / incident recovery | **`finalize-videos`' abandon path has no lower `created_at` bound, so the first cron tick after an outage restore mass-flips every stranded asset to a terminal state the finalizer then refuses to re-poll — silently converting TD-29's own prescribed recovery into a no-op.** Found 2026-09-22 (18th watchdog session of the Supabase SEV-1) by auditing what happens *at the moment of restore*, a question the prior 17 sessions had not asked. TD-29 correctly identified that the rescue SELECT is floored at `created_at >= now-6h` (`apps/api/src/routes/internal.ts:61-74`) so stranded assets are never picked up. The missing half: the two abandon UPDATEs immediately after it (`.in('status',['pending','uploading']).lt('created_at', twoHoursAgo)` and `.eq('status','processing').lt('created_at', sixHoursAgo)`) carry **no `.gte` floor** and run unconditionally, outside the `if (pending.length)` guard. After any outage longer than 6h the rescue and abandon windows stop overlapping, so the entire stranded cohort falls only into the abandon window and is flipped to `status='error'` within 60s of the DB returning (`vercel.json:10-11`, every-minute schedule). Three consequences: (1) `videoFinalize.ts:213` short-circuits on `ready`/`error`, so re-driving those ids through the finalizer — TD-29's literal remedy — does nothing; (2) TD-29's counting query filters on `status in ('pending','uploading','processing')` and therefore returns **0**, reading as "nothing was stranded" and closing the item for the wrong reason; (3) Cloudflare was up throughout the outage, so a large share of that cohort very likely finished transcoding and sits `ready` on the provider — the flip corrupts our record of recoverable user content, not merely dead uploads. In normal operation the design is sound (">6h processing = genuinely stuck"); it is *outage duration* that falsifies the assumption, the same class of defect as TD-29 itself. **Reversible, not permanent loss:** the UPDATE sets `status` only, so `provider_uid` survives, and although `video_assets` has no `updated_at` (`20260619120000_init_schema.sql:29-40`) the abandon path never writes `last_polled_at` (`20260717002042_video_assets_pipeline_hardening_cols.sql:14`), leaving it frozen pre-outage as a reconstruction handle. Volume unknown — counting needs the DB. | Two parts. **Recovery (owner, now):** disable the `finalize-videos` cron in the Vercel dashboard *before* clicking Resume, capture the cohort, re-enable — written up as §4 step 0 of the incident action sheet, together with the `last_polled_at`-based reconstruction query for the case where the cron already ran and the `status='processing', last_polled_at=null` reset needed to clear the terminal short-circuit. **Resilience (code, later):** give the abandon UPDATEs a lower bound symmetric with the rescue SELECT so an asset can never be abandoned without first having been eligible for rescue, and/or make the finalizer treat an operator-initiated reset as non-terminal. **⚠️ Do not assume TD-28 covers this — verified 2026-09-23 (48th watchdog session), and the distinction decides whether the cohort survives.** TD-28's remedy (destructure and check `error` on every `/internal/*` query, return 500) is the fix a reader naturally expects to also guard these UPDATEs, since `internal.ts:67`'s `const { data: pending }` is cited by *both* entries. It does not. At the first tick after Resume the database is **healthy**, so the rescue SELECT does not error — it succeeds and returns **empty**, because a cohort stranded for >6h is outside `created_at >= now-6h`. With no error to guard on, an error-check short-circuit never fires, and the unbounded `.lt('created_at', …)` UPDATEs still match and flip the whole cohort. The two defects share a line number and nothing else: TD-28 is an **observability** bug (failures reported as 200), TD-34 is a **window-asymmetry** bug (two filters that stop overlapping). Shipping TD-28 alone leaves TD-34 fully armed; only the symmetric lower bound above closes it. Consider adding `updated_at` to `video_assets` so a mass state change is forensically visible at all. | open — filed 2026-09-22 by the 18th watchdog session. Deliberately **not** shipped mid-outage, consistent with TD-28/29/30/31/33: any API deploy is unverifiable while `/health` returns 503 (CLAUDE.md hard rule 4). The owner-facing half needs no deploy and is already live in the action sheet, which is what makes this worth filing now rather than after restore — it is the only finding in this incident with a **pre-Resume** action. |
| TD-35 | P2 | payments / IAP | **The Apple/RevenueCat refund webhook has a 155-minute total retry budget, so any outage longer than ~2h35m permanently drops refund revocations — a refunded buyer keeps premium access forever and the creator is paid out for a reversed sale.** Found 2026-09-22 (23rd watchdog session of the Supabase SEV-1) by asking the incident's recurring question — *"what else has a clock the outage has outrun?"* — of the one system §2 of the action sheet covered only for Stripe. Sessions 1–22 read "the money clock" as *the Stripe clock*; Sizzle has a **second, independent payment rail**, and it is live: `vercel env ls production` shows **`REVENUECAT_API_KEY` and `REVENUECAT_WEBHOOK_AUTH` both set on Production, 67 days old** (`/health` cannot show this — `health.ts:120` computes `payments` from `stripeConfigured` alone, and both the configured and unconfigured webhook states return 401, so neither probe distinguishes them; the env listing was the only way to settle it). RevenueCat's published policy is *"retry later (up to 5 times) with an increasing delay (5, 10, 20, 40, and 80 minutes)"* ([docs/integrations/webhooks](https://www.revenuecat.com/docs/integrations/webhooks)) = **155 min ≈ 2h35m**, versus Stripe's **3 days** of automatic retries plus a 15-day dashboard resend and a 30-day API window. The outage began `2026-09-21T18:23:07Z`, so the earliest events exhausted at **`2026-09-21T20:58Z` — ~24.5h before this session** and the boundary rolls forward continuously. **The handler is not at fault and must not be "fixed" to 200:** `monetize.ts:796-859` deliberately returns 500 on every DB failure (`:818`, `:829`, `:834`, `:849`) so RevenueCat redelivers, and its own comment at `:814-815` names the stake — *"A read FAILURE must retry, not silently 200 — otherwise the refunded unlock would survive forever."* The defect is that the **provider's retry budget is smaller than the outage**, with no durable queue behind it. Per dropped `REFUND`/`CANCELLATION`: (1) `recipe_unlocks` is never deleted → permanent entitlement to refunded content; (2) `iap_transactions.revoked_at` never stamped → no record a reversal was owed; (3) the `tips` row stays `status='succeeded'`/`provider='apple'` → **the creator is credited and paid for a purchase Apple reversed**, with none of the loss-protection the Stripe path gets from `charge.refunded`/`dispute.*` (`:908-910`); (4) `bumpGoal` never unwinds. **Nothing else repairs it** — verified: `fetchNonSubscriptions` is called only on the grant path (`:326`), nothing else in the codebase reads or writes `revoked_at`, and none of the five crons in `apps/api/vercel.json` reconciles IAP refunds, so this webhook is the sole revocation mechanism. **Scope is narrow and stated as such:** only `REFUND` and `CANCELLATION` reach the database (`:806`); every other event type short-circuits to 200 at `:858` without a query, so the exposed set is exactly *Apple refunds/chargebacks occurring during the outage* — plausibly zero. **Volume unknown** (needs the RevenueCat dashboard or the DB); mechanism and deadline verified. **The grant side was audited in the same pass and is SAFE** — `apps/web/src/data/queries.ts:161-181` is confirm-first, claiming a still-unconsumed purchase before ever charging again (`:165-169`), the outage leaves the UI in a `pending` state rather than a buy button (`:178-180`), and the purchase persists unconsumed on RevenueCat, so a buyer charged mid-outage is granted their unlock on their next tap with no double-charge; the residual is only that the heal is user-triggered, so a buyer who never returns stays charged-without-unlock until they do. | Two parts. **Recovery (owner, after restore — NOT automatic):** app.revenuecat.com → Integrations → Webhooks → filter failed/retrying events since `2026-09-21T18:23Z`, keep the `REFUND`/`CANCELLATION` rows, and press **Retry** on each (*"Find the failed (or retrying) event in the table and click Retry"*). Replay is idempotent — the `recipe_unlocks` delete is idempotent by construction (documented `:824`), the `revoked_at` stamp is a plain overwrite, the ledger reversal is filtered on `status='succeeded'` (`:845`) so it cannot double-reverse, and `bumpGoal` only runs for rows that filter matched — so retrying an already-delivered event is a no-op and "when in doubt, retry" is correct. If dashboard retention no longer reaches back that far (not documented publicly), reconcile by hand against Apple's reported refunds, append-only and auditable per financial rule #5. Written up as **§4 step 6** of the incident action sheet, with §2 "The Apple clock" carrying the mechanism. Do it **before the next payout run** — consequence (3) is the one that gets harder to unwind once money has moved. **Resilience (code, later):** persist inbound RevenueCat events to a durable store on receipt and drain them with a reconciling job, so revocation survives an outage longer than the provider's retry budget; optionally a periodic sweep comparing `iap_transactions` against RevenueCat as a backstop. **Level C** — `apps/api/src/routes/monetize.ts` is on the autonomy-policy security-sensitive list (payments) and a durable queue likely adds DDL under `supabase/migrations/**`. | open — filed 2026-09-22 by the 23rd watchdog session. **Deliberately not shipped during the SEV-1**, consistent with TD-28/29/30/31/33/34: unverifiable against a database with no DNS record (CLAUDE.md hard rule 4), it is Level C on the payments path, and an unverifiable API deploy mid-incident perturbs the signals being watched for recovery. **The owner-facing half needs no deploy and is already live in the action sheet** — which is what makes it worth filing now rather than after restore, since unlike every other item here its recovery window is not waiting for the database to come back. |
| TD-36 | P2 | payments / observability | **The two money webhook handlers are the only 5xx paths in the API that never reach Sentry.** Found 2026-09-23 (36th watchdog session of the Supabase SEV-1) while auditing whether `/health`'s `sentryConfigured: true` meant the outage's dropped webhook deliveries were recoverable from Sentry after restore. They are not. Sentry capture is driven **only** by `app.onError` (`app.ts:64` → `lib/errors.ts:25-42`, which calls `captureException` for any thrown `AppError` with status >= 500 and for any unhandled throw), and Hono's `onError` fires on a **thrown** error only. Both money webhooks **catch and `return`** instead of throwing, so the hook is never reached: Stripe at `monetize.ts:1193-1196` (`catch (err) { console.error('[monetize] webhook error:', …); return c.json({code:'db_error'}, 500) }`) and RevenueCat at `:816-818`, `:828-829`, `:833-834`, `:848-849` (four `return c.json({code:'retry'}, 500)` branches, each preceded by a bare `console.error`). No response-level middleware inspects status codes (`app.ts:31-65` — `logger`, `securityHeaders`, CORS, `bodyLimit`, `rateLimit`, then `onError`/`notFound`). The intent plainly existed and stopped one branch short: `recoverFromCreator` calls `captureException` explicitly at `:780`, under a comment naming that branch "the one path that loses money silently" (`:773-774`). **This is TD-5's defect class, still live in the highest-value file in the repo** — TD-5 closed exactly this ("internal.ts failure paths console.error only despite Sentry being live") for the cron branches on 2026-08-07, but its scope was `internal.ts`; `monetize.ts` was never in it. **Cost, measured not theorised:** the sole record of a dropped delivery is the `console.error` line in Vercel runtime logs, and that buffer held **~21 minutes** during this incident (60 rows spanning `2026-09-23T09:25:17Z`–`09:45:55Z`; 48 of the 60 were the two every-minute crons at ~120 rows/hour). So each dropped money event went locally unrecoverable ~21 min after it occurred. This is the mechanism behind the action sheet's "**exposure size is unknown**" limit, and it means the Stripe `delivery_success=false` sweep and the RevenueCat dashboard Retry are the **only** sources of truth, not a backup for a local record that does not exist. | ~2 lines per branch: `await captureException(err, { issue, eventType, eventId })` alongside each existing `console.error`, keeping the `return c.json(…, 500)` exactly as-is (the 500 is correct and load-bearing — it is what makes the provider redeliver). Consider also a tiny response-level middleware that captures any outgoing 5xx, which would close this class permanently instead of per-branch. Add a unit test asserting `captureException` is called on a webhook DB failure — mockable with no live DB. | open — filed 2026-09-23. **Level C, not Level B:** `apps/api/src/routes/monetize.ts` is on the autonomy-policy security-sensitive path list, and CLAUDE.md rule 4 treats this file as financial-record code. Deliberately **not** shipped mid-outage for the same two reasons as TD-28/29/30/31/33/34: it cannot be verified on a real surface while the database has no DNS record (hard rule 4), and an unverifiable API deploy perturbs the `/health` and cron signals being watched for recovery. Ship as a reviewed Level C PR after restore. Note the irony worth keeping: this gap costs the most during exactly the kind of long outage that makes it unshippable. |
| TD-37 | P3 | ops tooling |  **`verify-deploy.mjs` blamed the wrong root cause when local HEAD was stale, after burning its whole poll budget.** Found 2026-09-23 (session 45 of the Supabase SEV-1), from a defect session 44 measured but did not file. The script defaults its target SHA to local `HEAD` (`scripts/verify-deploy.mjs:142`), but `git fetch` is not allowlisted unattended (TD-27), so on the GitHub git-data-API push path local HEAD stays frozen at an old commit while a newer one actually ships. Polling then waits the **full 8-minute deadline per project** for a deployment that can never appear — the commit is old enough that its build, if it ever had one, has scrolled out of the `limit=8` deployments page — and the run ends on `deployment: NOT FOUND after timeout — the git webhook likely missed the push`. That verdict is **false and confidently stated**: session 44 measured a brand-new prod deployment reaching **READY in 20s** on the same project in the same window, so the webhook was healthy. A verification tool that names the wrong cause during an open SEV-1 is worse than one that fails loudly. Cost so far: one session consumed a 180s budget for no result, and every future unattended session repeats it unless it remembers `--sha`. |  Fail fast instead of timing out, and only in the case that is provably unpollable: the deployments page is sorted newest-first and only ever moves newer, so a commit older than **everything on the page** has no deployment there and never will. Bail **only when the SHA was defaulted from HEAD** — an explicit `--sha` may legitimately name an old commit someone is redeploying, and that case must keep polling. Also enumerate the SHAs that *are* deployed in the not-found message, so "stale HEAD" and "dead webhook" separate at a glance. |  ✅ closed 2026-09-23 — shipped in-lane (Level A: ops-only tooling, invoked from `scripts/ops/*-prompt.md`, not CI, not a package script, no production effect). `staleHeadBail()` + `commitTimeIso()` added as **pure exported** functions; the poll loop consults the former each tick and exits with `deployment: NOT POLLABLE — …` naming the stale-HEAD cause, TD-27, and the `--sha` remedy. The timeout branch now prints the recent deployment SHAs and only offers the webhook hypothesis *after* the stale-HEAD one. **Verified on the real surface, both directions:** `node scripts/verify-deploy.mjs --api` (session 44's exact invocation) now bails in seconds — *"local HEAD d4c5395 (2026-09-02T08:19:40-07:00) predates every deployment on the page (oldest 2026-09-23T11:59:09.241Z) … Re-run with `--sha`"* — where it previously hung for 8 minutes; and `--api --sha 9664bdf0c6c58fc284ce4a332685e3d1eb831451` still prints `deployment: READY` → probe 503 → `deployed but unhealthy`, i.e. the normal path is byte-for-byte unchanged. The TD-24 token refresh also fired and recovered during the first run, so that fix is still live. Regression-guarded by **6 new tests** in `tests/invariants/ops-tooling.test.mjs` (bail on aged-out HEAD · `--sha` keeps polling · newer-than-oldest keeps polling · SHA already on page never bails · no-evidence cases never bail · `commitTimeIso` returns null rather than throwing for a commit absent locally). **Mutation-verified, not tautological:** deleting the `shaFromHead` guard from the implementation fails test 15 (44 → 43 pass); restoring it returns 44/44. |
| TD-38 | P3 | web / incident UX | **A server-side outage is reported to users as "check your connection".** Found 2026-09-23 (session 50 of the Supabase SEV-1), auditing the one surface 49 prior sessions had not: what the live app shows while the database is unreachable. The degradation itself is *correct* — boot cannot stall (`apps/web/src/lib/bootProgress.ts:38` arms an 8s failsafe; `apps/web/src/components/Feed.tsx:56-58` calls `markBootReady()` on the error path as well as the success path), the feed renders `FeedError` with a live retry (`Feed.tsx:89-90`), and `useAuth.ts:185-186` surfaces the error and clears `busy` instead of hanging. The defect is attribution: `Feed.tsx:164-165` reads *"Can't load the feed / Check your connection and try again"*, which blames the user's network for a 53-hour server outage, and the sign-in screen renders `error.message` raw, so a DNS/connect failure surfaces the bare auth-js retryable-fetch string. Cost is churn and support/review noise, not correctness. | The app already has the signal it needs: `useOnlineStatus.ts:4` wraps `navigator.onLine`. Branch the copy — offline ⇒ keep "check your connection"; online-but-API-failing ⇒ say the service is having trouble and that it is not the user. Same for the auth screen: map `AuthRetryableFetchError` to branded copy rather than printing `error.message`. Cosmetic, no auth/RLS/money path, OTA-safe — but verify in a real browser (and the simulator) before shipping, which no unattended session can do. | open — filed 2026-09-23 by session 50; **deliberately not shipped during the incident** (restores nothing, unverifiable unattended). Do it after restore, alongside TD-29/TD-34. |
