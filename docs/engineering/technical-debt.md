# Sizzle — technical debt register

Durable tracker (TODO comments don't count). Add an entry when work uncovers real debt;
close entries by linking the fixing commit/PR. Severity: P1 fix soon · P2 scheduled ·
P3 opportunistic. Sourced initially from the 2026-08-06 autonomy audit
(`autonomy-audit.md` holds the full context + evidence for each).

| ID | Sev | Subsystem | Problem | Fix sketch | Status |
|----|-----|-----------|---------|------------|--------|
| TD-1 | P1 | web UI | Conditional React hooks in 3 shipped components — RecipeSheet.tsx:75-91 (7 hooks after early return), CommentsSheet.tsx:41, CookSheet.tsx:357. Latent "rendered more hooks" crash class; the 9 remaining `npm run lint` errors. | Hoist hooks above early returns; verify all three sheets on the iOS Simulator; then add `lint` to ci.yml. | open |
| TD-2 | P1 | api deps | hono 4.12.26 in advisory range — ReDoS in the CORS middleware mounted app-wide (GHSA-8j4g-w8fx-2239). In-range fix 4.13.0. | `npm update hono` → typecheck → smoke CORS from getsizzle.app + native origins → owner-authorized deploy. | ✅ closed 2026-08-07 — hono 4.13.0 shipped (commit 7843842); lockfile = 4.13.0 (> 4.12.33 advisory range), `npm audit` no longer flags hono core |
| TD-3 | P1 | api security | `POST /hashtags/:tag/moderate` (hashtags.ts:224) is an admin action gated only by requireAuth+requireAdmin — bypasses the requireAdminUnlock second factor mounted on the /admin router. Contradicts SYSTEM_RISK_MAP:177-180. | Move under /admin or add requireAdminUnlock inline; confirm the dashboard client sends the unlock header on this call. | ✅ closed 2026-08-07 — requireAdminUnlock now inline on POST /hashtags/:tag/moderate (commit 7843842); verified hashtags.ts:230 (route + comment cite the 2026-08-06 audit) |
| TD-4 | P1 | crons | 4 of 5 crons have zero failure signal (only finalize-videos has the indirect stuckVideoBacklog gauge). | `cron_runs` heartbeat table + per-cron last-success age on /health + alert threshold. | ✅ closed 2026-08-07 — cron_runs heartbeat + /health cronAges + cron-stale problems live (commit 7843842); verified all 5 jobs upserting last_success_at in prod (health.ts:33-60, internal.ts recordCronRun) |
| TD-5 | P1 | observability | /health always 200 "ok" (DB-probe failure swallowed → null); Sentry events carry no release/OTA version; internal.ts failure paths console.error only despite Sentry being live. | Honest degraded/503; `release` field in both envelopes; captureException on the 4 cron failure branches. | 🟡 mostly done 2026-08-07 — honest 503/degraded ✓ (health.ts:72,117), API Sentry `release` ✓ (sentry.ts:44), cron captureException ✓ (internal.ts). REMAINING: web Sentry envelope still carries no `release` tag (apps/web/src/lib/sentry.ts) |
| TD-6 | P1 | money tests | Stripe/RevenueCat webhook + entitlement flows have none of the SYSTEM_RISK_MAP "required tests" (duplicate delivery, refund-after-earnings, chargeback-after-payout, account-switch entitlement). | In-process Hono + local Supabase harness with signed canned fixtures; commit it (stop discarding per-change harnesses). | open |
| TD-7 | P1 | db security | The 2026-07-29 RLS/grant-matrix verification is manual prose; grant drift already caused one real bypass. | Commit a runnable grant-matrix assertion (SQL, read-only) + wire into CHANGE_SAFETY_CHECKLIST after any grant-touching migration. | open |
| TD-8 | P2 | deploys | No verify-deploy tooling; API exposes no commit SHA; deploy.md still documents Railway/Fly. | Stamp VERCEL_GIT_COMMIT_SHA into /health + web version.json; scripts/verify-deploy.mjs (poll READY + probe + SHA compare); rewrite deploy.md. | 🟡 partial 2026-08-07 — scripts/verify-deploy.mjs ✓ + /health `commit` SHA ✓ (health.ts:81). REMAINING: web version.json not emitted, deploy.md still cites Railway/Fly |
| TD-9 | P2 | ota | OTA upload is a raw manual CLI command; nothing can fail a native-coupled bundle; no min-update-version anywhere. | `ota:release` wrapper: safety:diff vs last-binary tag hard-fails on NATIVE, then build + upload with --min-update-version from a committed compat table. | open |
| TD-10 | P2 | api types | Supabase client untyped (no Database generic, no generated types) — column renames compile clean and fail at runtime. | `supabase gen types` → thread generic through supabaseAdmin; regenerate on migration PRs. | open |
| TD-11 | P2 | api abuse | No per-route limits on GET /me/export (8 unbounded scans), POST /uploads/video/:id/poster (paid moderation call), follow/block mutations. | Targeted rateLimit buckets with the existing middleware. | open |
| TD-12 | P2 | uploads | Own-folder URL check is plain startsWith — `..`/percent-encoded traversal can address another user's object within the project host. | Parse with new URL(), reject %/?/#/.., require normalized pathname to keep the prefix. | open |
| TD-13 | P2 | docs | PROGRESS.md 6 builds behind; README describes pre-launch prototype; security.md/HOSTING.md/launch-blockers.md/premium-launch-plan.md/cloudflare-stream-setup.md/audit-2026-07-01.md carry resolved "MUST fix" claims with no closure banners; app-store-deployment.md wrongly says the Firebase plist is committed; SYSTEM_RISK_MAP "96"→95 migrations; ARCHITECTURE "18"→19 route groups. | One docs catch-up pass with dated supersession banners. | open |
| TD-14 | P2 | deps hygiene | @capacitor/cli in `dependencies` drags tar (critical advisory) into prod-scope audits; `npm audit fix` clears the whole transitive tier (tar, brace-expansion, postcss, shell-quote, protobufjs). | Move to devDependencies + audit fix (NOT --force) + verify cap sync + builds. | open |
| TD-15 | P2 | native | Contradictory claims about isPluginAvailable() for runtime-registered plugins (instagramStory.ts:24-27 vs NativeCameraRecorder.tsx:21) — one is wrong; segment recording gating may be affected. | One simulator log check; correct whichever comment/gate is wrong. | open |
| TD-16 | P3 | media | video_assets has no ready_at/error_at — transcode latency unmeasurable; abandon windows were chosen blind. | Additive migration + set in finalizeProviderAsset. | open |
| TD-17 | P3 | repo | `.github/modernize/java-upgrade/` — unowned Java-tool residue in a TS monorepo. | Confirm origin with Branden (rule 16), delete. | open |
| TD-18 | P3 | majors | Deliberate one-at-a-time major queue: Vite 5→8 (first — unblocks esbuild advisories), zustand 5, zod 4, React 19, TS 7, @hono/node-server 2. | Each is its own researched project per DEPENDENCY policy in renovate.json. | open — Dependabot **security** PR #6 (esbuild 0.24.2→0.28.2 + grouped vite bump) is open & CI-green but held for owner: it's the first (Vite) item of this queue and needs a local build + iOS-Simulator smoke; not an unattended merge (2026-08-09 sweep, risk summary on PR #6) |
| TD-20 | P1 | secrets | **Leaked Supabase PAT in public git history.** GitHub secret-scanning alert #1 (open, `publicly_leaked:true`) — a `sbp_…` Supabase Personal Access Token committed on 2026-07-15 in `.codex/config.toml` at commit `16355f66` ("Stop the app making claims that aren't true"), which is on `origin/main`. Recurrence of the CLAUDE.md hard-rule-1 PAT-leak class. **Contained going forward:** `.codex/` is now gitignored (`.gitignore:58`) so it can't re-leak, the file is untracked at HEAD, and the leaked value is absent from every current working-tree `.toml/.json/.env` (local config already replaced). **Not verified:** whether the leaked token is actually revoked on Supabase's side — the alert being open means it may still be live. | **Owner (Level D):** (1) revoke that PAT in Supabase → Account → Access Tokens if not already; (2) resolve/dismiss GitHub secret-scanning alert #1 as "revoked". History rewrite is optional and unnecessary once revoked (and would be a destructive force-push — owner decision only). | ✅ closed 2026-08-10 — owner revoked the PAT; GitHub secret-scanning alert #1 resolved as `revoked` (resolved_at 2026-08-10T14:52:32Z), open-alerts list verified empty via the API (2026-08-10 sweep). Containment from 08-09 still holds (`.codex/` gitignored, leaked value absent from working tree). History rewrite deliberately not done — unnecessary once revoked, and a destructive force-push on shared `main`. |
| TD-19 | P3 | api deps | `@hono/node-server` 1.19.17 (was 1.19.14; patched up in the 2026-08-08 Dependabot triage) — serve-static path traversal on Windows via encoded backslash (`npm audit` MODERATE; surfaced 2026-08-07 sweep, now the top prod-scope-adjacent item after hono core cleared). **Non-exploitable on two independent counts, re-verified 2026-08-10 sweep:** (1) the vulnerable code is the `serve-static` middleware, which is **never imported anywhere** in the API (`grep -rn serveStatic apps/api` → 0 matches); only `getRequestListener` — a different, unaffected export — is used, and it IS the prod Vercel adapter (`api/index.ts:28`). (2) production runs on Vercel **Linux**; the advisory is **Windows-only** (`%5C`→`\`). *(Corrects the earlier "not in the prod path / uses hono/vercel adapter" note — there is no `hono/vercel` usage; node-server's `getRequestListener` is the prod request adapter. Conclusion unchanged: non-exploitable.)* | Bump when a 1.x fix publishes, or fold into the `@hono/node-server` 2 major already queued in TD-18. No prod action needed. | open — Dependabot **security** PR #3 proposes the 2.0.10 **major**; CI-green but held for owner as a TD-18 queue item (re-confirmed 2026-08-10 sweep) |
