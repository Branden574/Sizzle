# Sizzle — technical debt register

Durable tracker (TODO comments don't count). Add an entry when work uncovers real debt;
close entries by linking the fixing commit/PR. Severity: P1 fix soon · P2 scheduled ·
P3 opportunistic. Sourced initially from the 2026-08-06 autonomy audit
(`autonomy-audit.md` holds the full context + evidence for each).

| ID | Sev | Subsystem | Problem | Fix sketch | Status |
|----|-----|-----------|---------|------------|--------|
| TD-1 | P1 | web UI | Conditional React hooks in 3 shipped components — RecipeSheet.tsx:75-91 (7 hooks after early return), CommentsSheet.tsx:41, CookSheet.tsx:357. Latent "rendered more hooks" crash class; the 9 remaining `npm run lint` errors. | Hoist hooks above early returns; verify all three sheets on the iOS Simulator; then add `lint` to ci.yml. | open |
| TD-2 | P1 | api deps | hono 4.12.26 in advisory range — ReDoS in the CORS middleware mounted app-wide (GHSA-8j4g-w8fx-2239). In-range fix 4.13.0. | `npm update hono` → typecheck → smoke CORS from getsizzle.app + native origins → owner-authorized deploy. | ✅ closed 2026-08-07 — hono 4.13.0 shipped (commit 7843842); lockfile = 4.13.0 (> 4.12.33 advisory range), `npm audit` no longer flags hono core |
| TD-3 | P1 | api security | `POST /hashtags/:tag/moderate` (hashtags.ts:224) is an admin action gated only by requireAuth+requireAdmin — bypasses the requireAdminUnlock second factor mounted on the /admin router. Contradicts SYSTEM_RISK_MAP:177-180. | Move under /admin or add requireAdminUnlock inline; confirm the dashboard client sends the unlock header on this call. | ✅ closed 2026-08-07 — requireAdminUnlock now inline on POST /hashtags/:tag/moderate (commit 7843842); verified hashtags.ts:230 (route + comment cite the 2026-08-06 audit) |
| TD-4 | P1 | crons | 4 of 5 crons have zero failure signal (only finalize-videos has the indirect stuckVideoBacklog gauge). | `cron_runs` heartbeat table + per-cron last-success age on /health + alert threshold. | ✅ closed 2026-08-07 — cron_runs heartbeat + /health cronAges + cron-stale problems live (commit 7843842); verified all 5 jobs upserting last_success_at in prod (health.ts:33-60, internal.ts recordCronRun) |
| TD-5 | P1 | observability | /health always 200 "ok" (DB-probe failure swallowed → null); Sentry events carry no release/OTA version; internal.ts failure paths console.error only despite Sentry being live. | Honest degraded/503; `release` field in both envelopes; captureException on the 4 cron failure branches. | 🟡 mostly done 2026-08-07 — honest 503/degraded ✓ (health.ts:72,117), API Sentry `release` ✓ (sentry.ts:44), cron captureException ✓ (internal.ts). REMAINING: web Sentry envelope still carries no `release` tag (apps/web/src/lib/sentry.ts) |
| TD-6 | P1 | money tests | Stripe/RevenueCat webhook + entitlement flows have none of the SYSTEM_RISK_MAP "required tests" (duplicate delivery, refund-after-earnings, chargeback-after-payout, account-switch entitlement). | In-process Hono + local Supabase harness with signed canned fixtures; commit it (stop discarding per-change harnesses). | open |
| TD-7 | P1 | db security | The 2026-07-29 RLS/grant-matrix verification is manual prose; grant drift already caused one real bypass. | Commit a runnable grant-matrix assertion (SQL, read-only) + wire into CHANGE_SAFETY_CHECKLIST after any grant-touching migration. | open — **partial cousin shipped 2026-08-17 (security sweep):** `scripts/ops/anon-boundary-probe.mjs` is a credential-free, read-only, black-box probe of the *anon* side of the boundary (22 sensitive tables must yield 0 rows, 12 `profiles` PII columns must be column-denied, `creator_earnings` must not execute, with a positive control). It is NOT the SQL grant-matrix assertion this entry asks for — it cannot see the `authenticated` role (where SYSTEM_RISK_MAP open findings #1/#4 live) or distinguish "RLS holding" from "table empty" — so TD-7 stays open. |
| TD-8 | P2 | deploys | No verify-deploy tooling; API exposes no commit SHA; deploy.md still documents Railway/Fly. | Stamp VERCEL_GIT_COMMIT_SHA into /health + web version.json; scripts/verify-deploy.mjs (poll READY + probe + SHA compare); rewrite deploy.md. | 🟡 partial 2026-08-07 — scripts/verify-deploy.mjs ✓ + /health `commit` SHA ✓ (health.ts:81). REMAINING: web version.json not emitted, deploy.md still cites Railway/Fly |
| TD-9 | P2 | ota | OTA upload is a raw manual CLI command; nothing can fail a native-coupled bundle; no min-update-version anywhere. | `ota:release` wrapper: safety:diff vs last-binary tag hard-fails on NATIVE, then build + upload with --min-update-version from a committed compat table. | open |
| TD-10 | P2 | api types | Supabase client untyped (no Database generic, no generated types) — column renames compile clean and fail at runtime. | `supabase gen types` → thread generic through supabaseAdmin; regenerate on migration PRs. | open |
| TD-11 | P2 | api abuse | No per-route limits on GET /me/export (8 unbounded scans), POST /uploads/video/:id/poster (paid moderation call), follow/block mutations. | Targeted rateLimit buckets with the existing middleware. | open |
| TD-12 | P2 | uploads | Own-folder URL check is plain startsWith — `..`/percent-encoded traversal can address another user's object within the project host. | Parse with new URL(), reject %/?/#/.., require normalized pathname to keep the prefix. | open |
| TD-13 | P2 | docs | PROGRESS.md 6 builds behind; README describes pre-launch prototype; security.md/HOSTING.md/launch-blockers.md/premium-launch-plan.md/cloudflare-stream-setup.md/audit-2026-07-01.md carry resolved "MUST fix" claims with no closure banners; app-store-deployment.md wrongly says the Firebase plist is committed; SYSTEM_RISK_MAP "96"→95 migrations; ARCHITECTURE "18"→19 route groups. | One docs catch-up pass with dated supersession banners. | open |
| TD-14 | P3 | deps hygiene | @capacitor/cli in `dependencies` (`apps/web/package.json:33`) rather than devDependencies — a build-time CLI declared as a runtime dep. **Severity lowered P2→P3 on 2026-08-16: the security driver is gone.** The entry was opened because the placement dragged tar (critical advisory) into prod-scope audits; as of this sweep `npm audit --omit=dev` reports **`found 0 vulnerabilities`**, so no advisory currently reaches prod scope through it. What remains is hygiene — the misplacement is still real and would re-expose the same class the next time a transitive advisory lands. | Move to devDependencies + `npm audit fix` (NOT --force) + verify `cap sync` and both builds. | open |
| TD-15 | P2 | native | Contradictory claims about isPluginAvailable() for runtime-registered plugins (instagramStory.ts:24-27 vs NativeCameraRecorder.tsx:21) — one is wrong; segment recording gating may be affected. | One simulator log check; correct whichever comment/gate is wrong. | open |
| TD-16 | P3 | media | video_assets has no ready_at/error_at — transcode latency unmeasurable; abandon windows were chosen blind. | Additive migration + set in finalizeProviderAsset. | open |
| TD-17 | P3 | repo | `.github/modernize/java-upgrade/` — unowned Java-tool residue in a TS monorepo. | Confirm origin with Branden (rule 16), delete. | open |
| TD-18 | P3 | majors | Deliberate one-at-a-time major queue: Vite 5→8 (first — unblocks esbuild advisories), zustand 5, zod 4, React 19, TS 7, @hono/node-server 2. | Each is its own researched project per DEPENDENCY policy in renovate.json. | open — Dependabot **security** PR #6 (esbuild 0.24.2→0.28.2 + grouped vite bump) is open & CI-green but held for owner: it's the first (Vite) item of this queue and needs a local build + iOS-Simulator smoke; not an unattended merge (2026-08-09 sweep, risk summary on PR #6). **PR #3** (`@hono/node-server` 1.19.17→2.0.10) is the queue's last item and, as of 2026-08-13, **carries no security driver at all** — TD-19 closed when GitHub narrowed the advisory range off 1.19.17, so #3 is now a purely optional major with no urgency. |
| TD-20 | P1 | secrets | **Leaked Supabase PAT in public git history.** GitHub secret-scanning alert #1 (open, `publicly_leaked:true`) — a `sbp_…` Supabase Personal Access Token committed on 2026-07-15 in `.codex/config.toml` at commit `16355f66` ("Stop the app making claims that aren't true"), which is on `origin/main`. Recurrence of the CLAUDE.md hard-rule-1 PAT-leak class. **Contained going forward:** `.codex/` is now gitignored (`.gitignore:58`) so it can't re-leak, the file is untracked at HEAD, and the leaked value is absent from every current working-tree `.toml/.json/.env` (local config already replaced). **Not verified:** whether the leaked token is actually revoked on Supabase's side — the alert being open means it may still be live. | **Owner (Level D):** (1) revoke that PAT in Supabase → Account → Access Tokens if not already; (2) resolve/dismiss GitHub secret-scanning alert #1 as "revoked". History rewrite is optional and unnecessary once revoked (and would be a destructive force-push — owner decision only). | ✅ closed 2026-08-10 — owner revoked the PAT; GitHub secret-scanning alert #1 resolved as `revoked` (resolved_at 2026-08-10T14:52:32Z), open-alerts list verified empty via the API (2026-08-10 sweep). Containment from 08-09 still holds (`.codex/` gitignored, leaked value absent from working tree). History rewrite deliberately not done — unnecessary once revoked, and a destructive force-push on shared `main`. |
| TD-19 | P3 | api deps | `@hono/node-server` 1.19.17 (was 1.19.14; patched up in the 2026-08-08 Dependabot triage) — serve-static path traversal on Windows via encoded backslash (`npm audit` MODERATE; surfaced 2026-08-07 sweep, now the top prod-scope-adjacent item after hono core cleared). **Non-exploitable on two independent counts, re-verified 2026-08-10 sweep:** (1) the vulnerable code is the `serve-static` middleware, which is **never imported anywhere** in the API (`grep -rn serveStatic apps/api` → 0 matches); only `getRequestListener` — a different, unaffected export — is used, and it IS the prod Vercel adapter (`api/index.ts:28`). (2) production runs on Vercel **Linux**; the advisory is **Windows-only** (`%5C`→`\`). *(Corrects the earlier "not in the prod path / uses hono/vercel adapter" note — there is no `hono/vercel` usage; node-server's `getRequestListener` is the prod request adapter. Conclusion unchanged: non-exploitable.)* | Bump when a 1.x fix publishes, or fold into the `@hono/node-server` 2 major already queued in TD-18. No prod action needed. | ✅ closed 2026-08-13 — **the advisory no longer covers the installed version.** GitHub narrowed GHSA-frvp-7c67-39w9's range on `updated_at 2026-08-12T18:26:51Z` (after the 08-12 sweep ran) to `< 1.19.15` **and** `>= 2.0.0, < 2.0.5`; installed/lockfile version is **1.19.17**, outside both. Verified: `npm audit` now reports 2 advisories (was 3) with **zero** hono mentions; the `@hono/node-server` Dependabot alerts (#1/#16) are gone from `dependabot/alerts` at **every** state, not merely dismissed. The other published node-server advisory, GHSA-9mqv-5hh9-4cgg (WebSocket handshake DoS), is `>= 2.0.0, <= 2.0.9` — 1.x never affected. So 1.19.17 is clean on both published advisories on version grounds alone; the prior two-fold non-exploitability argument (re-verified this sweep: `serveStatic` 0 hits, `upgradeWebSocket` 0 hits, `getRequestListener` is the prod adapter at `api/index.ts:28`, prod is Vercel Linux) is now belt-and-braces rather than the load-bearing reason. **PR #3 is therefore no longer a security PR** — it is a plain optional major, tracked only by TD-18. |
| TD-21 | P2 | ops tooling | **The daily sweep's DB-state check has a single point of failure and no fallback.** On 2026-08-16 the Supabase MCP returned `Unauthorized. Please provide a valid access token … via the --access-token flag or SUPABASE_ACCESS_TOKEN` on every call (`execute_sql`, `get_advisors`), so sweep items 5 (parked `pending_media_deletions`, `video_assets` stuck >6h, `cron_runs` repeated failures) and 5b (Supabase security advisors) could not run. Sweeps 08-11…08-14 used it successfully, so this is new. Root cause **not determined**: the MCP reads its credential from the local-only `.mcp.json`, and reading that file is (correctly) permission-blocked for the unattended agent, so the token cannot be inspected from inside a sweep. No repo script offers an alternative read-only path — `scripts/` holds only `gen-apple-secret`, `safety-diff`, `secrets-check`, `verify-deploy` and `ops/*.sh`. Partial cover exists but is **not** equivalent: `/health` independently proved `stuckVideoBacklog` 0 (same table, 15-min threshold ⇒ subsumes the >6h question) and all five `cronAges` fresh with `problems:[]` (⇒ no cron failing longer than its stale threshold). **Genuinely unchecked this sweep: `pending_media_deletions` attempts≥10, and the Supabase advisor baseline.** | Three options, cheapest first. (a) **Owner/Level D:** re-authorize the Supabase MCP (verify/rotate the PAT in `.mcp.json`) — restores items 5/5b exactly as before. (c) **Owner, one-line config, no new credential:** the **claude.ai Supabase connector** (`mcp__claude_ai_Supabase__*`) is already connected in the sweep session — 2026-08-17 confirmed it is *not* in the auth-required list, and `list_projects` failed only with "requested permissions … not granted" — but `.claude/settings.json` allowlists just the `.mcp.json`-backed `mcp__supabase__{execute_sql,list_tables,get_logs,get_advisors}`. Adding the connector's `list_projects`/`execute_sql`/`get_advisors` to that allowlist gives sweeps an **independent second path** (authenticated via Branden's claude.ai account, not the PAT), which is the real SPOF fix. The unattended agent deliberately did **not** self-grant this — widening its own permission set is an owner call. (b) **Level B, service-role fallback:** `scripts/ops/db-state.mjs` running the three sweep queries off the API's service-role env — still valid, but note the agent cannot verify it against a real DB unattended (`Read(./apps/api/.env*)` is denied by design), so it needs one attended run before it counts as a working fallback, and it puts a service-role key in the sweep's hands where (c) does not. | open — raised 2026-08-16; **still Unauthorized 2026-08-17** (`execute_sql` same error), items 5/5b unchecked a second day; option (c) identified 2026-08-17. **Weekly security sweep 2026-08-17 (same day, later):** both paths still blocked (`get_advisors` Unauthorized; connector `list_projects` still "permissions not granted"), so the advisor baseline and the `pending_media_deletions` check remain uncovered. Partial mitigation shipped: `scripts/ops/anon-boundary-probe.mjs` (see TD-7) covers the *anon* half of the weekly RLS spot-check with no credential at all and passed clean against prod. It does not replace advisors or the parked-deletions query. **Daily sweep 2026-08-18: day 4, both paths still blocked** (`.mcp.json` PAT `execute_sql` → same Unauthorized; connector `list_projects` → still "permissions not granted"); anon-boundary probe re-run clean as the partial substitute. |
| TD-22 | P3 | api auth | **`CRON_SECRET` bearer check is not constant-time.** `apps/api/src/routes/internal.ts:42` gates all five crons with a plain `!==` string compare of the `authorization` header against `Bearer <CRON_SECRET>`, whereas the three webhooks (`monetize.ts` Stripe + RevenueCat, `uploads.ts` Cloudflare) all use `timingSafeEqual` with a length pre-check. Surfaced by the 2026-08-17 security sweep's route-auth pass. **Practically non-exploitable** — recovering a long random secret one byte at a time needs sub-microsecond timing resolution that Vercel serverless + network jitter does not offer — so this is consistency/hardening, not a live hole. | Mirror the RevenueCat pattern: length check + `timingSafeEqual(Buffer.from(auth), Buffer.from(expected))`; keep the `!expected` fail-closed branch. **Level C** (auth gate on the cron path — a mistake here silently 401s every cron, incl. finalize-videos): ship attended, verify with one authorized cron call + `/health cronAges` advancing afterwards. Not shipped unattended for exactly that blast radius. | open |
| TD-23 | P3 | ops tooling | **The local watchdog summons on host-side network blips.** `scripts/ops/watchdog.sh` treats a single `curl` returning `000` (connection/DNS-layer failure) as an incident and summons a full unattended session. Twice now (2026-08-10 07:47 PDT and 2026-08-17 15:53 PDT — 2 of the 3 real summons to date) the trigger was a momentary DNS-resolver failure on the Mac (`curl: (6) Could not resolve host`), while production was up the whole time — on 08-17 the off-Mac GitHub `Uptime` run logged HTTP 200 for both surfaces 20 s before the local probe failed. Each false summon burns a paid session and the 60-min cooldown. The true-outage alert path is already covered independently by `.github/workflows/uptime.yml` (3 probes 45 s apart, GitHub-side), so a short local confirm delay costs nothing in coverage. | In `watchdog.sh`: when a probe returns `000` (not a 5xx — a 5xx is a real origin answer), `sleep 20` and re-probe once before appending to `PROBLEMS`; optionally also require the latest `Uptime` run to be non-success before summoning on `000`. Test with `SIZZLE_WATCHDOG_TEST_URL=https://does-not-exist.invalid/health` + `SIZZLE_WATCHDOG_TEST_PROMPT` and confirm one retry then no summon-on-DNS-blip vs. still-summons on a real 5xx. **Attended (Level C by blast radius, not by path list):** launchd executes the repo file directly, so an edit is live at the next 5-min tick with no CI/deploy gate, and a bug would suppress *real* summons. | open — filed 2026-08-17 by the 15:53 false-alarm session |
