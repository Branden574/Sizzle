# Incident & Sweep Log

Appended by summoned sessions (see docs/operations/automation.md).

## Daily sweep 2026-08-07

All green, no incidents. **Production health** `/health` = `ok`, zero problems, `stuckVideoBacklog` 0, DB reachable; all five crons within their staleness thresholds (finalize-videos 10s, publish-scheduled 15s, rollup-hashtag-trends 630s, rollup-watch-ratios 1435s, save-nudges 27588s vs its 26h/93600s limit — expected for a daily 21:00 UTC job). `getsizzle.app` = 200. **CI** run 31240000167 (latest push) concluded `success`; dependency-review is correctly skipped on push events. **PRs**: none open — no dependency/Renovate PRs to triage. **npm audit**: still 10 advisories, unchanged in count from the 2026-08-06 baseline, but the one prod-path item (hono core CORS ReDoS) is now cleared — remainder are transitive build tooling already tracked in TD-14/TD-18; one dev-only+Windows-only `@hono/node-server` serve-static advisory surfaced and is now logged as TD-19 (non-exploitable here — prod uses the Vercel adapter, dev is macOS/Linux). **Supabase read-only state**: `pending_media_deletions` with attempts≥10 = 0; `video_assets` stuck non-ready >6h = 0; `cron_runs` all showing recent successes with clean `last_result` payloads (no repeated failures). **Flag drift corrected** in `technical-debt.md`: verified-fixed entries **TD-2** (hono 4.13.0), **TD-3** (hashtag-moderate `requireAdminUnlock` inline), **TD-4** (cron_runs heartbeats + honest `/health`) closed against commit 7843842; **TD-5** and **TD-8** narrowed to their genuinely-remaining web-side pieces (web Sentry has no `release` tag; web `version.json` not emitted; deploy.md still cites Railway/Fly); **TD-19** added. No code changes shipped this sweep — the only remaining P1, **TD-1** (9 conditional-hook lint errors in RecipeSheet/CommentsSheet/CookSheet), requires iOS-Simulator verification and is out of the unattended lane. Pre-existing uncommitted web WIP (App.tsx/sentry.ts/main.tsx — stale-chunk reload + local-dev Sentry suppression) was left untouched. **For Branden**: nothing Level D or urgent. Small next-session items when convenient — finish TD-5/TD-8 web bits (Sentry `release` + `version.json`), fix TD-1 on the simulator, and note that `autonomy-audit.md`'s implementation ledger (its rows #4/#5/#10/#12) is now stale relative to the corrected `technical-debt.md`.

## Daily sweep 2026-08-08

All green, no incidents, no code changes. **Production health** `/health` = `ok`, zero problems, `stuckVideoBacklog` 0, DB reachable, `paymentsKeyMode` live, push/email/Sentry/moderation all configured; cron ages all within cadence (finalize-videos 7s, publish-scheduled 40s, rollup-hashtag-trends 725s [<15m], rollup-watch-ratios 1528s [<30m], save-nudges 65529s ≈18.2h — expected for the daily 21:00 UTC job; its last success was 2026-08-07 21:01 UTC with a clean `{nudged:0,considered:3}`). `getsizzle.app` = 200. **CI**: last 5 runs on `main` all `success` (latest 31240244864). **PRs**: none open — nothing to triage or merge. **npm audit**: unchanged from the 2026-08-07 baseline — 10 advisories (1 critical / 6 high / 3 moderate), every one transitive dev/build tooling. Traced the one not previously named in TD-14: nanoid 3.3.13 (high) is a `postcss`→`vite` subdep (dev-only per lockfile, zero production request-path exposure) and is cleared by the same non-`--force` `npm audit fix` as the rest of the TD-14/TD-18 tier; `@hono/node-server` moderate stays TD-19 (dev + Windows-only, non-exploitable here). No in-lane fix taken: every remaining fix lands inside the deliberately-sequenced TD-14 (move `@capacitor/cli`→devDeps first) / TD-18 (Vite major) plans, not appropriate for unattended lockfile churn. **Supabase read-only state**: `pending_media_deletions` attempts≥10 = 0; `video_assets` non-ready >6h = 0; `cron_runs` all five recent successes with clean `last_result`, none failed (`last_started_at ≤ last_success_at` for every job). **Flag drift**: HEAD is still the prior sweep's own commit (c47cfec) with the working tree up-to-date on origin, so no code has landed since 2026-08-07 — `technical-debt.md` remains accurate as written; no entries opened or closed. Re-verified TD-5's remaining web item against reality: on-disk `apps/web/src/lib/sentry.ts` still emits no `release` tag, so it stays open. Pre-existing uncommitted web WIP (App.tsx/sentry.ts/main.tsx — stale-chunk reload + local-dev Sentry suppression) left untouched per rule 11. **For Branden**: nothing Level D or urgent. Same convenient next-session items as yesterday — finish the TD-5/TD-8 web bits (Sentry `release` + `version.json`) and the TD-1 conditional-hook fix; both are out of the unattended lane (TD-5/8 web files are entangled with your uncommitted WIP; TD-1 needs the iOS Simulator).

## Daily sweep 2026-08-09

**One real finding — a P0 secret exposure needing Branden — everything else green.** ⚠️ **Secret-scanning alert #1 is OPEN** (`gh api .../secret-scanning/alerts?state=open`): a Supabase Personal Access Token (`sbp_…`) was committed 2026-07-15 in `.codex/config.toml` at commit `16355f66` ("Stop the app making claims that aren't true"), which sits on `origin/main` → `publicly_leaked:true`. This is a recurrence of the CLAUDE.md hard-rule-1 PAT-leak class. Evidence gathered: `.codex/` **is now gitignored** (`.gitignore:58`) so it cannot re-leak; the file is **untracked at HEAD**; and the leaked value is **absent from every current working-tree `.toml/.json/.env`** (local config already replaced — the file was last modified 2026-07-15 22:35, after the 22:14 leak commit), which strongly suggests it was already rotated locally. **What could not be verified unattended:** whether that PAT is actually revoked on Supabase's side (the open alert means it may still be live), and `.mcp.json` was left untouched (permission-denied — correct). This is the first sweep to run the item-5b secret-scanning API check, which is why the 08-07/08-08 sweeps (which reported "all green") never saw an alert that has been open since 08-07. **Rotation is Level D** (credentials) — logged as **TD-20 (P1)** and Branden notified by push. Owner actions: (1) revoke the PAT in Supabase → Account → Access Tokens if not already done; (2) dismiss GitHub alert #1 as "revoked". No history rewrite attempted (destructive force-push on shared `main`; unnecessary once revoked). **Production health** `/health` = `ok`, zero problems, `stuckVideoBacklog` 0, `paymentsKeyMode` live, push/email/Sentry/moderation configured; all crons on-cadence (finalize-videos 7s, publish-scheduled 3s, rollup-hashtag-trends 757s [<15m], rollup-watch-ratios 1529s [<30m], save-nudges 65529s ≈18.2h — expected for the daily 21:00 UTC job, last success 2026-08-08 21:01 UTC `{nudged:0,considered:3}`). `getsizzle.app` = 200. **CI**: last push commit (4740fc9) `CI` = success; both open PRs' checks all green. The two `failure` rows in `gh run list` are the **Dependabot Updates** background workflow (security-update *refresh* erroring in `update_files` because PRs #3/#6 already cover those deps in the monorepo — a known Dependabot monorepo quirk targeting `/apps/api` vs the existing `/` PR), **not** app CI and not a code defect. **PRs triaged**: two open, both **breaking/major** and thus held for owner with risk-summary comments posted — **#3** `@hono/node-server` 1.19.17→2.0.10 (major; dev-only dep, not in the Vercel prod request path — see TD-19) and **#6** esbuild 0.24.2→0.28.2 + grouped vite (breaking build-tooling, TD-18's first Vite item; dev-only advisory). Neither is auto-mergeable (breaking) so neither was merged. **npm audit**: 3 advisories (2 moderate + 1 high), all dev/build tooling, all requiring breaking fixes → all already tracked (TD-18/TD-19); none in-lane. **Supabase security advisors**: no new findings — only the documented baseline (deny-all `rls_enabled_no_policy` INFO lints, `pg_trgm` in public WARN, leaked-password-protection pending WARN). **Supabase read-only state**: `pending_media_deletions` total 0 (attempts≥10 = 0); `video_assets` non-ready 0 (stuck >6h = 0); `cron_runs` all five recent successes with clean `last_result`, no repeated failures. **Flag drift**: corrected TD-19's stale version (1.19.14→1.19.17) and linked PRs #3/#6 to TD-19/TD-18; added **TD-20** for the leaked PAT. Pre-existing uncommitted web WIP (App.tsx/sentry.ts/main.tsx) left untouched per rule 11; only `technical-debt.md` and this log were staged. **For Branden (do this):** revoke + dismiss the leaked Supabase PAT (2-minute Level-D task, details in TD-20 / the push). Non-urgent when convenient: decide on the two held major PRs (#3/#6), and the standing TD-5/TD-8 web bits + TD-1 simulator fix.

## Incident 2026-08-10 07:47 PDT — watchdog summon: API + frontend "unreachable" → FALSE ALARM (transient local DNS)

**What fired.** Watchdog (`scripts/ops/watchdog.sh`) summoned an unattended session at 2026-08-10 07:47:12 local reporting `API unreachable (HTTP 000)` and `Frontend https://getsizzle.app HTTP 000000`. Raw /health capture was `curl: (6) Could not resolve host: sizzle-chi.vercel.app`.

**Root cause (verified, not the symptom).** A transient **DNS/network failure on the watchdog host itself** at the moment of the 07:47 probe — *not* a production outage. Evidence for host-side, not origin-side: (1) the API failure was `curl (6) could not resolve host` — a resolver failure, whereas a Vercel origin outage would still resolve DNS and return 5xx from the edge; (2) both independently-deployed Vercel projects (API `sizzle` + frontend `sizzle-api`) failed *simultaneously* at the connection layer (`000`/unresolved), the fingerprint of a single shared client-side network drop rather than two separate origins dying in the same second; (3) `watchdog.sh` itself anticipates this exact class ("a summon that dies on a transient network error e.g. right after Mac wake"). Most likely a momentary resolver hiccup coinciding with the 5-min launchd tick / Mac wake.

**Production was never actually down.** On probe (~3 min after detection) both surfaces were fully healthy and stayed healthy across a spaced recovery window:
- API `/health` → **HTTP 200**, body `{"status":"ok","problems":[],"commit":"a42806c",...}` — `stuckVideoBacklog` 0, `cloudflareConfigured` true, `push` ok, `paymentsKeyMode` live, email/Sentry/moderation configured. `commit` = current HEAD `a42806c`, so **no rogue deploy** went out around the alert time.
- Frontend `getsizzle.app` → **HTTP 200**.
- Spaced confirmations (anti-flap): 14:49 UTC api=200 web=200 (dns 0.186s); 14:50 UTC health `status:ok` problems `[]`; 14:52 UTC api=200 web=200 (dns 0.009s/0.004s). No flapping; DNS lookup time fell from 0.186s→0.009s as the resolver cache re-warmed — consistent with a just-cleared momentary resolver failure.

**What I did.** Diagnosed only — no code change, no deploy, no rollback (all correctly unwarranted: the previous deploy was fine and is still live). Verified recovery is stable, logged this entry.

**Still open / for Branden.** Nothing urgent from this incident. Two non-blocking observations noted in passing (not the cause, not acted on): (a) `/health` reports `cronAges.save-nudges` ≈ 64,209 min (~44.6h) — larger than the ~18h seen in recent sweeps; `/health` still returns `ok` so it is below its gating threshold, but worth a glance next sweep to confirm the daily 21:00 UTC job is still firing; (b) the pre-existing uncommitted web WIP (App.tsx/sentry.ts/main.tsx) was left untouched per hard-rule 11. Possible hardening (out of unattended lane, Level C — touches ops guardrail): give the watchdog a brief retry/confirm before summoning on `000`/DNS-resolution failures, so a single host-side blip doesn't burn an incident summon.

## Daily sweep 2026-08-10

**All green — and the 08-09 P0 is closed: the leaked Supabase PAT was revoked and secret-scanning alert #1 resolved.** ✅ **TD-20 closed:** `gh api .../secret-scanning/alerts?state=open` returns `[]`; alert #1 shows `resolution:"revoked"`, `resolved_at 2026-08-10T14:52:32Z` (owner action ~20 min before this sweep ran). Debt register updated with the evidence. **Production health** `/health` = `ok`, zero problems, `stuckVideoBacklog` 0, `paymentsKeyMode` live, push/email/Sentry/moderation configured; `getsizzle.app` = 200. All crons on-cadence (finalize-videos 39s, publish-scheduled 10s, rollup-hashtag-trends 783s [<15m], rollup-watch-ratios 1557s [<30m], save-nudges 65,581s ≈18.2h — expected for the daily 21:00 UTC job; last success 2026-08-09 21:00 UTC with clean `{nudged:0,considered:3}`). This also **closes observation (a) from the 07:47 incident entry above**: its "~44.6h" save-nudges figure was a unit misread — `cronAges` is **seconds**, and 65,581s matches `cron_runs.last_success_at` to the second; the job is firing on schedule. **CI**: last 5 runs on `main` all `success` (latest 31400654395). **PRs**: the same two held majors — **#3** `@hono/node-server` 1.19.17→2.0.10 and **#6** esbuild 0.24.2→0.28.2 + vite 5.4.21→7.3.6 — both CI-green and both already carrying the 08-09 hold-for-owner risk comments; correctly not merged (major/breaking ⇒ owner lane; TD-18/TD-19). **npm audit**: unchanged — 3 advisories (2 moderate, 1 high), all dev/build tooling, every available fix semver-major, all tracked in TD-18/TD-19; nothing in-lane. One nuance verified: `npm audit` now proposes vite **8.2.1** as the fix root while PR #6 targets **7.3.6** — the three vite-tree advisory ranges all end at ≤6.4.2, so #6's 7.3.6 already clears them; vite 8 remains a TD-18 queue decision, not a security requirement. **Supabase security advisors**: baseline only (deny-all `rls_enabled_no_policy` INFO lints, `pg_trgm` in public WARN, leaked-password-protection WARN) — no new findings. **Supabase read-only state**: `pending_media_deletions` attempts≥10 = 0; `video_assets` non-ready >6h = 0; `cron_runs` all five recent successes with clean `last_result`, no repeated failures. **Flag drift**: TD-20 closed this sweep; every other `technical-debt.md` entry matches reality as re-checked. Pre-existing uncommitted web WIP (App.tsx/sentry.ts/main.tsx — stale-chunk auto-reload on `vite:preloadError` + Marketing error boundary + local-dev Sentry suppression) left untouched per rule 11; only the debt register and this log were staged. **For Branden**: nothing urgent, nothing Level D. Standing items when convenient: decide the two held major PRs (#3/#6), the TD-5/TD-8 web bits (Sentry `release` + `version.json` — entangled with your uncommitted WIP), and TD-1 conditional hooks on the simulator.

## Incident 2026-08-10 09:00 PDT — watchdog summon: "CI failing on main: Uptime" → FALSE ALARM (intentional alert-chain self-test)

**What fired.** Watchdog (`scripts/ops/watchdog.sh`) summoned an unattended session at 2026-08-10 09:00:05 local (16:00:05 UTC) reporting `CI failing on main: Uptime`. Raw `/health` in the summon was already `{"status":"ok","problems":[],"commit":"eeffeb2",...}` — i.e. production healthy at detection time.

**Root cause (verified, not the symptom).** The watchdog's CI probe (`watchdog.sh:46-50`) reads the single most-recent **completed** run on `main` (`gh run list --branch main --status completed --limit 1`) and pages if its conclusion is `failure`. At the poll instant that run was Uptime workflow dispatch **`31406471914`**, manually triggered with **`force_fail=true`** at 08:58 local — a **deliberate alert-chain self-test**. The failing step is literally named "Test-mode deliberate failure" and does `echo "::error::ALERT-CHAIN TEST — this failure is intentional (force_fail=true)."; exit 1` (`uptime.yml:29-33`). The immediately-prior real dispatch (`31406440374`, no force_fail) **passed**, its probes showing `/health` HTTP 200 and `getsizzle.app` HTTP 200 at 15:58 UTC. Production never went down. **Compounding 1-second race:** the successful `ci.yml` push run for HEAD `eeffeb2` ("Runbook…") completed at 16:00:04Z (`success`) and by created-time is newer than the force_fail dispatch — it would have been the latest completed run — but the watchdog queried at 16:00:05 before that CI run flipped to `completed`, so it saw the intentional test failure instead. The condition therefore **self-cleared within ~1s**: the watchdog CI probe now returns `conclusion=success, "Runbook: document the GitHub Actions uptime paging layer", event=push`.

**Production was never actually down.** Healthy across four probes spanning ~6 min:
- 15:58:12 UTC (Uptime dispatch `…40374`): `/health` 200, `getsizzle.app` 200.
- 16:00:03 UTC (summon capture): `/health` `status:ok`, `problems:[]`, commit `eeffeb2`.
- 16:01:31 UTC (direct): `/health` 200 `status:ok problems:[]`; frontend 200.
- 16:04:17 UTC (direct, final): `/health` 200 `status:ok problems:[]`, `stuckVideoBacklog` 0, `paymentsKeyMode` live, push/email/Sentry/moderation configured; frontend 200. Commit stayed `eeffeb2` throughout → no rogue deploy.

**What I did.** Diagnosed only — no code change, no deploy, no rollback, no watchdog edit (all correctly unwarranted; the "failure" was an intentional test, not a regression, and modifying alert logic under an unattended incident risks suppressing real pages — a monitoring-design call for the owner). Verified the failing run is the deliberate `force_fail` test, confirmed production health at four timestamps, and confirmed the watchdog condition has already self-cleared.

**Separate observation (unrelated to the page, not acted on).** Two **Dependabot Updates** runs failed at 15:58 UTC — `dependency_file_not_supported` for `vite` (`/apps/web`) and `@hono/node-server` (`/apps/api`): Dependabot **security** updates target the per-package manifest dirs, but this npm-workspaces monorepo keeps its lockfile at root `/`, so Dependabot "can't update … without a lockfile or pinned version requirement." Prior root-targeted Dependabot runs (08-08) passed. This does **not** affect running production and does **not** gate `ci.yml` (which is green); it only means a bump PR wasn't opened. It was **not** what the watchdog paged on.

**Still open / for Branden (nothing urgent, nothing Level D).**
1. **Watchdog CI probe is over-broad (recurring flap source).** Any latest-completed `failure` on `main` pages — including intentional Uptime `force_fail` self-tests and unrelated Dependabot failures. Consider scoping the probe to the workflow(s) that actually gate production and/or excluding `workflow_dispatch` runs of Uptime, **without** blinding it to a real red. This is a `scripts/ops/` guardrail change → owner-reviewed lane (out of the unattended incident lane). Companion to the 07:47 entry's hardening note (retry/confirm on `000`/DNS blips).
2. **Dependabot monorepo security-update config.** `.github/dependabot.yml` (or the security-update directory targets) so per-workspace security updates resolve the root lockfile — clears the two recurring red rows and confirms whether an open `vite` advisory needs addressing another way (Level C; `.github/**`). Overlaps the existing TD-18 vite track.

## Security sweep 2026-08-10 (weekly adversarial)

**One in-lane security fix shipped, nine false-positive alerts dismissed with reasons, one doc-accuracy correction — no new exploitable exposure; all prod controls verified intact.** This is the **first weekly sweep to run after CodeQL was enabled** (commit `9812618`, 2026-08-08), so it triaged CodeQL's entire initial backlog.

**① Supabase security advisors** (`get_advisors type=security`): **baseline only, no new/escalated findings.** ~31 deny-all `rls_enabled_no_policy` INFO lints on service-role-only tables (now including the new `cron_runs`), `pg_trgm` in `public` WARN (accepted), leaked-password-protection WARN (pending Branden — Level D). Matches SYSTEM_RISK_MAP exactly.

**② CodeQL — 10 open alerts, all triaged.**
- **REAL, FIXED in-lane (1):** #10 `js/stack-trace-exposure` MEDIUM at `apps/api/api/index.ts:27` — the pre-app bootstrap init-failure handler returned the raw `initError` (an `err.stack`) to the caller on a 500. This is the one error path that bypasses the exemplary global `onError` (`lib/errors.ts`, which already never leaks 5xx detail). Fixed to `console.error` the detail server-side (Vercel logs) and return a generic `{error:'api_init_failed'}` body — no stack/paths to clients (CWE-209). Tightening-only: no client depends on the `detail` field; init-failure basically never fires in prod. API `npm run typecheck` clean; `initError` still read by the init guard + the new log. Only #10 remains open post-fix (auto-resolves on the next CodeQL scan after this lands).
- **DISMISSED as false-positive via the code-scanning API (9), each with a specific reason:** #1 `js/insecure-randomness` (`services/stream.ts:64`) — `MockStream` is local-dev-only (used only when Cloudflare is unconfigured; prod `videoProvider=cloudflare`), the `Math.random` UID is a throwaway local id, not a security token ("won't fix"). #2–#7 `js/xss-through-dom` (`ImageCropper.tsx:95`, `MadeItPrompt.tsx:92`, `VideoTrimmer.tsx:86`, `sheets/UploadSheet.tsx:539`, `lib/storage.ts:246` & `:282`) — every flagged sink is an `<img>/<video> src` (or React `src` prop) fed a local `blob:`/`capacitor:`/object URL of the user's OWN picked media; `img`/`video` `src` is not a script-execution sink and the value is not attacker-controlled cross-user data. #8–#9 `js/incomplete-sanitization` (`routes/cooks.ts:104`, `routes/seo.ts:247`) — the handle is allowlisted to `[A-Za-z0-9_]` immediately BEFORE the `_`→`\_` escape, so `%`/`\` can never reach it, and it is passed as a parameterized PostgREST `.ilike()` filter (no SQL injection).

**③ Dependabot — 9 open alerts, all non-exploitable in prod, all already tracked; no in-lane bump (every fix is a semver-major in Renovate's needs-owner-review group).** `@hono/node-server` 1.19.17 serve-static path-traversal (#1/#16, GHSA-frvp-7c67-39w9, Windows-only) = **TD-19**, whose rationale I **corrected this sweep**: the old note ("not in the prod path / uses `hono/vercel` adapter; node-server is local-dev only") was factually wrong — `grep` confirms there is **no `hono/vercel` usage** and `getRequestListener` from `@hono/node-server` **is** the prod Vercel adapter (`api/index.ts:28`). The accurate two-fold non-exploitability: (a) the vulnerable `serve-static` middleware is **never imported anywhere** (`grep -rn serveStatic apps/api` → 0 matches), and (b) prod runs on Vercel **Linux** while the advisory is **Windows-only**. vite ≤6.4.2 / esbuild ≤0.24.2 (#2–#8) = dev-only build tooling, not in the production runtime, dev host macOS → **TD-18** + held PR #6.

**④ Secret scanning + hygiene:** `secret-scanning/alerts?state=open` = `[]` (TD-20 stays closed). `node scripts/secrets-check.mjs --all` = **clean (450 files, all tracked)**. Only two config files were added in the last 8 days — `.claude/settings.json` (a permissions allowlist) and `renovate.json` (dep-update config); both eyeballed, neither holds a secret. No `.env`/plist/service-account committed.

**⑤ Route-auth audit:** no NEW routes since last week — the only code commits in the window are two **tightening** migrations (`20260808160000` revoke-definer-fn-execute, `20260806230000` cron_heartbeats). Webhook signature verification intact: **Stripe** recomputes HMAC-SHA256 over the raw `${t}.${raw}` and compares with `timingSafeEqual`, 401 on mismatch (`monetize.ts:874–884`); **RevenueCat** constant-time `timingSafeEqual` on `REVENUECAT_WEBHOOK_AUTH` (`monetize.ts:797–799`). Global `onError` confirmed to never leak 5xx detail/stacks (`lib/errors.ts:29–33`).

**⑥ RLS spot-check** (read-only, tables/functions touched by last week's migrations): `cron_runs` = RLS on, 0 policies, **no** anon/authenticated grants (deny-all ✓); `creator_earnings` + both `enqueue_profile_media_sweep`/`enqueue_video_asset_media_deletion` definer fns = EXECUTE **denied** to anon & authenticated (the new revoke migration verified **live** ✓). **Documented open findings unchanged, no regression:** `recipe_steps`/`recipe_ingredients` still grant `authenticated` (and `anon`) full DML = SYSTEM_RISK_MAP **open finding #1** (moderation bypass on owner edit-through-PostgREST; RLS still blocks non-owners since `auth.uid()` scoping holds — the gap is that `moderate()` never re-runs; fix is a Level-C revoke migration pending Branden, not shippable unattended per CLAUDE.md rule 10). `comment_likes`/`reports` still grant DML = finding #4 (low impact).

**⑦ Exposure drift:** `/health` = `ok`, structure-only (booleans + modes + commit SHA `eeffeb2`), **no secrets/keys**, `paymentsKeyMode` live. `/admin/security-status` and `/admin/log` both **401** unauthenticated.

**Shipped this sweep** (staged explicitly — `apps/api/api/index.ts`, `docs/engineering/technical-debt.md`, `docs/operations/incidents/LOG.md`; pre-existing uncommitted web WIP App.tsx/sentry.ts/main.tsx left untouched per rule 11): the stack-trace-exposure fix + TD-19 rationale correction. Dismissals were applied directly via the GitHub API (no repo change). **For Branden (nothing Level D from this sweep beyond the standing items):** SYSTEM_RISK_MAP open finding #1 (recipe_steps/ingredients revoke migration) remains the one meaningful open security item — a ~2-line tightening migration that needs your authorization to run in prod; plus the standing enable-leaked-password-protection toggle, held major PRs #3/#6, and TD-1/TD-5/TD-8.

## Auth hardening 2026-08-10 (attended)
Enabled leaked-password protection (`password_hibp_enabled: true`) and raised the
server password floor 6 → 10 via the Supabase Management API — the client signup
form already required 10+, so the weaker server floor only affected API-direct
signups. Verified live; security advisors now show zero actionable warnings
(deny-all INFO baseline + accepted pg_trgm only). Existing passwords unaffected.

## Daily sweep 2026-08-11

**All green — status quo since the 08-10 daily sweep; nothing needed Branden and nothing shipped.** **Production health** `/health` = `ok`, `problems:[]`, `stuckVideoBacklog` 0, `commit` `e48e667` (= current HEAD, so no rogue deploy), `paymentsKeyMode` live, push/email/Sentry/moderation configured; `getsizzle.app` = 200. **Crons** all on-cadence and last-run-successful — verified directly against `cron_runs` (every job `last_started_at ≤ last_success_at`, i.e. the last run succeeded): finalize-videos 35s, publish-scheduled 7s, rollup-hashtag-trends 21s (`{updated:32}`), rollup-watch-ratios 1,714s ≈28.5m (runs :17/:47, last success 14:47 UTC), save-nudges 65,686s ≈18.2h (daily 21:00 UTC, last success 2026-08-10 21:01 UTC, clean `{nudged:0,considered:3}`); each is within its `/health` stale threshold. **CI**: the `CI` workflow ran **green on the latest main commit `e48e667`** (push event) and there were **0** non-success runs across the last 20 on `main` — confirmed CI is genuinely active on main pushes, not silently skipped (the frequent `Uptime` scheduled runs are all `success`). **PRs**: still the same two held majors — **#3** `@hono/node-server` 1.19.17→2.0.10 and **#6** esbuild 0.24.2→0.28.2 + vite 5.4.21→7.3.6 — both CI-green and both already carrying the 08-09 hold-for-owner risk comments; correctly **not** merged (both semver-**major** ⇒ owner lane per triage rule 3; TD-18/TD-19). No patch/minor dependency PRs exist to auto-merge. **npm audit**: unchanged — 3 advisories: vite **HIGH** (7.5, `server.fs.deny` bypass, GHSA-fx2h-pf6j-xcff), esbuild moderate (dev-server, GHSA-67mh-4wv8-2f99), `@hono/node-server` moderate (serve-static path traversal, GHSA-frvp-7c67-39w9). All are dev/build-tooling and **non-prod** (the Vite dev server never runs in production — Sizzle ships a pre-built static bundle; `serve-static` is never imported and prod is Linux while the advisory is Windows-only), every available fix is a semver-major already tracked in TD-18/TD-19; nothing in-lane. Down from 10 advisories at the 08-06 audit. **Supabase security advisors** (`get_advisors security`): baseline only — deny-all `rls_enabled_no_policy` INFO lints + `pg_trgm`-in-public WARN; the leaked-password-protection WARN is now **gone**, confirming the 08-10 attended `password_hibp_enabled` toggle (commit `e48e667`) took effect. No new or escalated findings. **Secret scanning**: `secret-scanning/alerts?state=open` = `[]` — no P0; TD-20 stays closed. **Supabase read-only state**: `pending_media_deletions` attempts≥10 = 0; `video_assets` non-ready >6h = 0; Cloudflare backlog >15m = 0; `cron_runs` shows no repeated failures. **Flag drift**: `technical-debt.md` re-checked and still matches reality (TD-20 closed 08-10; no entry to open or close today), so nothing was staged there. Pre-existing uncommitted web WIP (App.tsx / lib/sentry.ts / main.tsx) left untouched per hard-rule 11; **only this log was staged.** `npm run secrets:check` clean before push; `verify-deploy` intentionally **not** run — this is a docs-only push that touches no app, so no Vercel rebuild is expected (the `/health` commit SHA won't advance). **For Branden**: nothing urgent, nothing Level D. Standing items unchanged — decide the two held major PRs (#3/#6); the TD-5/TD-8 web bits (Sentry `release` + `version.json`, entangled with your uncommitted WIP); TD-1 conditional hooks on the simulator; SYSTEM_RISK_MAP open finding #1 (recipe_steps/ingredients revoke migration, Level C).

## Daily sweep 2026-08-12

**All green — second consecutive no-action day; nothing shipped but this log.** **Production health** `/health` = `ok`, `problems:[]`, `stuckVideoBacklog` 0, `commit` `2b75926` (= current HEAD, no rogue deploy), `paymentsKeyMode` live, push/email/Sentry/moderation configured; `getsizzle.app` = 200. **Crons** all on-cadence and last-run-successful, verified against `cron_runs` (every job `last_started_at ≤ last_success_at`): finalize-videos 54s, publish-scheduled 57s, rollup-hashtag-trends 726s (`{updated:32}`), rollup-watch-ratios 1,577s ≈26m, save-nudges 65,581s ≈18.2h (daily 21:00 UTC, last success 2026-08-11 21:00 UTC, `{nudged:0,considered:3}`) — all within `/health` stale thresholds, no failures. **CI**: `CI` workflow green on the HEAD push `2b75926` (58s); all scheduled `Uptime` runs success. **PRs**: unchanged — the two held majors **#3** (`@hono/node-server` 1.19.17→2.0.10) and **#6** (esbuild 0.24.2→0.28.2 + vite), both CI-green, both already carrying the 08-09 hold-for-owner risk comments; correctly **not** merged (semver-major ⇒ owner lane; TD-18/TD-19). One new fact verified this sweep: PR #3's target 2.0.10 also ships the fix for **GHSA-9mqv-5hh9-4cgg** (unauthenticated memory-leak DoS via aborted WebSocket handshake in `upgradeWebSocket`) — grep confirms `upgradeWebSocket`, like `serveStatic`, is **never imported** in `apps/api`, so the installed 1.19.17 stays non-exploitable on every published count; TD-19's register text remains accurate, no drift edit needed. **npm audit**: unchanged — 3 advisories (vite HIGH GHSA-fx2h-pf6j-xcff, esbuild moderate GHSA-67mh-4wv8-2f99, `@hono/node-server` moderate GHSA-frvp-7c67-39w9), all dev/build-tooling or never-imported code paths, every fix semver-major and tracked in TD-18/TD-19; nothing in-lane. **Supabase security advisors**: documented baseline only (deny-all `rls_enabled_no_policy` INFO lints + `pg_trgm` WARN); the leaked-password WARN remains gone post-`e48e667`. **Secret scanning**: open alerts = `[]` (TD-20 stays closed). **Read-only DB state**: `pending_media_deletions` attempts≥10 = 0; `video_assets` non-ready >6h = 0; `cron_runs` shows no repeated failures. **Flag drift**: `technical-debt.md` re-checked and still matches reality — untouched. Pre-existing uncommitted web WIP (App.tsx / lib/sentry.ts / main.tsx, +61/−4 — an in-progress guard suppressing Sentry reporting from locally-served builds) left untouched per hard-rule 11; **only this log staged**. `npm run secrets:check` clean before push; `verify-deploy` intentionally not run — docs-only push, no app affected, the `/health` commit SHA will not advance. **For Branden**: nothing urgent, nothing Level D. Standing items unchanged — decide held majors #3/#6; TD-1 conditional hooks on the simulator; TD-5/TD-8 web bits (Sentry `release` + `version.json`, entangled with your uncommitted WIP); SYSTEM_RISK_MAP open finding #1 (recipe_steps/ingredients revoke migration, Level C).

## Daily sweep 2026-08-13

**All green, and one debt entry closed on new evidence: TD-19 (`@hono/node-server`) is dead — the advisory stopped covering our installed version yesterday.** **Production health** `/health` = `ok`, `problems:[]`, `stuckVideoBacklog` 0, `commit` `a767d7a` (= current HEAD, no rogue deploy), `paymentsKeyMode` live, push/email/Sentry/moderation configured; `getsizzle.app` = 200 in 0.27s. **Crons** all five on-cadence and last-run-successful, verified against `cron_runs` (every job `last_started_at ≤ last_success_at`): finalize-videos 19s, publish-scheduled 35s, rollup-hashtag-trends 806s (`{updated:32}`), rollup-watch-ratios 1,635s ≈27m (runs :17/:47, last success 14:47 UTC `{updated:1}`), save-nudges 65,691s ≈18.25h — expected for the daily 21:00 UTC job (last success 2026-08-12 21:00 UTC, clean `{nudged:0,considered:3}`); each within its `/health` stale threshold, no failures. **CI**: 25/25 most recent runs on `main` are `success` (all scheduled `Uptime`; no push runs in the window because the last two commits were docs-only ops sweeps). **Read-only DB state**: `pending_media_deletions` attempts≥10 = **0** (table empty); `video_assets` non-ready >6h = **0** (zero non-ready rows at all). **Supabase security advisors**: documented baseline only — deny-all `rls_enabled_no_policy` INFO lints + `pg_trgm`-in-public WARN; leaked-password WARN still absent post-`e48e667`. No new or escalated findings. **Secret scanning**: `secret-scanning/alerts?state=open` = `[]` — no P0; TD-20 stays closed.

**The one real change this sweep — `npm audit` dropped 3 advisories → 2, and I root-caused the delta rather than assuming an install drifted.** The vanished item is `@hono/node-server` GHSA-frvp-7c67-39w9. Nothing changed locally: installed **and** lockfile are still `1.19.17`, `apps/api/package.json` still `^1.13.7`, `git status` on both files clean. The change is **upstream**: GitHub **narrowed the advisory's vulnerable range** at `updated_at 2026-08-12T18:26:51Z` — a few hours *after* the 08-12 sweep ran — from one that included 1.19.17 to `< 1.19.15` **plus** `>= 2.0.0, < 2.0.5`. 1.19.17 sits outside both. Corroborated three ways: `npm audit` now has **zero** hono mentions; the `@hono/node-server` Dependabot alerts (#1/#16) are absent from `dependabot/alerts` at **every** state, not merely dismissed; and the only other published node-server advisory, GHSA-9mqv-5hh9-4cgg (unauthenticated memory-leak DoS via aborted WebSocket handshake), is `>= 2.0.0, <= 2.0.9`, so 1.x was never in scope. **→ TD-19 closed** with that evidence. The prior two-fold non-exploitability argument was independently re-verified this sweep (`serveStatic` **0** import sites, `upgradeWebSocket` **0**, `getRequestListener` confirmed as the prod Vercel adapter at `api/index.ts:28`, prod on Linux vs a Windows-only advisory) and still holds — it is simply no longer the load-bearing reason. Consequence worth flagging: **PR #3 is no longer a security PR**, just an optional major; TD-18's queue note was updated to say so and a dated explainer comment was posted to the PR.

**Remaining advisories (2), both unchanged and both out-of-lane**: vite **HIGH** (GHSA-fx2h-pf6j-xcff) plus two vite MEDIUMs (GHSA-4w7w-66w2-5vf9, GHSA-v6wh-96g9-6wx3) and esbuild MEDIUM (GHSA-67mh-4wv8-2f99) — `npm audit` rolls the vite tree into one row. All are dev/build tooling that never runs in production (Sizzle ships a pre-built static bundle; the Vite dev server is not a production surface), every available fix is semver-**major**, and all are tracked in TD-18. Nothing patch-level existed to take in-lane. **PRs**: unchanged and correctly not merged — **#6** (esbuild 0.24.2→0.28.2 + vite 5.4.21→7.3.6) and **#3** (`@hono/node-server` 1.19.17→2.0.10), both CI-green, both semver-major ⇒ owner lane per triage rule 3, both already carrying hold-for-owner risk comments. No patch/minor dependency PRs exist to auto-merge.

**Flag drift**: register re-checked line by line against reality, not taken on trust — TD-1 re-verified by running `npm run lint` (exactly **9** errors, in precisely the three files the entry names: RecipeSheet.tsx:75-91, CommentsSheet.tsx:41, CookSheet.tsx:357 ✓); TD-5's remaining half confirmed still real (`git show HEAD:apps/web/src/lib/sentry.ts` has no `release` tag); TD-8's remainder confirmed (no web `version.json` emitted; `docs/deploy.md` still cites Railway/Fly); TD-17 confirmed (`.github/modernize/java-upgrade/` still present). Only TD-19 needed closing; nothing new to open. Pre-existing uncommitted web WIP (App.tsx / lib/sentry.ts / main.tsx, +61/−4 — stale-chunk auto-reload on `vite:preloadError`, a Marketing error boundary, and local-dev Sentry suppression) left **untouched** per hard-rule 11; only `technical-debt.md` and this log were staged. `npm run secrets:check` clean before push; `verify-deploy` intentionally **not** run — docs-only push touching no app, so no Vercel rebuild is expected and the `/health` commit SHA will not advance.

**For Branden**: nothing urgent, nothing Level D, no push notification sent. Standing items, now one lighter — **#3 lost its security justification**, so the held-majors decision is just #6 (vite/esbuild, the genuinely security-driven one) plus #3 whenever convenient; TD-1 conditional hooks on the simulator; TD-5/TD-8 web bits (Sentry `release` + `version.json`, entangled with your uncommitted WIP); SYSTEM_RISK_MAP open finding #1 (recipe_steps/ingredients revoke migration, Level C).

## Daily sweep 2026-08-14

**All green — no action needed, nothing shipped but this log; and the "dev-only" claim under the two held PRs stopped being my inference and became two independent upstream facts.** **Production health** `/health` = `ok`, `problems:[]`, `stuckVideoBacklog` 0, `commit` `e1ed276` (= current HEAD, so no rogue deploy), `paymentsKeyMode` **live**, `cloudflareConfigured`/`moderationConfigured`/`emailConfigured`/`sentryConfigured` true, `push` ok; `getsizzle.app` = 200 in 0.40s. **Crons** all five on-cadence and last-run-**successful**, verified against `cron_runs` rather than `/health` alone (every job `last_started_at ≤ last_success_at`): finalize-videos 52s (all-zero result, clean), publish-scheduled 32s (`{published:0}`), rollup-hashtag-trends 853s ≈14m (`{updated:32}`, runs \*/15), rollup-watch-ratios 1,676s ≈28m (runs :17/:47, last success 14:47 UTC `{updated:0}`), save-nudges 65,688s ≈18.25h — expected for the daily 21:00 UTC job (last success 2026-08-13 21:00 UTC, clean `{nudged:0,considered:3}`). Each is inside its `/health` stale threshold; no failures, no repeated-failure `last_result` anywhere. **CI**: the `CI` workflow ran **green on the HEAD push `e1ed276`** (51s) and `CodeQL` is green on its latest run; across the last 30 runs on `main` there are **zero** non-success rows. Worth noting for future sweeps: `gh run list --branch main --limit 5` shows only scheduled `Uptime` rows, because `Uptime` fires every ~22 min while the last two commits were docs-only — that is not CI being skipped, and it was confirmed by querying `--workflow=ci.yml` directly.

**Read-only DB state**: `pending_media_deletions` attempts≥10 = **0** (table is empty outright); `video_assets` non-ready >6h = **0**, and in fact **zero** non-ready rows at any age; no parked media, no stuck transcodes.

**npm audit — count unchanged at 2, but newly corroborated.** Still vite **HIGH** (GHSA-fx2h-pf6j-xcff) plus two vite MEDIUMs (GHSA-4w7w-66w2-5vf9, GHSA-v6wh-96g9-6wx3) and esbuild MEDIUM (GHSA-67mh-4wv8-2f99), `npm audit` rolling the vite tree into one row; every available fix is semver-**major**, all tracked in TD-18, nothing patch-level to take in-lane. Previous sweeps argued "dev-only, never runs in production" from first principles (Sizzle ships a pre-built static bundle; the Vite dev server is not a production surface). This sweep confirmed it **two independent ways that don't depend on my reasoning**: (1) `npm audit --omit=dev` reports **`found 0 vulnerabilities`** — npm's own production-scope resolution agrees nothing vulnerable reaches a shipped dependency tree; (2) **all 7 open Dependabot alerts** (`dependabot/alerts?state=open`) carry GitHub's own `"scope": "development"` classification — #2/#5/#6/#8 medium, #3/#7 high, #4 medium, across vite and esbuild only. Declaration sites match: `vite ^5.4.11` is in `apps/web` devDependencies and `esbuild ^0.24.0` is `apps/api` build tooling. So the hold on PR #6 costs no production exposure.

**PRs**: unchanged and correctly **not** merged — **#6** (esbuild 0.24.2→0.28.2 + vite 5.4.21→7.3.6) and **#3** (`@hono/node-server` 1.19.17→2.0.10). Both are CI-green on all 8 checks (checks, analyze, dependency-review, CodeQL, GitGuardian, both Vercel builds), both are semver-**major** ⇒ owner lane per triage rule 3, and both already carry hold-for-owner risk comments (#6 from 08-09, #3 updated 08-13 when its security driver vanished). No duplicate comment was added — the existing ones are still accurate. No patch/minor dependency PRs exist to auto-merge.

**Supabase security advisors** (`get_advisors type=security`): **documented baseline exactly, no new or escalated findings** — **31** deny-all `rls_enabled_no_policy` INFO lints on service-role-only tables (counted, and matching the "~31" recorded on 08-10) plus the accepted `pg_trgm`-in-public WARN. The leaked-password-protection WARN remains **absent**, still confirming the 08-10 attended `password_hibp_enabled` toggle (`e48e667`) holds. **Secret scanning**: `secret-scanning/alerts?state=open` = `[]` — no P0; TD-20 stays closed.

**Flag drift**: register re-checked against reality rather than trusted — TD-1 re-verified by running `npm run lint` (exactly **9** errors / 15 problems, in precisely the three files the entry names: RecipeSheet.tsx:75-91, CommentsSheet.tsx:41, CookSheet.tsx:357 ✓); TD-5's remaining half still real (`git show HEAD:apps/web/src/lib/sentry.ts` has no `release` tag); TD-8's remainder still real (no web `version.json`); TD-17 still real (`.github/modernize/java-upgrade/` present); TD-18's text re-checked against `gh pr diff 6`, which does target vite **7.3.6** and esbuild **0.28.2** as written. **Nothing to open and nothing to close**, so `technical-debt.md` was deliberately left untouched. Pre-existing uncommitted web WIP (App.tsx / lib/sentry.ts / main.tsx, +61/−4 — stale-chunk auto-reload on `vite:preloadError`, a Marketing error boundary, and local-dev Sentry suppression) left **untouched** per hard-rule 11; **only this log was staged.** `npm run secrets:check` clean before push (1 file staged, clean).

**Correction — a recurring wrong assumption in the last three sweep entries, caught by checking instead of asserting.** The 08-11, 08-12 and 08-13 entries each state that a docs-only push means "no Vercel rebuild is expected and the `/health` commit SHA will not advance", and each skipped `verify-deploy` on that basis. **That is false.** I wrote the same sentence here, then re-probed `/health` after pushing and found `commit` had advanced `e1ed276` → **`02cce9f`** — this docs-only commit rebuilt and promoted **both** projects. The earlier entries actually contain the disproof already: 08-13 recorded `commit a767d7a` "= current HEAD", and `a767d7a` *is* the 08-12 docs-only sweep commit, so that push plainly rebuilt too. The ignored-build-step does **not** skip these commits. Corrected practice, applied from today: **run `verify-deploy` after every sweep push, docs-only or not.** Doing so here passed cleanly — `sizzle (API)` deployment **READY**, `/health` HTTP 200, serving commit `02cce9f` **== HEAD ✓**, status `ok`; `sizzle-api (frontend)` deployment **READY**, `getsizzle.app` HTTP 200. One operational note for the next sweep: `verify-deploy.mjs` first failed with `vercel api 403`; running `vercel whoami` (identity `branden574`) refreshed the CLI token in place and the re-run succeeded, exactly as the script's own docstring says — a stale token, not a dead webhook. CI also passed on the pushed commit (`checks` green in 49s: typecheck, money-math unit, invariant, contract, secret scan, both builds, artifact validation).

**For Branden**: nothing urgent, nothing Level D, no push notification sent. Standing items unchanged — decide the two held majors (#6 vite/esbuild is the only one with any security motive, and per the scope evidence above it is dev-only, so it is a convenience call not a risk call; #3 is purely optional); TD-1 conditional hooks on the simulator; TD-5/TD-8 web bits (Sentry `release` + `version.json`, still entangled with your uncommitted WIP); SYSTEM_RISK_MAP open finding #1 (recipe_steps/ingredients revoke migration, Level C).

## Daily sweep 2026-08-16

**Production is green, but this sweep is the first with a real coverage hole: the Supabase MCP is unauthorized, so the read-only DB checks (items 5/5b) could not run. Everything else checked out, and I did not paper over the gap.**

**The gap, stated honestly.** Every Supabase MCP call — `execute_sql` and `get_advisors` alike — returned `Unauthorized. Please provide a valid access token to the MCP server via the --access-token flag or SUPABASE_ACCESS_TOKEN`. Sweeps 08-11 through 08-14 all used this same tool successfully, so it broke inside the last two days. I could **not** root-cause it: the MCP takes its credential from the local-only `.mcp.json`, and reading that file is permission-blocked for the unattended agent — which is the correct guardrail (it is a secrets-bearing file, CLAUDE.md hard-rule 2/7), so I did not attempt to work around it. `scripts/` offers no alternative read-only DB path (only `gen-apple-secret`, `safety-diff`, `secrets-check`, `verify-deploy`, `ops/*.sh`), and the tables in question (`pending_media_deletions` and friends) are deny-all to anon by design, so PostgREST is not a substitute either. **What `/health` independently covers, and what it does not:** `stuckVideoBacklog` = **0** is computed from `video_assets` itself over `provider=cloudflare` in `pending|uploading|processing` older than 15 min (`health.ts:15-27`) — a 15-minute threshold strictly subsumes the sweep's ">6h stuck" question, so that check is genuinely satisfied, not assumed. `cronAges` for all five jobs plus `problems:[]` proves no cron has been failing longer than its stale threshold (`health.ts:31-41`), which covers the "repeated failures" question for any sustained failure. **Genuinely unchecked today: `pending_media_deletions` with attempts≥10, and the Supabase security-advisor baseline.** Both are recorded as unverified rather than reported green. → **TD-21 opened** (P2), with an owner half (re-authorize the MCP — Level D, credentials) and a Level-B half that removes the single point of failure: a read-only `scripts/ops/db-state.mjs` running the three sweep queries off the API's existing service-role env, so a dead MCP token costs one signal instead of the whole DB check. Push notification sent, since only Branden can restore the credential.

**Production health** `/health` = `ok`, `problems:[]`, `stuckVideoBacklog` 0, `commit` `617bbea` = current HEAD (no rogue deploy), `paymentsKeyMode` **live**, cloudflare/moderation/email/Sentry configured, `push` ok; `getsizzle.app` = **200**. **Crons** — all five fresh against their real Vercel schedules (`apps/api/vercel.json`), which matters because two of the ages look alarming at a glance and are not: finalize-videos 40s and publish-scheduled 37s (both `* * * * *`); rollup-hashtag-trends 761s ≈12.7m (`*/15`); rollup-watch-ratios 1,549s ≈25.8m (runs `17,47` — a 30-min cadence, so ~26m is mid-cycle, not late); save-nudges 65,540s ≈18.2h (daily `0 21 * * *`, last success 2026-08-15 21:00 UTC, i.e. exactly on time). All inside the `CRON_STALE_SECONDS` thresholds; `/health` agrees with `problems:[]`.

**CI**: green. `gh run list --branch main --limit 5` again shows only scheduled `Uptime` rows — expected, per the 08-14 note, because `Uptime` fires every ~10-20 min while the last commits were docs-only; querying `ci.yml` directly confirms the workflow ran **success on the HEAD push `617bbea`** (47s) and on every sweep push before it. **0 non-success runs across the last 30 on `main`.** CodeQL last ran green. **CodeQL alerts: `code-scanning/alerts?state=open` is now empty (0)** — alert #10 (`js/stack-trace-exposure`, fixed in-lane during the 08-10 security sweep) has auto-resolved on a subsequent scan exactly as that entry predicted, so the fix is confirmed landed and the queue is clean.

**Dependency PRs**: unchanged, both correctly left for the owner and **no duplicate comment added** — the existing hold notes are still accurate. **#6** (esbuild 0.24.2→0.28.2 + vite 5.4.21→7.3.6) and **#3** (`@hono/node-server` 1.19.17→2.0.10) are both CI-green on all checks and both semver-**major** ⇒ owner lane per triage rule 3. No patch/minor dependency PRs exist to auto-merge.

**npm audit**: unchanged at 2 rows — esbuild MODERATE (GHSA-67mh-4wv8-2f99) and the vite tree HIGH (GHSA-fx2h-pf6j-xcff `server.fs.deny` bypass, plus MEDIUMs GHSA-4w7w-66w2-5vf9 path traversal and GHSA-v6wh-96g9-6wx3 launch-editor NTLMv2). **No new advisory since the 08-14 baseline**: all 7 open Dependabot alerts still carry GitHub's own `"scope": "development"`, and all were created 2026-08-08T20:54 — nothing new has landed. `npm audit --omit=dev` = **`found 0 vulnerabilities`**, so production scope is clean; every fix is semver-major and tracked in TD-18. Nothing patch-level to take in-lane. **Secret scanning**: `secret-scanning/alerts?state=open` = `[]` — no P0; TD-20 stays closed.

**Flag drift** — register re-checked against reality, and **one entry needed correcting rather than merely re-confirming**. TD-1 re-verified by running `npm run lint` (exactly **9 errors / 15 problems**, in the three files named ✓); TD-5's remaining half still real (`git show HEAD:apps/web/src/lib/sentry.ts` → **0** occurrences of `release`); TD-8's remainder still real (no `apps/web/public/version.json`; `docs/deploy.md` still has 2 Railway/Fly mentions); TD-17 still real (`.github/modernize/java-upgrade/` present). **TD-14 corrected (P2→P3):** it was opened because `@capacitor/cli` in `dependencies` "drags tar (critical advisory) into prod-scope audits" — that consequence no longer holds, since `npm audit --omit=dev` now reports zero vulnerabilities. The misplacement itself is still real (`apps/web/package.json:33`) and still worth fixing, because it would re-expose the same class the next time a transitive advisory lands — so the entry stays open with the security claim removed rather than being closed on a stale rationale. **TD-21 added** as above.

**Shipped**: docs only — `docs/engineering/technical-debt.md` (TD-14 correction, TD-21 added) and this log, **staged as explicit paths**. Pre-existing uncommitted web WIP (`App.tsx` / `lib/sentry.ts` / `main.tsx`, +61/−4 — stale-chunk auto-reload on `vite:preloadError`, a Marketing error boundary, local-dev Sentry suppression) left **untouched** per hard-rule 11. `npm run secrets:check` clean before push; `verify-deploy` run after push per the 08-14 correction (docs-only pushes **do** rebuild both projects).

**For Branden**: **one thing needs you — re-authorize the Supabase MCP** (verify/rotate the PAT in `.mcp.json`). Until then every daily sweep loses the parked-media-deletions check and the Supabase advisor baseline; nothing in production is affected, and no other item is blocked on it. Standing items unchanged: the two held majors (#6 vite/esbuild — dev-only scope, so a convenience call not a risk call; #3 purely optional); TD-1 conditional hooks on the simulator; TD-5/TD-8 web bits (Sentry `release` + `version.json`, still entangled with your uncommitted WIP); SYSTEM_RISK_MAP open finding #1 (recipe_steps/ingredients revoke migration, Level C).

## Daily sweep 2026-08-17

**Production is green; the sweep itself ran into two external blockers — the Supabase MCP is still unauthorized (day 2 of TD-21) and GitHub is in a critical partial outage that took the secret-scanning API check with it. Neither is a Sizzle fault, both are recorded as unchecked rather than papered over, and one cheaper fix for TD-21 was identified.**

**Production health** `/health` = `ok`, `problems:[]`, `stuckVideoBacklog` 0, `commit` `bf9a8c4` = current HEAD (no rogue deploy), `paymentsKeyMode` **live**, cloudflare/moderation/email/Sentry configured, `push` ok; `getsizzle.app` = **200** in 0.26s. **Crons** all five fresh against their real Vercel schedules: finalize-videos 55s and publish-scheduled 59s (`* * * * *`); rollup-hashtag-trends 738s ≈12.3m (`*/15`); rollup-watch-ratios 1,561s ≈26m (`17,47` — probe at 15:13 UTC, last run 14:47, so mid-cycle not late); save-nudges 65,544s ≈18.2h (daily `0 21`, last success 2026-08-16 21:00 UTC, on time). All inside `CRON_STALE_SECONDS`; `problems:[]` agrees. `stuckVideoBacklog` 0 again subsumes the ">6h non-ready" DB question at a stricter 15-min threshold, and fresh `cronAges` + empty `problems` again rules out any cron in sustained failure — so of the DB items only `pending_media_deletions` attempts≥10 and the advisor baseline are truly unchecked.

**GitHub outage — a real external event, verified not assumed.** The first `secret-scanning/alerts?state=open` call returned HTTP **503** ("No server is currently available"), as did every retry over ~15 minutes, `state=resolved`, and one `gh pr checks` GraphQL call. `githubstatus.com` explains it: incident "Incident with GitHub.com", impact **critical**, opened 2026-08-17T13:40Z and still `investigating` at 15:10Z — **API Requests, Issues, Pull Requests, Actions and Copilot in major_outage, Webhooks partial_outage, Pages degraded**. Plain REST (`repos/…`) and `dependabot/alerts` still answered intermittently, which is how the rest of this sweep got its data. **Consequence: the secret-scanning P0 check could not be completed today.** Partial local substitute run instead: a pattern grep of the tracked working tree for `sbp_…`, `sk_live_…`, `whsec_…`, PEM private-key headers and Supabase-style JWTs (excluding the gitignored `.env*`/`.mcp.json`) found **only** three hits, all in **untracked** built bundles (`apps/web/ios/App/App/public/…`, the simulator `build/…` product, `apps/web/android/…/assets/public/…` — `git ls-files` returns nothing for them) and the matched value decodes to the local **`supabase-demo` anon** JWT, a well-known public default, not a credential. So the current tree is clean; git *history* was not re-scanned (that is what the GitHub check adds). **Re-run the API check next sweep** — TD-20's closure evidence (alert #1 resolved `revoked` 08-10) is unaffected. Also flagged for today's push: with Webhooks degraded, the push→CI and push→Vercel triggers may not fire; handled below.

**Supabase DB state (items 5/5b) — still blocked, second consecutive sweep.** `mcp__supabase__execute_sql` returned the identical `Unauthorized … --access-token flag or SUPABASE_ACCESS_TOKEN` error, so the PAT in local-only `.mcp.json` remains dead/rotated (unreadable from the sweep by design — `Read(./.mcp.json)` is in the settings deny list, correctly). **New this sweep:** I looked for an independent path and found one that needs no new credential — the **claude.ai Supabase connector** (`mcp__claude_ai_Supabase__*`) is *connected* in the sweep session (absent from the auth-required server list) but its tools are not in `.claude/settings.json`'s allowlist, which only names the `.mcp.json`-backed `mcp__supabase__{execute_sql,list_tables,get_logs,get_advisors}`; calling `list_projects` failed with "requested permissions … not granted", exactly as expected. I did **not** add it myself — an unattended agent widening its own permission set is an owner call even for read-only intent — but recorded it as **TD-21 option (c)**, the cheapest real SPOF fix (a second read-only path authenticated by Branden's claude.ai account rather than the PAT). TD-21's option (b) (`scripts/ops/db-state.mjs` off the API's service-role env) was re-examined and *not* built: `Read(./apps/api/.env*)` is denied for the agent, so it could not be verified against a real DB unattended, and an unverified script does not count as a fallback (root-cause/verify rule); it also hands the sweep a service-role key where (c) does not. TD-21 updated accordingly, status "still Unauthorized 2026-08-17".

**CI**: `gh run list --branch main --limit 5` shows only scheduled `Uptime` rows (all `success`, incl. 15:10Z during the outage — expected, per the 08-14 note); `--workflow=ci.yml` confirms the `CI` workflow ran **success on the HEAD push `bf9a8c4`** (49s) and on every sweep push before it; **0 non-success across the last 30 runs on `main`.** `code-scanning/alerts?state=open` = **0** (answered before the outage deepened). **Dependency PRs**: unchanged — **#6** (esbuild 0.24.2→0.28.2 + vite 5.4.21→7.3.6) and **#3** (`@hono/node-server` 1.19.17→2.0.10), both **all 8 checks green** (CodeQL, GitGuardian, both Vercel builds, analyze, checks, dependency-review), both semver-**major** ⇒ owner lane per triage rule 3, both already carrying accurate hold comments; no duplicate comment added; no patch/minor dependency PRs exist to auto-merge. **npm audit**: unchanged at 2 rows — esbuild MODERATE (GHSA-67mh-4wv8-2f99) and the vite tree HIGH (GHSA-fx2h-pf6j-xcff + MEDIUMs GHSA-4w7w-66w2-5vf9, GHSA-v6wh-96g9-6wx3); every fix semver-major (`fixAvailable.isSemVerMajor: true`, vite→8.2.1, esbuild→0.28.2), tracked in TD-18; **`npm audit --omit=dev` = `found 0 vulnerabilities`**; all 7 open Dependabot alerts still `scope: development`, all created 2026-08-08T20:54 — nothing new has landed since the 08-14 baseline. Nothing patch-level to take in-lane.

**Flag drift** — register re-checked against reality, not trusted: TD-1 by running `npm run lint` (**9 errors / 15 problems**, in the three named files ✓); TD-5's remaining half still real (`git show HEAD:apps/web/src/lib/sentry.ts` → **0** `release`); TD-8's remainder still real (no `apps/web/public/version.json`; `docs/deploy.md` still 2 Railway/Fly mentions); TD-14 still real (`apps/web/package.json:33` `@capacitor/cli` in `dependencies`); TD-17 still real (`.github/modernize/java-upgrade/` present); TD-18's PR facts re-confirmed. **Nothing to close; TD-21 updated as above; nothing new opened** (the GitHub outage is an external incident, logged here, not debt).

**Shipped**: docs only — `docs/engineering/technical-debt.md` (TD-21) and this log, **staged as explicit paths**. Pre-existing uncommitted web WIP (`App.tsx` / `lib/sentry.ts` / `main.tsx`, +61/−4) left **untouched** per hard-rule 11. `npm run secrets:check` clean before push; `verify-deploy` run after push per the 08-14 correction — see the deploy note appended below if the outage interfered.

**For Branden**: **still one thing needs you — restore a DB path for the sweep** (TD-21): either re-authorize the `.mcp.json` Supabase PAT (option a), or — cheaper, no new credential — add the claude.ai Supabase connector's `list_projects`/`execute_sql`/`get_advisors` to the `permissions.allow` list in `.claude/settings.json` (option c). Until one lands, every sweep loses the parked-media-deletions check and the advisor baseline; production is unaffected. Also: **re-run the secret-scanning API check yourself if you're around before tomorrow's sweep** — GitHub's outage blocked it today (local tree grep was clean). Standing items unchanged: held majors #6/#3 (dev-only scope, convenience call); TD-1 conditional hooks on the simulator; TD-5/TD-8 web bits (entangled with your uncommitted WIP); SYSTEM_RISK_MAP open finding #1 (recipe_steps/ingredients revoke migration, Level C).

**Deploy note (post-push, same sweep):** the outage did interfere, mildly and only with timing. `git push` itself succeeded (`bf9a8c4..fb129b7`). `verify-deploy.mjs` first hit the known stale-token `vercel api 403` (fixed in place with `vercel whoami`, per the 08-14 note), then reported **frontend `sizzle-api` READY + `getsizzle.app` 200** but **API `sizzle` NOT FOUND after the 8-min window**. Before reaching for the CLI fallback I checked `vercel ls sizzle`: a production deployment for the push was **already Ready, 39s old**, and `/health` was serving `commit fb129b7` — the GitHub→Vercel webhook was **delayed past the verifier's timeout by the Webhooks partial outage, not lost**. Re-running `verify-deploy --api` then passed cleanly (READY, HTTP 200, `fb129b7 == HEAD ✓`, status `ok`). No CLI deploy was performed. CI also ran **green on `fb129b7`** (44s). Lesson for future sweeps during a GitHub incident: a NOT-FOUND from the verifier is a *timeout*, so list the project's deployments once before falling back to a CLI deploy — a redundant CLI deploy would otherwise stack a second production promotion on top of the webhook one. Secret-scanning API remained 503 through the last retry after the push.

## Security sweep 2026-08-17 (weekly adversarial)

**No new exploitable exposure; every production control re-verified intact; one new read-only tool shipped that let me check the live RLS boundary despite the dead DB credential; one P3 hardening nit filed (TD-22). Two coverage holes remain and are stated, not hidden: the Supabase security-advisor baseline and the `pending_media_deletions` query (both need TD-21).** Ran during GitHub's ongoing "Incident with GitHub.com" (impact critical, `API Requests` major_outage since 13:40Z) — every GitHub result below was retried until it returned real data.

**① Supabase security advisors — NOT RUN (blocked, day 3 of TD-21).** `mcp__supabase__get_advisors` → `Unauthorized … --access-token flag or SUPABASE_ACCESS_TOKEN` (identical to 08-16/08-17 daily). The claude.ai Supabase connector (TD-21 option c) is still not in the agent's allowlist — `list_projects` → "requested permissions … not granted", so it has not been enabled since this morning's daily sweep. Consequence: I cannot say today whether the advisor list still equals the 08-14 baseline (31 deny-all `rls_enabled_no_policy` INFOs + accepted `pg_trgm`), nor re-confirm `password_hibp_enabled`/the 10-char floor. Nothing suggests regression (no migration, no attended DDL, no auth-config change since 08-10), but "nothing suggests" is not verification. **Recorded as unverified.**

**② CodeQL + Dependabot + secret scanning — clean, fully triaged.** `code-scanning/alerts?state=open` = **0** (alert #10 stayed auto-resolved). Latest CodeQL analysis on `main` is 08-11 (`e48e667`, 9 results = the 9 dismissed FPs) — that is **by design**, not staleness: `codeql.yml` is path-filtered to `apps/**`/`packages/**` plus a weekly Tuesday 10:23 UTC schedule, and there has been **no `apps/**` commit since 08-10** (`f83f857`, the last sweep's own fix); next scheduled scan is tomorrow. `dependabot/alerts?state=open` = the same **7** dev-scope vite/esbuild alerts (#2–#8, all `scope: development`, all created 2026-08-08T20:54, all semver-major fixes) = TD-18; nothing new, nothing in-lane. **Secret scanning — the check yesterday's outage blocked is now complete:** first two calls returned 503, a third returned a bare **404** — which on this endpoint can also mean "secret scanning disabled", so I did not accept it as noise: `repos/Branden574/Sizzle` `security_and_analysis` shows `secret_scanning: enabled` and `secret_scanning_push_protection: enabled`; subsequent retries returned `state=open` = **`[]`** and `state=resolved` = alert #1 still `resolved` (2026-08-10T14:52:32Z). No P0; TD-20 stays closed. (Optional owner hardening seen in the same payload, not acted on: `secret_scanning_non_provider_patterns` and `secret_scanning_validity_checks` are `disabled`.)

**③ Route-auth audit — no new routes, no gaps.** Enumerated every `router.<verb>(` in all 17 route files (~150 routes). Every mutation carries `requireAuth` (+`requireNotBanned` where the action is social/financial); the only middleware-less routes are public by design and were each read: `GET /health`, `GET /cooks/handle-available`, `GET /monetize/config` (returns provider/fee/preset numbers only), the three `/r /u /b` SEO renderers (gate on `status='published'`, `!auto_hidden`, `!cook.private`, `!banned` → 404 otherwise), `POST /support/requests` (rate-limited public form), and the three webhooks. **Admin double gate intact:** `admin.use('*', requireAuth, requireNotBanned, requireAdmin)` then `admin.use('*', requireAdminUnlock)`, `EXEMPT` = exactly `/admin/unlock`, `/admin/passphrase`, `/admin/security-status` (`middleware/adminUnlock.ts:20`), the two bootstrap routes rate-limited 10/min; `POST /hashtags/:tag/moderate` still carries `requireAuth, requireAdmin, requireAdminUnlock` (TD-3 fix holds; invariant test asserts it). **Webhooks verify BEFORE acting, all three:** Stripe — raw body, `t`/`v1` parse, ±300s replay window, HMAC-SHA256 over `${t}.${raw}`, `timingSafeEqual` with length pre-check, 401 on mismatch, only then `JSON.parse` + switch (`monetize.ts:866-890`); RevenueCat — `timingSafeEqual` on `REVENUECAT_WEBHOOK_AUTH` before the body is read (`monetize.ts:796-800`); Cloudflare Stream — 403 unless configured+secret, ±300s window, HMAC over `${time}.${raw}`, `timingSafeEqual`, uid regex, then the same moderated finalizer as the cron (`uploads.ts:317-354`). Crons: `internal.use('*')` requires `Bearer CRON_SECRET`, fail-closed when unset. **One nit → TD-22 (P3):** that CRON compare is a plain `!==` while the three webhooks use `timingSafeEqual`; practically non-exploitable over Vercel jitter, and a mistake there silently 401s every cron, so it is filed for an attended Level-C fix rather than shipped blind. Known open finding #2 (`GET /recipes/:id/playback` non-premium branch) unchanged.

**④ Secret hygiene — clean.** `node scripts/secrets-check.mjs --all` = **clean (450 files, all tracked)**. Files ADDED in the last 8 days: only `.github/workflows/uptime.yml` (read: two public `curl`s to `/health` and `getsizzle.app`, no `secrets.*`, no tokens); files touched: `api/index.ts` (last sweep's fix), `watchdog.sh` (0 secret-shaped hits), docs and sweep prompts. No `.env`/plist/service-account committed; the only `.env*` in `git ls-files` is the intended `apps/web/.env.production.example`. Nothing tracked matches the CLAUDE.md forbidden list (`.mcp.json`, `.vercel/`, `.vercelignore`, Firebase plists).

**⑤ RLS spot-check — SQL path blocked, so I verified the boundary itself instead.** No migration has landed since the 08-10 sweep (last three are `20260729190000`, `20260806230000`, `20260808160000`, all live-verified then), so drift could only be out-of-band — precisely what the blocked `information_schema`/`pg_policies` query would catch. Rather than record "unchecked", I built and ran **`scripts/ops/anon-boundary-probe.mjs`**: it fetches the deployed bundle, extracts the *public* client key every browser already ships with (the prod bundle carries the new `sb_publishable_…` format, not a legacy JWT — the first draft failed on that and was fixed), and issues read-only `SELECT … limit 1` GETs as a logged-out visitor; the key is never printed, nothing is written. **Result against production: PASS.** All 22 sensitive tables yielded **0 rows** to anon — money/entitlement (`tips`, `iap_transactions`, `payouts`, `subscriptions`, `recipe_unlocks`, `product_purchases`), admin/moderation/ops (`admin_credentials`, `admin_sessions`, `moderation_log`, `reports`, `cron_runs`, `pending_media_deletions`), private data (`push_tokens`, `support_requests`, `messages`, `user_blocks`, `notifications`), and the paywalled content tables (`recipes`, `recipe_steps`, `recipe_ingredients`, `video_assets`, `comments`). Of those, 7 are `42501` at the grant level (`iap_transactions`, `product_purchases`, `admin_credentials`, `admin_sessions`, `cron_runs`, `pending_media_deletions`, `comments`) and 15 are "grant present, RLS returned nothing" — the documented deny-all/owner-only baseline, **not** a leak (I initially mis-labelled `200 rows=0` as a regression, then re-read the migrations: `20260701090000` made `recipes`/steps/ingredients/`video_assets` owner-only, and the deny-all INFO lints are exactly this shape). `profiles` column-scoping (`20260701100000` + `20260712000000`) holds live: `select=id,handle` → **200 rows=1** (positive control, so the denials are trustworthy) while all 12 PII columns probed (`phone`, `role`, `banned`, `banned_reason`, `country`, `region`, `stripe_account_id`, `tastes`, `notif_prefs`, `ban_appeal_text`, `delete_at`, `boost`) → **401 42501** column-permission-denied. `rpc/creator_earnings` → **42501** (the `20260808160000` revoke holds live). **Honest limits:** this sees only the *anon* role, so SYSTEM_RISK_MAP open findings #1 (`recipe_steps`/`recipe_ingredients` DML still granted to `authenticated` → moderation bypass on owner edit) and #4 (`comment_likes`/`reports`) are **neither confirmed nor refuted today** — they were live-verified unchanged on 08-10 and nothing has touched grants since; and `rows=0` cannot distinguish RLS-holding from an empty table (`payouts` is known-empty, so its pass is the weak kind). Recorded against TD-7 (partial cousin, entry stays open) and TD-21 (partial mitigation, does not cover advisors or parked deletions).

**⑥ Exposure drift — clean.** `/health` = `ok`, `problems:[]`, structure-only (booleans + modes + `commit f89790a` = HEAD, `paymentsKeyMode: live`), **0** secret-shaped string values (`sbp_|sk_|whsec_|eyJ|BEGIN|40+ chars` → no hits). Unauthenticated probes against production: `/admin/security-status` **401**, `/admin/log` **401**, `/admin/stats` **401**, `/admin/users` **401**, `/internal/finalize-videos` **401**, `POST /hashtags/x/moderate` **401**, `POST /monetize/webhook/stripe` (no sig) **401**, `POST /monetize/webhook/revenuecat` (no auth) **401**, `POST /uploads/webhook/cloudflare-stream` **403** ("Webhook not enabled" — `CLOUDFLARE_STREAM_WEBHOOK_SECRET` unset in prod; consistent with the code's "we skip Stream webhooks, the cron drives assets to ready" design and fail-closed).

**Shipped this sweep** (explicit paths only; pre-existing uncommitted web WIP `App.tsx`/`lib/sentry.ts`/`main.tsx` untouched per rule 11): `scripts/ops/anon-boundary-probe.mjs` (new, Level A — read-only ops tooling, no prod effect, no credential, ~40 GETs; exits non-zero on any anon-visible row/column), `docs/engineering/technical-debt.md` (TD-22 opened P3; TD-7 and TD-21 annotated), this log. No control was weakened; no dismissals were needed (CodeQL queue already empty). **Not shipped, deliberately:** TD-22 (attended Level C), SYSTEM_RISK_MAP edits (guardrail doc, Level C — the anon-boundary result confirms existing invariants rather than adding one; fold it in on your next attended pass if you want the "Last verified" date to move). `npm run secrets:check` before push; `verify-deploy` after (docs/scripts push still rebuilds both projects per the 08-14 correction; expect a webhook delay while GitHub is degraded — list deployments before any CLI fallback, per this morning's deploy note).

**For Branden — nothing P0/P1, no push notification sent.** (1) **TD-21 is now three sweeps old**: enable option (c) (allowlist the claude.ai Supabase connector's `list_projects`/`execute_sql`/`get_advisors`) or re-authorize the `.mcp.json` PAT — until then the advisor baseline and `pending_media_deletions` are unverified every day, and the weekly RLS check is anon-side only. (2) Consider wiring `node scripts/ops/anon-boundary-probe.mjs` into `scripts/ops/security-sweep-prompt.md` item 5 (I did not edit my own sweep prompt unattended). (3) TD-22 whenever you next touch `internal.ts` attended. Standing items unchanged: SYSTEM_RISK_MAP open finding #1 (recipe_steps/ingredients revoke migration, Level C — still the one meaningful open security item), held majors #6/#3, TD-1/TD-5/TD-8.

## Incident 2026-08-17 15:53 PDT — watchdog summon: API + frontend "unreachable" → FALSE ALARM (transient local DNS, second occurrence)

**What fired.** Watchdog (`scripts/ops/watchdog.sh`) summoned an unattended session at 2026-08-17 15:53:22 PDT (22:53:22 UTC) reporting `API unreachable (HTTP 000)` and `Frontend https://getsizzle.app HTTP 000000`. Raw /health capture: `curl: (6) Could not resolve host: sizzle-chi.vercel.app`. **Identical fingerprint to the 2026-08-10 07:47 false alarm** — this is now 2 of the 3 real (non-self-test) summons to date.

**Root cause (verified against an independent vantage point).** A transient **DNS-resolver failure on the watchdog host (this Mac)** at the moment of the 15:53 launchd tick — production was never down. This time the conclusion does not rest on inference alone: the **off-Mac GitHub Actions `Uptime` workflow** (`.github/workflows/uptime.yml`, ubuntu runner, probes the same two URLs) ran at **22:52:56 UTC** and logged `Probe API /health … attempt 1: HTTP 200` at **22:53:02Z** and `Probe frontend getsizzle.app … attempt 1: HTTP 200` at **22:53:02Z** (run 32078005036) — i.e. **both surfaces returned 200 from GitHub's network 20 seconds before the local watchdog reported them unresolvable.** Every Uptime run in the surrounding two hours (20:37Z … 22:52Z, 12 runs) is `success`. Supporting evidence, same as 08-10: `curl (6)` is a resolver failure, not an origin failure (a Vercel origin outage still resolves and returns 5xx from the edge); two independently-deployed Vercel projects (`sizzle` = API, `sizzle-api` = frontend) "failing" in the same second at the connection layer is the fingerprint of one shared client-side network drop; and `watchdog.sh` itself anticipates the class ("transient network error e.g. right after Mac wake"). Not a deploy: both projects' latest production deployments are `● Ready` and ~7 h old (`vercel ls sizzle` / `vercel ls sizzle-api`), so nothing shipped near the alert.

**Production health, spaced confirmations (anti-flap, all HTTP 200):** 15:54 PDT api=200 (0.80s) web=200 (1.07s); **15:56:51** `/health` → `{"status":"ok","problems":[],"commit":"a3cd986",…}` — `commit` = current HEAD, `stuckVideoBacklog` 0, `cloudflareConfigured` true, `push` ok, `paymentsKeyMode` live, email/Sentry/moderation configured, all five `cronAges` fresh (finalize-videos 18s, publish-scheduled 38s, rollup-hashtag-trends 661s, rollup-watch-ratios 571s, save-nudges 6990s ≈1.9h — expected for the daily 21:00 UTC job), DNS lookup 0.004s (resolver healthy again); web=200 remote 216.150.16.193 dns 0.003s; **15:58** api=200 web=200 dns 0.003s/0.003s. No flapping.

**What I did.** Diagnosed only — no code change, no deploy, no rollback (all correctly unwarranted). Filed **TD-23 (P3)** for the watchdog hardening that the 08-10 entry proposed and that has now paid its second false summon: retry/confirm before summoning on `000`/DNS-resolution failures. Not shipped unattended, deliberately: the incident prompt's false-alarm rule is "log with evidence and stop", and the watchdog is the ops control plane — launchd runs the repo file directly, so an edit is live at the next 5-min tick with no CI or deploy gate, and a mistake there could suppress a *real* summon. Sandbox note for future summons: `~/Library/Logs/sizzle-watchdog.log` and `pmset -g log` are outside the unattended session's read scope, so wake/resolver evidence has to come from external vantage points (the Uptime workflow logs, as here). Pre-existing uncommitted web WIP (`App.tsx`/`lib/sentry.ts`/`main.tsx`) untouched per rule 11; only this log and `technical-debt.md` staged.

**Still open / for Branden.** Nothing urgent, nothing Level D. Approve TD-23 when convenient (small `watchdog.sh` change: on `000`, `sleep 20` and re-probe once — or require the GitHub Uptime run to also be failing — before declaring a problem; test via `SIZZLE_WATCHDOG_TEST_URL` + `SIZZLE_WATCHDOG_TEST_PROMPT`). Each false summon costs a full unattended session; the true-outage path is already covered off-Mac by the Uptime workflow, so a 20-second delay in local detection is cheap.

## Daily sweep 2026-08-18

**All green; nothing shipped but this log and a one-line TD-21 update. The only sweep gap is the same one as the last three days — no DB path (TD-21, day 4) — and it is recorded as unchecked, with the credential-free anon-boundary probe run as the partial substitute.**

**Production health** `/health` = `ok`, `problems:[]`, `stuckVideoBacklog` 0, `commit` `ab940a5` = current HEAD (no rogue deploy), `paymentsKeyMode` **live**, cloudflare/moderation/email/Sentry configured, `push` ok; **200 in 0.41s**. `getsizzle.app` = **200** in 0.14s. **Crons** all five fresh against their real schedules (probe 15:13 UTC): finalize-videos 50s and publish-scheduled 47s (`* * * * *`); rollup-hashtag-trends 733s ≈12.2m (`*/15`); rollup-watch-ratios 1,557s ≈26m (`17,47`, last run 14:47 → mid-cycle, not late); save-nudges 65,588s ≈18.2h (daily `0 21`, last success 2026-08-17 21:00 UTC, on time). `problems:[]` agrees; `stuckVideoBacklog` 0 subsumes the ">6h non-ready" DB question at its stricter 15-min threshold, and fresh `cronAges` + empty `problems` rules out sustained cron failure — so of the DB items only `pending_media_deletions` attempts≥10 and the advisor baseline are truly unchecked.

**CI**: `gh run list --branch main` is all scheduled `Uptime` `success` rows plus today's scheduled **CodeQL success** (10:32Z — the Tuesday scan the security sweep said was due; `code-scanning/alerts?state=open` = **0**, so it found nothing new); `--workflow=ci.yml` shows **success on the HEAD push `ab940a5`** (55s) and on the two pushes before it. **Secret scanning**: `secret-scanning/alerts?state=open` = **`[]`** on the first call (GitHub healthy again — yesterday's outage-blocked check is now double-confirmed). **Dependency PRs**: unchanged — **#6** (esbuild 0.24.2→0.28.2 + vite bump) and **#3** (`@hono/node-server` 1→2), all checks green, both semver-major ⇒ owner lane, both already carrying accurate hold comments; no duplicate comment added; no patch/minor PRs to auto-merge. **npm audit**: unchanged at 2 rows — esbuild MODERATE (GHSA-67mh-4wv8-2f99) + vite HIGH (GHSA-4w7w-66w2-5vf9 path traversal, launch-editor NTLM); every fix `isSemVerMajor: true` (vite→8.2.1, esbuild→0.28.2) = TD-18; **`npm audit --omit=dev` = `found 0 vulnerabilities`**; `dependabot/alerts?state=open` = the same 7 dev-scope alerts (#2–#8, all created 2026-08-08T20:54). Nothing patch-level to take in-lane.

**Supabase DB state (items 5/5b) — still blocked, day 4 of TD-21.** `mcp__supabase__execute_sql` → identical `Unauthorized … --access-token flag or SUPABASE_ACCESS_TOKEN`; the claude.ai Supabase connector (`mcp__claude_ai_Supabase__list_projects`, TD-21 option c) → still "requested permissions … not granted", so it has not been allowlisted since 08-17. Not self-granted (owner call). Partial substitute: **`node scripts/ops/anon-boundary-probe.mjs` against production → OK** — 22 sensitive tables yield 0 rows to anon, all 12 `profiles` PII columns `401 42501`, `rpc/creator_earnings` execute denied, positive control (`profiles` id/handle) readable. Read-only, no credential, no writes. Advisor baseline and `pending_media_deletions` attempts≥10 remain **unverified**. No migration has landed since `20260808160000` (last three unchanged), so drift could only be out-of-band.

**Flag drift** — register re-checked against reality: TD-1 `npm run lint` still **9 errors / 15 problems** in the same three files; TD-5's remaining half still real (`git show HEAD:apps/web/src/lib/sentry.ts` → 0 `release`; the uncommitted WIP on that file adds localhost suppression, not a release tag); TD-8's remainder still real (no `apps/web/public/version.json`; `docs/deploy.md` still 2 Railway mentions); TD-14 still real (`apps/web/package.json:33`); TD-17 still real (`.github/modernize/java-upgrade/` present); TD-22 still real (`internal.ts:41` plain compare); TD-18 PR facts unchanged. **Nothing to close; TD-21 status line updated; nothing new opened.**

**Shipped**: docs only — `docs/engineering/technical-debt.md` (TD-21 day-4 note) and this log, staged as explicit paths. Pre-existing uncommitted web WIP (`App.tsx` / `lib/sentry.ts` / `main.tsx`, +61/−4: `vite:preloadError` reload + marketing ErrorBoundary + Sentry localhost suppression) left **untouched** per hard-rule 11. `npm run secrets:check` before push; `verify-deploy` after.

**For Branden**: nothing P0/P1, no push notification sent. **TD-21 is four sweeps old** — either re-authorize the `.mcp.json` Supabase PAT (option a) or allowlist the claude.ai Supabase connector's `list_projects`/`execute_sql`/`get_advisors` in `.claude/settings.json` (option c); until then every sweep loses the parked-deletions check and the advisor baseline. Standing items unchanged: held majors #6/#3 (dev-only scope, convenience call); TD-1 conditional hooks on the simulator; TD-5/TD-8 web bits (entangled with your uncommitted WIP — worth committing that WIP, it looks complete); TD-22/TD-23 small attended fixes; SYSTEM_RISK_MAP open finding #1 (recipe_steps/ingredients revoke migration, Level C).

## Daily sweep 2026-08-19

**All green. One real in-lane fix shipped — `/health` now reports `parkedMediaDeletions`, which permanently closes half of the TD-21 blind spot (day 5 with no DB path) without adding any credential. Nothing else needed changing; nothing needs Branden urgently.**

**Production health** `/health` = `ok`, `problems:[]`, `stuckVideoBacklog` 0, `commit` `9ff49d0` = HEAD at probe time (no rogue deploy), `paymentsKeyMode` **live**, cloudflare/moderation/email/Sentry configured, `push` ok. `getsizzle.app` = **200**. **Crons** all five fresh against their real Vercel schedules (probe 15:13 UTC): finalize-videos 15s and publish-scheduled 46s (`* * * * *`); rollup-hashtag-trends 795s ≈13.3m (`*/15`, last 15:00); rollup-watch-ratios 1,549s ≈25.8m (`17,47` — last run 14:47, next 15:17, so mid-cycle not late); save-nudges 65,562s ≈18.2h (daily `0 21`, last success 2026-08-18 21:00 UTC, on time). All inside `CRON_STALE_SECONDS`; `problems:[]` agrees.

**CI**: `--workflow=ci.yml` shows **success on the HEAD push `9ff49d0`** (1m0s) and on the three pushes before it; `gh run list --branch main` is otherwise all scheduled `Uptime` `success` rows. CodeQL last ran **success 08-18 10:32Z** (weekly Tuesday schedule + `apps/**`/`packages/**` path filter; no `apps/**` commit since, so no run was due today — my API change today will trigger one on push). **`code-scanning/alerts?state=open` = 0. `secret-scanning/alerts?state=open` = `[]`** — no P0, TD-20 stays closed. **Dependency PRs**: unchanged — **#6** (esbuild 0.24.2→0.28.2 + vite) and **#3** (`@hono/node-server` 1→2), all checks green, both semver-**major** ⇒ owner lane per triage rule 3, both already carrying accurate hold comments (08-09 / 08-13); **no duplicate comment added** — nothing about either has changed and a daily re-comment is noise. No patch/minor dependency PRs exist to auto-merge. **npm audit**: unchanged at 2 rows — esbuild MODERATE (GHSA-67mh-4wv8-2f99) + vite HIGH (GHSA-fx2h-pf6j-xcff, plus GHSA-4w7w-66w2-5vf9 path traversal and GHSA-v6wh-96g9-6wx3 launch-editor NTLM); every fix `isSemVerMajor: true` (vite→8.2.1, esbuild→0.28.2) = TD-18. **`npm audit --omit=dev` = `found 0 vulnerabilities`**; `dependabot/alerts?state=open` = the same **7** alerts (#2–#8), all `scope: development`, all created 2026-08-08T20:54 — **no new advisory since the 08-14 baseline**. Nothing patch-level to take in-lane.

**Supabase DB state (items 5/5b) — still blocked, day 5 of TD-21, but the gap is now half-closed for good.** `mcp__supabase__execute_sql` → identical `Unauthorized … --access-token flag or SUPABASE_ACCESS_TOKEN`; the claude.ai connector (option c) → still "requested permissions … not granted", so it has not been allowlisted since 08-17. Not self-granted (owner call). Rather than record the same two items unchecked for a fifth day, I closed the one that could be closed in-lane: **`/health` now returns `parkedMediaDeletions`** — a service-role `count(*)` of `pending_media_deletions where attempts >= 10` (`apps/api/src/routes/health.ts`), mirroring the existing `stuckVideoBacklog` probe exactly. The sweep already reads `/health`, so this needs no DB path and, unlike TD-21 option (b), **puts no service-role key in the sweep's hands**. Deliberately **report-only**: it is *not* added to `problems`, because a parked row is a reconciliation task, not downtime, and 503-ing the API (and every uptime monitor with it) over one undeleted object would be a self-inflicted outage. Regression-guarded by a new invariant test ("parked media deletions stay visible on /health") which asserts both halves of the wiring **and** that it never enters `problems`; verified the test fails against `HEAD:health.ts` before the change, so it is a real guard, not a tautology. Correction to the earlier framing in TD-21/prior logs: this condition was never *unalarmed* — `drainMediaDeletions` already calls `captureException` every run while any row is parked (`internal.ts`) — it was invisible **to the sweep**, which is what this fixes. **Remaining genuinely unchecked: the Supabase security-advisor baseline (item 5b) only.** Partial substitute re-run as usual: **`node scripts/ops/anon-boundary-probe.mjs` → OK** (22 sensitive tables yield 0 rows to anon, all 12 `profiles` PII columns `401 42501`, `rpc/creator_earnings` execute denied, positive control readable). No migration has landed since `20260808160000`, so advisor drift could only be out-of-band.

**Flag drift** — every open entry re-verified against reality, nothing closed on a stale rationale: TD-1 `npm run lint` still **9 errors / 15 problems** in the same three files; TD-5's remaining half still real (`apps/web/src/lib/sentry.ts` → **0** occurrences of `release`); TD-8's remainder still real (no `apps/web/public/version.json`; `docs/deploy.md` still 2 Railway/Fly mentions); TD-14 still real (`apps/web/package.json:33`); TD-17 still real (`.github/modernize/java-upgrade/` present); TD-22 still real (`internal.ts:42` plain `!==` compare); TD-18 PR facts unchanged. **Nothing to close; TD-21 updated with the day-5 status and the half-fix above; nothing new opened.**

**Shipped**: `apps/api/src/routes/health.ts` (the gauge), `tests/invariants/security-invariants.test.mjs` (the guard), `docs/engineering/technical-debt.md` (TD-21), and this log — **staged as explicit paths**. Gates: `npm run typecheck` clean both workspaces · `npm test` green · `npm run test:invariants` **14/14** incl. the new test · `npm run safety:diff` → "No security-sensitive paths touched. Minimum lane: A/B" · `npm run secrets:check` clean before push · `node scripts/verify-deploy.mjs` after. Pre-existing uncommitted web WIP (`App.tsx` / `lib/sentry.ts` / `main.tsx`, +61/−4: `vite:preloadError` reload + marketing ErrorBoundary + Sentry localhost suppression) left **untouched** per hard-rule 11.

**For Branden**: nothing P0/P1, no push notification sent. **TD-21 is five sweeps old but now costs less** — after today only the **Supabase advisor baseline** is unverified each day (parked deletions are covered by `/health` from now on). Still worth restoring a DB path: re-authorize the `.mcp.json` PAT (option a) or allowlist the claude.ai connector's `list_projects`/`execute_sql`/`get_advisors` in `.claude/settings.json` (option c). Standing items unchanged: held majors #6/#3 (dev-only scope — a convenience call, not a risk call); TD-1 conditional hooks on the simulator; TD-5/TD-8 web bits (still entangled with your uncommitted WIP — it looks complete and is worth committing); TD-22/TD-23 small attended fixes; SYSTEM_RISK_MAP open finding #1 (recipe_steps/ingredients revoke migration, Level C — still the one meaningful open security item).

**Deploy verification (addendum, same sweep).** The push promoted correctly on both projects, but `node scripts/verify-deploy.mjs` **failed on its first run** with an unhandled `Error: vercel api 403` before checking anything — the script reads the raw Vercel CLI token from `~/Library/Application Support/com.vercel.cli/auth.json` and that token had gone stale. Not assumed to be noise and not worked around: `vercel whoami` returned `branden574` (refreshing the token in place, exactly as the script's own docstring prescribes), and the clean re-run reported **`sizzle` (API) READY, probe HTTP 200, serving commit `be449a0` == HEAD ✓, health `ok`** and **`sizzle-api` (frontend) READY, `getsizzle.app` HTTP 200**. Independently corroborated before that re-run by polling `/health` directly, which flipped `9ff49d0` → **`be449a0`** on the second attempt. **The new gauge is live and already answering the sweep's own question: `parkedMediaDeletions: 0`** — so item 5's parked-deletions check is *verified green today*, not recorded as unchecked for the first time in five sweeps. Filed **TD-24 (P3)**: `verify-deploy.mjs` should catch the 403, run `vercel whoami` once and retry rather than dying on a stack trace, since an unattended sweep stopping there would report a perfectly good deploy as unverified.

## Daily sweep 2026-08-20

**All green. Shipped the TD-24 fix — and it proved itself within the hour: the stale-token 403 recurred on its very first run and the new code recovered automatically instead of dying. One new entry filed (TD-25): `renovate.json` is dead config and nothing proposes routine dependency updates. Nothing needs Branden urgently.**

**Production health.** `/health` = `ok`, `problems:[]`, `stuckVideoBacklog` **0**, `parkedMediaDeletions` **0**, `commit` `9b0086f` = HEAD at probe time (no rogue deploy), `paymentsKeyMode` **live**, cloudflare/moderation/email/Sentry configured, `push` ok; HTTP **200**. `getsizzle.app` = **200** in 0.14s. **Crons** all five fresh against their real Vercel schedules (probe 15:13 UTC) and all inside `CRON_STALE_SECONDS`: finalize-videos 42s and publish-scheduled 20s (`* * * * *`, limit 600); rollup-hashtag-trends 763s ≈12.7m (`*/15`, limit 7200); rollup-watch-ratios 1,581s ≈26.4m (`17,47` — last run 14:47, next 15:17, mid-cycle not late, limit 7200); save-nudges 65,579s ≈18.2h (daily `0 21`, last success 2026-08-19 21:00 UTC, on time, limit 93,600). `problems:[]` agrees.

**CI / scanning — clean.** `gh run list --branch main --limit 5` is all scheduled `Uptime` `success`; `--workflow=CI` shows **success on the HEAD push `9b0086f`** (39s) and on the three pushes before it; CodeQL last ran **success 08-19** on the sweep's own push (weekly Tuesday schedule + `apps/**`/`packages/**` path filter, so none was due today). **`code-scanning/alerts?state=open` = 0. `secret-scanning/alerts?state=open` = `[]`** — no P0, TD-20 stays closed. The one non-green thing in the whole run history is the **"Dependabot Updates" workflow, 2 failures dated 2026-08-10** — not ignored: pulled the log, it is `dependency_file_not_supported {"dependency-name":"vite"}` on a security-update job. Coverage was **not** lost (PR #6 already carries the vite+esbuild bump) and it has not recurred, but it fed TD-25 below.

**Dependency PRs — unchanged, both correctly held.** **#6** (esbuild 0.24.2→0.28.2 grouped with vite) and **#3** (`@hono/node-server` 1.19.17→2.0.10): all 8 checks green on both, both semver-**major** ⇒ owner lane per triage rule 3, and both already carry accurate hold comments (08-09 / 08-13 respectively). **No duplicate comment added** — nothing about either has changed and a daily re-comment is noise. No patch/minor PRs exist to auto-merge (see TD-25 — structurally, none ever will).

**npm audit — no new advisories.** Unchanged at 2 rows: esbuild **MODERATE** (GHSA-67mh-4wv8-2f99, dev-server SSRF) + vite **HIGH** (GHSA-4w7w-66w2-5vf9 path traversal, GHSA-fx2h-pf6j-xcff `server.fs.deny` bypass, GHSA-v6wh-96g9-6wx3 launch-editor NTLM). Every fix is `isSemVerMajor: true` (vite→8.2.2, esbuild→0.28.2) = **TD-18**, so nothing is patch-level and nothing is in-lane. **`npm audit --omit=dev` = `found 0 vulnerabilities`** — prod scope clean. `dependabot/alerts?state=open` = the same **7** alerts (#2–#8), every one `scope: development`, all created 2026-08-08T20:54 — **no new advisory since the 08-14 baseline**.

**Supabase DB state (items 5/5b) — day 6 of TD-21, but item 5 is now fully answered without a DB path.** `mcp__supabase__execute_sql` → identical `Unauthorized … --access-token flag or SUPABASE_ACCESS_TOKEN`; the claude.ai connector (option c) → still not allowlisted, so it has not been granted since 08-17. Not self-granted (owner call). What changed: yesterday's `/health` gauge did its job on its first working day — **`parkedMediaDeletions: 0`** answers the `attempts >= 10` question directly, **`stuckVideoBacklog: 0`** subsumes ">6h non-ready" at its stricter 15-min threshold, and five fresh `cronAges` with `problems:[]` rules out repeated cron failure. So all three of item 5's sub-checks are **verified green**, not recorded as unchecked. Partial substitute re-run as usual: **`node scripts/ops/anon-boundary-probe.mjs` → OK** (22 sensitive tables yield 0 rows to anon, all 12 `profiles` PII columns `401 42501`, `rpc/creator_earnings` execute denied, positive control readable). **Genuinely unchecked: the Supabase security-advisor baseline (item 5b) only** — now the sole reason TD-21 stays open. No migration has landed since `20260808160000`, so advisor drift could only be out-of-band.

**Shipped — TD-24 closed (Level B, release tooling).** `scripts/verify-deploy.mjs` no longer dies on a stale Vercel CLI token. A new `createVercelClient()` owns the token and, on **401 or 403** (403 is what was actually observed; 401 is the same class), runs `vercel whoami` — which re-mints `auth.json` in place — re-reads the token and retries **once**. The refresh is capped at once per process, deliberately: a genuinely dead token must fail fast rather than spin `whoami` on every 10s poll tick. Every non-auth status stays a hard error, and a failed `whoami` raises a `vercel login` hint instead of an opaque spawn error. **The fix validated itself on a real surface:** the smoke run printed `vercel api 403 — token looks stale, refreshing…` on its first API call and then completed both projects — `sizzle` (API) READY, probe **200**, serving commit `9b0086f` **== HEAD ✓**, health `ok`; `sizzle-api` (frontend) READY, `getsizzle.app` **200**. The old code would have thrown before checking either project, so this recurs daily rather than being a one-off. Regression-guarded by **5 new tests** in `tests/invariants/ops-tooling.test.mjs` (refresh-and-retry uses the RE-READ token · 401 is the same class · at-most-once, no spin · 404/429/500 never trigger a refresh · failed-refresh surfaces the login hint). Confirmed a real guard, not a tautology: `HEAD:scripts/verify-deploy.mjs` has **0** exports and self-executes its poll loop at import, so these tests cannot even load against the old file. Picked up by CI automatically via the existing `tests/invariants/*.test.mjs` glob — **no `.github/workflows/**` edit needed**, which kept the diff out of Level C.

**Flag drift** — every open entry re-verified against reality; nothing closed on a stale rationale. **Closed: TD-24** (above). **Opened: TD-25 (P3)** — `renovate.json` has been dead config since the 08-06 audit wrote it: Renovate has **never opened a PR** (all 6 PRs in repo history are `dependabot/*` or the original `codex/*`; 0 match `renovate`), because the Renovate GitHub App — owner action #2 in `autonomy-audit.md` — was never installed. There is also **no `.github/dependabot.yml`**, so Dependabot runs security-updates-only. Net effect worth naming: **the sweep's own triage rule 3 has nothing to act on and structurally never will** — routine patch/minor upgrades are never proposed, so the tree drifts until something becomes a security problem. Owner call either way (Renovate app install = Level D; `.github/**` config = Level C), so filed rather than shipped. Still real, unchanged: TD-1 (`npm run lint` still **9 errors / 15 problems**), TD-5's remaining half (`apps/web/src/lib/sentry.ts` → **0** occurrences of `release`), TD-8's remainder (no `apps/web/public/version.json`; `docs/deploy.md` still cites Railway/Fly), TD-14 (`apps/web/package.json:33`), TD-17 (`.github/modernize/java-upgrade/` present), TD-22 (`internal.ts:42` plain `!==`), TD-23 (`watchdog.sh:42` still summons on a bare `000`), TD-18 PR facts.

**Gates**: `npm run typecheck` clean both workspaces · `npm test` green (money math 7 · invariants **19/19** incl. the 5 new · controls 6) · `npm run safety:diff` → "RELEASE TOOLING DETECTED → minimum **Level B**" (no security-sensitive path) · `npm run secrets:check` clean before push · `node scripts/verify-deploy.mjs` after. **Staged as explicit paths only.** Pre-existing uncommitted web WIP (`App.tsx` / `lib/sentry.ts` / `main.tsx`, +61/−4: `vite:preloadError` reload + marketing `ErrorBoundary` + Sentry localhost suppression) left **untouched** per hard-rule 11 — it is now 4 sweeps old.

**For Branden**: nothing P0/P1, no push notification sent. (1) **TD-21, day 6** — the daily cost is now down to one item (the Supabase advisor baseline), but restoring a DB path is still worth it: re-authorize the `.mcp.json` PAT (option a) or allowlist the claude.ai connector's `list_projects`/`execute_sql`/`get_advisors` in `.claude/settings.json` (option c). (2) **TD-25 is new and is a real hole in the automation story** — decide Renovate-app vs `.github/dependabot.yml`, then delete the loser's config; until then the repo gets no routine dependency maintenance at all. (3) Your uncommitted web WIP looks complete and is entangled with TD-5/TD-8 — worth committing. Standing items unchanged: held majors #6/#3 (dev-only scope — a convenience call, not a risk call); TD-1 conditional hooks on the simulator; TD-22/TD-23 small attended fixes; SYSTEM_RISK_MAP open finding #1 (recipe_steps/ingredients revoke migration, Level C — still the one meaningful open security item).

## Incident 2026-08-21 06:48 PDT — watchdog summon: API + frontend "unreachable" → FALSE ALARM (host-side network blip, third occurrence)

**What fired.** Watchdog (`scripts/ops/watchdog.sh`) summoned an unattended session at 2026-08-21 06:48:29 PDT (13:48:29 UTC) reporting `API unreachable (HTTP 000)` and `Frontend https://getsizzle.app HTTP 000000`. Raw /health capture: `curl: (7) Failed to connect to sizzle-chi.vercel.app port 443 after 139 ms: Couldn't connect to server`. Same class as the 2026-08-10 07:47 and 2026-08-17 15:53 false alarms — **3 of the 4 real (non-self-test) summons to date.**

**Root cause (verified from an independent vantage point).** A transient **host-side network failure on the watchdog Mac** at the moment of the 06:48 launchd tick. Production was never down. **Note the variant:** the prior two were resolver failures (`curl: (6) Could not resolve host`); this one is **`curl: (7)` — DNS resolved fine and the TCP connect failed in 139 ms.** So the correct name for this class is "host-side network blip", not "DNS blip". Evidence, in order of weight: (1) **Off-Mac probes bracket the event green** — the scheduled GitHub `Uptime` run at **13:32:37Z** (16 min before) was `success`, and a `workflow_dispatch` run I fired at **13:51:32Z** (3 min after) was `success`, both probing the same two URLs from GitHub's network; every Uptime run back to 11:24Z is `success`. (2) **Nothing shipped** — `vercel ls sizzle` and `vercel ls sizzle-api` both show their latest production deployment `● Ready` and **22 h old**, so no deploy coincided with the alert and rollback was never a candidate. (3) **A 139 ms connect refusal on two *different* Vercel anycast IPs in the same second** (API `216.198.79.67`, frontend `216.150.16.129` — separately deployed projects on separate prefixes) is the fingerprint of one shared client-side drop; a real Vercel origin outage resolves and answers 5xx from the edge rather than refusing TCP instantly. (4) `watchdog.sh` already anticipates this class in its own retry comment ("transient network error e.g. right after Mac wake").

**Production health, spaced confirmations (anti-flap, all HTTP 200).** 06:48:54 PDT (25 s after the "outage") api=**200** conn=0.038s ip=216.198.79.67, web=**200** conn=0.132s ip=216.150.16.129 — i.e. recovered untouched. ~06:50 api=**200** conn=0.026s, web=**200** conn=0.021s. 06:52:29 `/health` → `{"status":"ok","problems":[],"commit":"bf9005f",…}` with `commit` = current HEAD (no rogue deploy), `stuckVideoBacklog` **0**, `parkedMediaDeletions` **0**, `cloudflareConfigured` true, `push` ok, `paymentsKeyMode` **live**, moderation/email/Sentry configured, dns 0.003s (resolver healthy). **Crons** all five inside their limits: finalize-videos 55 s, publish-scheduled 26 s, rollup-hashtag-trends 434 s, rollup-watch-ratios 308 s, save-nudges 60,582 s ≈16.8 h — checked against `health.ts`, whose `save-nudges` limit is **93,600 s** (daily 21:00 UTC job, on time), so `problems:[]` is honest and not masking a stale cron. **Beyond `/health`:** the real `/feed/for-you?limit=3` endpoint served **200** in 0.42 s, confirming actual request-path function, not just liveness. **CI** green — `--workflow CI --branch main` latest completed is `success` on HEAD `bf9005f`. No flapping across ~4 min and three spaced checks.

**What I did.** Diagnosed only — **no code change, no deploy, no rollback**, all correctly unwarranted (nothing was broken and nothing had shipped). Updated **TD-23** to record the third occurrence, the new `curl (7)` variant, and one useful confirmation for whoever implements it: **keying the retry on `000` covers both variants**, since `curl` reports `000` for the (6) resolver failure and the (7) connect failure alike — no extra branch needed. Recommended **P3→P2** on the strength of 3-of-4 summons now being false. Deliberately **did not** ship the `watchdog.sh` retry itself: TD-23 marks it attended (Level C by blast radius), launchd executes the repo file directly so an edit is live at the next 5-min tick with no CI or deploy gate, and the failure mode of a mistake there is *silent suppression of a real production alert* on a live App Store app with real money. Waiting costs tokens; getting it wrong costs an outage nobody hears about — so the asymmetry favours waiting for Branden. Pre-existing uncommitted web WIP (`App.tsx` / `lib/sentry.ts` / `main.tsx`) untouched per hard-rule 11; only this log and `technical-debt.md` staged.

**Sandbox note for future summons.** `~/Library/Logs/sizzle-watchdog.log`, `pmset -g log`, `dig`, and `scutil --dns` are all outside this unattended session's permitted scope (reads are confined to the repo, and several commands need approval that nobody is present to give). Host-side wake/resolver evidence therefore cannot be collected directly — it has to be inferred from external vantage points, as here. The `gh workflow run uptime.yml` dispatch is the cheapest way to get a clean off-Mac datapoint on demand and is worth reaching for first.

**Still open / for Branden.** Nothing urgent, nothing Level D, nothing broken in production. **One thing to approve: TD-23.** The change is small and fully specified in the entry — in `watchdog.sh`, when a probe returns `000` (not a 5xx, which is a real origin answer), `sleep 20` and re-probe once before appending to `PROBLEMS`; optionally also require the latest `Uptime` run to be non-success before summoning. Test with `SIZZLE_WATCHDOG_TEST_URL=https://does-not-exist.invalid/health` + `SIZZLE_WATCHDOG_TEST_PROMPT`, confirming one retry and no summon on a blip vs. still summoning on a real 5xx. This is the third paid session spent on a blip; the true-outage path stays covered off-Mac by `uptime.yml` (3 probes 45 s apart), so a 20-second local confirm delay costs nothing in real coverage.

## Daily sweep 2026-08-21 — prod green; anon-boundary probe was covering 22 of 49 tables (TD-26, fixed + CI-guarded)

**Production health.** `/health` = `ok`, `problems: []`, `commit` `d6e8e36` = current HEAD (no rogue deploy), `stuckVideoBacklog` **0**, `parkedMediaDeletions` **0**, `paymentsKeyMode` **live**, cloudflare/moderation/email/Sentry configured, `push` ok. `getsizzle.app` = **200**. **Crons** all five fresh against their real schedules (probe 15:13 UTC): publish-scheduled 5 s and finalize-videos 51 s (`* * * * *`); rollup-hashtag-trends 773 s ≈12.9 m (`*/15`); rollup-watch-ratios 1,569 s ≈26 m (`17,47`, last run 14:47 → mid-cycle, not late); save-nudges 65,432 s ≈18.2 h (daily `0 21`, last success 2026-08-20 21:00 UTC, on time). No job stale.

**CI / alerts.** `gh run list --branch main` is all scheduled `Uptime` `success` rows plus **CI success on the HEAD push `d6e8e36`** (59 s). `secret-scanning/alerts?state=open` = **`[]`** (no P0; TD-20 stays closed) — note the first call returned a zsh glob error, not an empty result, and was re-run quoted before being believed. `code-scanning/alerts?state=open` = **0**. Local suite green: **7** unit + **19→20** invariants + **6** web controls.

**Dependency PRs / audit — unchanged, nothing in-lane.** **#6** (esbuild 0.24.2→0.28.2 + vite) and **#3** (`@hono/node-server` 1→2) both CI-green, both semver-**major** ⇒ owner lane per triage rule 3, both already carrying accurate hold comments (08-09 / 08-13); **no duplicate comment added** — nothing has changed and a daily re-comment is noise. `npm audit` unchanged at 2 rows (esbuild MODERATE, vite HIGH), every fix `isSemVerMajor: true` = TD-18; **`npm audit --omit=dev` = `found 0 vulnerabilities`**; `dependabot/alerts?state=open` = the same **7** alerts (#2–#8), all `scope: development`, all created 2026-08-08T20:54 — **no new advisory since the 08-14 baseline**. Per TD-25 there is still no engine proposing routine patch/minor updates, so triage rule 3 continues to have nothing to act on.

**Supabase DB state (items 5/5b) — day 7 of TD-21.** `mcp__supabase__get_advisors` → identical `Unauthorized … --access-token flag or SUPABASE_ACCESS_TOKEN`; claude.ai connector `list_projects` → still not granted (not self-granted; widening the agent's own permissions is an owner call). Item 5 fully answered without a DB path as of 08-19: `parkedMediaDeletions` **0**, `stuckVideoBacklog` **0** (15-min threshold ⇒ subsumes ">6h non-ready"), five fresh `cronAges` + `problems: []` ⇒ no repeated cron failure. A **third** path was tested and ruled out: PostgREST's OpenAPI root (`GET /rest/v1/` with the public publishable key) would have enumerated the live schema, but production answers **401** to anon — correct posture, dead end for the sweep.

**What I fixed — TD-26: the anon-boundary probe had silently stopped covering the schema.** Trying to substitute for the blocked advisors, I compared the probe's table list against the migrations and found it asserted against **22 hand-written tables while `supabase/migrations` declares 49**. Twenty-six tables — every collection/save/repost/reaction/follow/conversation/creator/analytics/hashtag table — were **never probed at all**. The list was correct when written on 08-17 and decayed as the schema grew; nothing tied it to the migrations, so it would have kept decaying indefinitely. **This was tooling decay, not a breach: I probed all 26 and production is clean.** Fix, in-lane (Level B per `safety:diff` — release tooling, no app/native/money/auth path): (1) classification extracted to `scripts/ops/anon-boundary-tables.mjs` and expanded to **47** must-yield-no-rows tables + `profiles` (column-scoped) + `hashtags`, each classified by **reading its migration's grants/policies**, not by copying whatever prod answered; (2) `hashtags` is anon-readable **by design** — `hashtags_public_read` (`20260718120000`, tightened by `20260718122000`) grants anon `select using (status = 'active' and not is_blocked and not is_sensitive)` — so the probe now asserts that predicate live (`is_blocked=eq.true`, `is_sensitive=eq.true`, `status=neq.active` must each return 0 rows), which stops "readable by design" from degrading into "readable, unchecked"; (3) the decay itself is now **CI-enforced** — a new invariant test parses every `create table` out of the migrations and fails on any unclassified table, any classified-but-undeclared table, any table in both buckets, and any anon-readable table with no predicate check. **Verified a real guard, not a tautology:** removing `saves` makes it fail, restoring it makes it pass. **Live result after the change: all 49 tables clean** — 47 yield 0 rows to anon, 3 `hashtags` predicate checks pass, 12 `profiles` PII columns still `401 42501`, `creator_earnings` still not executable, positive control readable. Net effect on TD-21: this substitutes for the advisor's `rls_disabled_in_public`-class lints **from the anon side across the whole schema** — the half that could actually hurt users — leaving the `authenticated`-side view, `function_search_path_mutable`, the `pg_trgm`/deny-all INFO baseline and `password_hibp_enabled` as the genuinely unverified remainder.

**Register drift (item 6).** TD-7 updated (the probe is materially stronger, but still anon-only + black-box, so it stays open); TD-21 updated with day 7, the ruled-out OpenAPI path, and its now-smaller residual gap; **TD-26 filed and closed in the same sweep**. One entry was itself stale: **TD-13** asserted the migration count should be corrected `"96"→95`, but the tree now holds **97** — corrected, with a note that the catch-up pass must re-count rather than trust a figure that moves with every migration. No entry needed closing that was left open.

**One thing I did NOT do.** The whole-schema result belongs in `SYSTEM_RISK_MAP.md` → Database (alongside "PostgREST is exposed and the anon key is public"): as of today the anon-facing surface is **49/49 tables verified clean**, and the check is CI-guarded against schema growth. I left that edit for Branden because `docs/engineering/**` guardrail docs are **Level C** under `autonomy-policy.md`, and TD-26 + this entry already carry the evidence. Suggested wording is in TD-26.

**Untouched, deliberately.** Branden's uncommitted web WIP (`App.tsx`, `main.tsx`, `apps/web/src/lib/sentry.ts` — Sentry local-dev suppression, exempting Capacitor native) is preserved and unstaged per CLAUDE.md rule 11; only my five paths were staged.

**For Branden**: nothing P0/P1, no push notification sent. (1) **TD-21, day 7** — restoring a DB path is still worth it (re-authorize the `.mcp.json` PAT, or allowlist the claude.ai connector's `list_projects`/`execute_sql`/`get_advisors`), but after today it buys less: only the `authenticated`-side advisor view and the auth-config lints are still dark. (2) **TD-25** — pick Renovate-app vs `.github/dependabot.yml` and delete the loser's config; until then the repo gets no routine dependency maintenance. (3) **TD-23** (watchdog `000` retry) is now 3-of-4 false summons and is a small attended edit. (4) Your web WIP looks complete and is entangled with TD-5/TD-8 — worth committing. Standing items unchanged: held majors #6/#3 (dev-only scope — a convenience call, not a risk call); TD-1 conditional hooks on the simulator; TD-22 attended; SYSTEM_RISK_MAP open finding #1 (recipe_steps/ingredients revoke migration, Level C — still the one meaningful open security item).

## Daily sweep 2026-08-23 — prod green; closed TD-8, and fixed a verifier that passed a broken frontend

**Production health.** `/health` = `ok`, `problems: []`, `commit` `69467a4` = HEAD at probe time (no rogue deploy), `stuckVideoBacklog` **0**, `parkedMediaDeletions` **0**, `paymentsKeyMode` **live**, cloudflare/moderation/email/Sentry configured, `push` ok. `getsizzle.app` = **200**. Beyond liveness, the real request path answered: `/feed/for-you?limit=3` → **200** in 0.39 s. **Crons** all five fresh, each checked against its *actual* schedule in `apps/api/vercel.json` and its limit in `health.ts:52-61` (probe 15:13 UTC): finalize-videos 3 s and publish-scheduled 59 s (`* * * * *`, limit 600); rollup-hashtag-trends 722 s ≈12 m (`*/15`, limit 7,200); rollup-watch-ratios 1,560 s ≈26 m (`17,47` — last run 14:47, mid-cycle, not late); save-nudges 65,534 s ≈18.2 h (daily `0 21`, last success 2026-08-22 21:00 UTC, on time, limit 93,600). `problems: []` is honest, not masking a stale job.

**CI / alerts — clean.** `gh run list --workflow=CI --branch main` = **success on the HEAD push `69467a4`** (50 s) and on the four pushes before it; the only other rows on `main` are scheduled `Uptime` runs, all `success`. **`secret-scanning/alerts?state=open` = `[]`** (no P0; TD-20 stays closed) and **`code-scanning/alerts?state=open` = `[]`**. Local gates green: **7** unit + **20→28** invariants + **6** web controls, `npm run typecheck` clean in both workspaces.

**Dependency PRs / audit — unchanged, nothing in-lane.** **#6** (esbuild 0.24.2→0.28.2 + vite) and **#3** (`@hono/node-server` 1→2): all 8 checks SUCCESS on both, both semver-**major** ⇒ owner lane per triage rule 3, both already carrying accurate hold comments (08-09 / 08-13). **No duplicate comment added** — nothing about either has changed and a daily re-comment is noise. `npm audit` unchanged at 2 rows (esbuild MODERATE GHSA-67mh-4wv8-2f99; vite HIGH ×3), every fix `isSemVerMajor: true` = TD-18; **`npm audit --omit=dev` = `found 0 vulnerabilities`**; `dependabot/alerts?state=open` = the same **7**, all `scope: development` — no new advisory since the 08-14 baseline. Per TD-25 nothing proposes routine patch/minor updates, so rule 3 still has nothing to act on.

**Supabase (items 5/5b) — day 8 of TD-21.** `mcp__supabase__get_advisors` → identical `Unauthorized … --access-token flag or SUPABASE_ACCESS_TOKEN`; claude.ai connector `list_projects` → still not permitted. Not self-granted — widening the agent's own permission set is an owner call. Item 5 answered without a DB path as it has been since 08-19 (`parkedMediaDeletions` 0 · `stuckVideoBacklog` 0, whose 15-min threshold strictly subsumes ">6h non-ready" · five fresh `cronAges` with `problems: []` ⇒ no repeatedly-failing cron). Substitute re-run clean: `node scripts/ops/anon-boundary-probe.mjs` → **OK**, all 49 migration-declared tables (47 yield 0 rows to anon, 3 `hashtags` policy-predicate checks, 12 `profiles` PII columns `401 42501`, `creator_earnings` execute denied, positive control readable). Genuinely unverified remains only the advisor's `authenticated`-side view + auth-config lints.

**Shipped — TD-8 closed (Level B; `safety:diff` → "RELEASE TOOLING DETECTED → minimum Level B", no security-sensitive path).** Two remainders, plus a real bug found while doing them.

*The bug, which is the important part:* `scripts/verify-deploy.mjs` **asserted nothing about the frontend**. Its web branch printed `probe … HTTP <status>` and then fell through — `failed` was only ever set inside `key === 'api'` branches. So a READY deployment serving **HTTP 500 on getsizzle.app exited 0 as "verified"**, and an alias pointing at a previous build was invisible. On a live App Store app that is a verifier giving false assurance, which is worse than no verifier. Now: a non-ok probe fails, and the served build is identified.

*(1) The frontend can now name its own commit.* A `versionManifest()` rollup plugin in `apps/web/vite.config.ts` emits `dist/version.json` = `{version, commit, builtAt}`, `commit` from `VERCEL_GIT_COMMIT_SHA` / `GITHUB_SHA`. `apps/web/vercel.json` serves it **`no-store`** — a cached copy would report the *previous* build as live, defeating the whole check. The app never imports the file, so runtime risk to users is nil, and nothing secret is in it (the SHA is already public on `/health`). *(2) `checkWebVersion()`* fetches it and compares to HEAD, giving the frontend the same served-surface proof `/health` already gives the API. An **unstamped** manifest (`commit: null`, i.e. the archive CLI deploy that CLAUDE.md prescribes when the webhook dies) is **reported, not failed** — failing there would raise a false alarm during exactly the outage the fallback exists for. A commit shorter than 7 chars is rejected rather than prefix-matched.

*(3) `docs/deploy.md` rewritten* against verified files, with a dated supersession banner. It had documented the API on **Railway / Fly.io** and the web on "Vercel / Netlify / any static host" — production has never run that way. It now covers the two reverse-named Vercel projects (`sizzle` = API, `sizzle-api` = frontend, with project ids), the Build-Output-API serverless function (`nodejs22.x`, maxDuration 30 s, `config.json` routing all paths to one function) rather than a `node dist/index.js` process, the genuinely required vs optional env vars including the two boot-time guards (demo-keys-against-hosted, `STRIPE_SECRET_KEY` without a webhook secret), the five cron schedules with their `/health` stale limits, the mandatory verify-deploy step, and the true `/health` shape. Its **trusted-proxy note was backwards** and is corrected: the anon rate-limit key prefers `x-vercel-forwarded-for`, else the **right-most** XFF hop — never the client-supplied left-most (`apps/api/src/middleware/rateLimit.ts:7-18`).

**Verification, not assertion.** The emission was proven on real builds twice: a plain `npm run build -w @sizzle/web` → `commit: null`, and driving Vite's build API with the real `apps/web/vite.config.ts` and `VERCEL_GIT_COMMIT_SHA` set → that SHA stamped through. **8 new tests** in `tests/invariants/ops-tooling.test.mjs` (exact match · short-SHA prefix accepted · alias mismatch FAILS · too-short commit FAILS · missing file FAILS · unreachable FAILS · unstamped archive deploy reports-not-fails · the build-emits-it + `no-store`-header contract). Confirmed real guards, not tautologies: flipping the header to `public, max-age=3600` **fails** the suite; restoring it passes — 28/28.

**Flag drift (item 6)** — every open entry re-verified against reality, nothing closed on a stale rationale. **Closed: TD-8** (above). **Narrowed: TD-13** — `deploy.md` has left its scope, and the two lines that *pointed at* it were corrected so the docs stop contradicting each other (`PROGRESS.md:108` no longer advertises a "Railway/Fly API + static web"; `docs/security.md` H-2 no longer says the IP limiter "requires trusted-proxy config in production", since that is resolved in code). Its ARCHITECTURE claim was re-counted rather than trusted: `app.ts` mounts exactly **19** route groups. The rest of TD-13 was deliberately **not** cherry-picked — piecemeal edits across eight docs is what produced this drift; it wants the single coherent pass. **TD-21** updated with day 8. Still real, unchanged: TD-1 (`npm run lint` re-run: **9 errors / 15 problems**, same three files), TD-5's remaining half (`apps/web/src/lib/sentry.ts` → **0** occurrences of `release`), TD-14 (`apps/web/package.json:33`), TD-17 (`.github/modernize/java-upgrade/` present), TD-22 (`internal.ts:42` plain `!==`), TD-23 (`watchdog.sh:42` bare `000`), TD-18 PR facts. Nothing new opened.

**Untouched, deliberately.** Branden's uncommitted web WIP (`App.tsx`, `main.tsx`, `apps/web/src/lib/sentry.ts` — `vite:preloadError` reload, Marketing `ErrorBoundary`, Sentry localhost suppression) preserved and unstaged per CLAUDE.md rule 11; now **6 sweeps old**. Only my eight paths plus this log were staged. `autonomy-audit.md` row 11 still repeats the stale "deploy.md still says Railway/Fly" claim, but it lives under `docs/engineering/**` (Level C), so it was left for Branden rather than self-approved.

**For Branden**: nothing P0/P1, no push notification sent. (1) **Worth knowing:** deploy verification was silently one-sided until today — every "web verified" line in previous sweep logs meant only "a READY deployment existed and something answered", never "getsizzle.app serves HEAD". From this deploy onward it means both. (2) **TD-21, day 8** — restoring a DB path still needs you (re-authorize the `.mcp.json` PAT, or allowlist the claude.ai connector's `list_projects`/`execute_sql`/`get_advisors`); only the `authenticated`-side advisor view and auth-config lints are still dark. (3) **TD-25** — pick Renovate-app vs `.github/dependabot.yml` and delete the loser's config. (4) **TD-23** (watchdog `000` retry) is 3-of-4 false summons, a small attended edit. (5) Your web WIP still looks complete — worth committing. (6) One-line follow-up now that TD-8 is closed: `autonomy-audit.md` row 11 needs its deploy.md clause struck (Level C, yours). Standing items unchanged: held majors #6/#3; TD-1 on the simulator; TD-22 attended; SYSTEM_RISK_MAP open finding #1 (recipe_steps/ingredients revoke migration, Level C — still the one meaningful open security item), plus the 08-21 suggestion to record the 49/49 anon-clean result there.

## Daily sweep 2026-08-24 — prod green; steady state, nothing shipped but the register

**Production health.** `/health` = `ok`, `problems: []`, `commit` `a1d05bf` = HEAD at probe time (no rogue deploy), `stuckVideoBacklog` **0**, `parkedMediaDeletions` **0**, `paymentsKeyMode` **live**, cloudflare/moderation/email/Sentry configured, `push` ok. `getsizzle.app` = **200**. **Crons** all five fresh against their schedules and `health.ts` limits (probe 15:13 UTC): finalize-videos 2 s and publish-scheduled 44 s (`* * * * *`); rollup-hashtag-trends 705 s ≈11.8 m (`*/15`); rollup-watch-ratios 1,569 s ≈26 m (`17,47` — mid-cycle, not late); save-nudges 65,533 s ≈18.2 h (daily `0 21`, last success 2026-08-23 21:00 UTC, on time, limit 93,600). `problems: []` is honest, not masking a stale job.

**CI / alerts — clean.** `gh run list --workflow=CI --branch main` = success on the HEAD push `a1d05bf` (54 s) and the pushes before it; all scheduled `Uptime` rows success. **`secret-scanning/alerts?state=open` = `[]`** (no P0) and **`code-scanning/alerts?state=open` = `[]`**. Local gates green: **7** unit + **28** invariants + **6** web controls, `npm run typecheck` clean in both workspaces.

**Dependency PRs / audit — unchanged, nothing in-lane.** **#6** (esbuild+vite) and **#3** (`@hono/node-server` 2): both OPEN, both 8/8 checks SUCCESS, both semver-**major** ⇒ owner lane per triage rule 3, both already carrying accurate hold comments — no duplicate comment added. `npm audit` unchanged at 2 rows (esbuild MODERATE, vite HIGH; every fix `isSemVerMajor` = TD-18); **`npm audit --omit=dev` = 0 vulnerabilities**; `dependabot/alerts?state=open` = the same **7**, all dev-scope — no new advisory since the 08-14 baseline. TD-25 still means nothing proposes routine patch/minor updates.

**Supabase (items 5/5b) — day 9 of TD-21.** `mcp__supabase__get_advisors` → identical `Unauthorized`; claude.ai connector `list_projects` → still not permitted (not self-granted — widening the agent's own permission set stays an owner call). Item 5 fully answered without a DB path (`parkedMediaDeletions` 0 · `stuckVideoBacklog` 0, 15-min threshold subsumes ">6h non-ready" · five fresh `cronAges` with `problems: []` ⇒ no repeatedly-failing cron). Substitute re-run clean: `node scripts/ops/anon-boundary-probe.mjs` → **OK**, all 49 migration-declared tables (47 yield 0 rows to anon, 3 `hashtags` predicate checks, 12 `profiles` PII columns `401 42501`, `creator_earnings` execute denied, positive control readable). Genuinely unverified remains only the advisor's `authenticated`-side view + auth-config lints.

**Flag drift (item 6).** Every open entry spot-verified against reality; all still real, none closable: TD-1 (`npm run lint` re-run: **9 errors / 15 problems**, same files), TD-5's remaining half (committed `apps/web/src/lib/sentry.ts` → **0** occurrences of `release`), TD-14 (`apps/web/package.json:33`), TD-17 (`.github/modernize/java-upgrade/` present), TD-22 (`internal.ts:42` plain `!==`), TD-23 (`watchdog.sh:42` bare `000`). TD-21 updated with day 9. Nothing new opened; nothing shipped besides this log + the register — a steady-state sweep, and every open item is deliberately parked in an attended/owner lane, so no code change was in scope today.

**Untouched, deliberately.** Branden's uncommitted web WIP (`App.tsx`, `main.tsx`, `apps/web/src/lib/sentry.ts`) preserved and unstaged per CLAUDE.md rule 11; now **7 sweeps old**. Only this log + `technical-debt.md` staged.

**For Branden**: nothing P0/P1, no push notification sent. Standing asks, all unchanged: (1) **TD-21, day 9** — restore a DB path (re-authorize the `.mcp.json` PAT, or allowlist the claude.ai connector's `list_projects`/`execute_sql`/`get_advisors`); only the `authenticated`-side advisor view and auth-config lints are still dark. (2) **TD-25** — pick Renovate-app vs `.github/dependabot.yml`. (3) **TD-23** (watchdog `000` retry) — 3-of-4 false summons, small attended edit. (4) Held majors #6/#3 whenever you want them (dev-only scope, convenience not risk). (5) Your web WIP is 7 sweeps old and looks complete — worth committing. (6) `autonomy-audit.md` row 11 still needs its stale deploy.md clause struck (Level C, yours). (7) SYSTEM_RISK_MAP open finding #1 (recipe_steps/ingredients revoke migration, Level C) remains the one meaningful open security item, plus the 08-21 suggestion to record the 49/49 anon-clean result there.

## Security sweep 2026-08-24 (weekly adversarial)

**No new exploitable exposure; every re-checkable production control re-verified intact; nothing needed fixing, so nothing was shipped beyond this log and a TD-21 annotation. One coverage hole persists and is stated, not hidden: the Supabase security-advisor baseline (TD-21, day 9 — this is the second consecutive weekly sweep it has been dark).**

**① Supabase security advisors — NOT RUN (blocked, day 9 of TD-21, 2nd weekly miss).** `mcp__supabase__get_advisors` → identical `Unauthorized … --access-token flag or SUPABASE_ACCESS_TOKEN`. The claude.ai Supabase connector (TD-21 option c) is still not in the agent's allowlist — `list_projects` → "requested permissions … not granted"; not self-granted, as before. Consequence unchanged from 08-17: cannot re-confirm the advisor baseline (≈30 deny-all `rls_enabled_no_policy` INFOs + accepted `pg_trgm`) nor the 2026-08-10 auth hardening (`password_hibp_enabled` + 10-char server floor) — if either regressed, this sweep would not see it. Nothing suggests regression (zero migrations in 8 days, no attended DDL, no auth-config change on record), but recorded as **unverified**, not clean.

**② CodeQL + Dependabot + secret scanning — clean, fully triaged.** `code-scanning/alerts?state=open` = **0**. `secret-scanning/alerts?state=open` = **`[]`**. `dependabot/alerts?state=open` = the same **7** dev-scope alerts (#2–#8: vite GHSA-fx2h-pf6j-xcff HIGH + GHSA-v6wh-96g9-6wx3 / GHSA-4w7w-66w2-5vf9 MEDIUM across `apps/web/package.json` + root lockfile, esbuild GHSA-67mh-4wv8-2f99 MEDIUM) — unchanged since 2026-08-08, all dev-server/build-time scope, all semver-major fixes = TD-18, PR #6 held for the owner's local-build + Simulator smoke. Nothing new, nothing in-lane, no dismissals needed.

**③ Route-auth audit — one API file changed since the 08-17 audit, no new routes, no gaps.** The only `apps/api/src` commit in 8 days is `be449a0` (`health.ts`): a `parkedMediaDeletions` gauge — service-role `head:true` count of `pending_media_deletions` where `attempts >= 10`, aggregate number only, no identifiers, deliberately excluded from `problems` so a parked row can never 503 the API (invariant-test-guarded; already reviewed in the 08-19 daily). Re-swept all 17 route files regardless: **admin double gate intact** (`admin.use('*', requireAuth, requireNotBanned, requireAdmin)` at `admin.ts:29` then `requireAdminUnlock` at `:33`, exemptions still exactly `/unlock`, `/passphrase`, `/security-status`, the bootstrap routes rate-limited 10/min); **crons fail-closed** (`internal.use('*')` rejects unless `Bearer CRON_SECRET` matches, absent secret rejects everything); `me.use('*', requireAuth)` at `me.ts:20`; the multi-line `uploads.post('/video'|'/tus')` and `support.post('/tickets')` definitions each carry `requireAuth` + rate limits (+ the 50/day upload quota); the middleware-less set is the same public-by-design list as 08-17 (`/health`, `/cooks/handle-available`, `/monetize/config` fee constants only, `/r /u /b` SEO renderers, rate-limited `POST /support/requests`, the webhooks). **Webhooks verify BEFORE acting, all three, re-read at source:** Stripe (`monetize.ts:866`) — raw body, ±300s replay window, HMAC-SHA256 over `${t}.${raw}`, `timingSafeEqual` with length pre-check, 401 before any parse-and-act; RevenueCat (`monetize.ts:796`) — length-checked `timingSafeEqual` on `REVENUECAT_WEBHOOK_AUTH` before the body is read; Cloudflare Stream (`uploads.ts:317`) — 403 unless configured+secret, ±300s window, HMAC, `timingSafeEqual`, uid regex, then the same moderated finalizer as the cron. `npm test` green end-to-end (unit → invariants → controls chain completed; final suite 6/6). TD-22 (plain `!==` on the CRON compare) unchanged, still attended-lane. Known open finding #2 (`GET /recipes/:id/playback` non-premium branch) unchanged.

**④ Secret hygiene — clean.** `node scripts/secrets-check.mjs --all` = **clean (453 files, all tracked)** (450 on 08-17; the delta is the three ops files shipped since). Files ADDED in the last 8 days: zero matching `.env|plist|json` — the only adds are `scripts/ops/anon-boundary-probe.mjs` + `anon-boundary-tables.mjs` and sweep docs (commits `a3cd986`, `bf9005f`, `9ae0404`). No forbidden path (`.mcp.json`, `.vercel/`, `.vercelignore`, Firebase plists, real `.env*`) is tracked.

**⑤ RLS spot-check — no migration-driven drift possible; boundary re-verified from the anon side.** Zero commits under `supabase/migrations` in 8 days, so the sweep's trigger set ("tables touched by last week's migrations") is empty and any drift would be out-of-band — exactly what the blocked `information_schema`/`pg_policies` query would catch (TD-21). Substitute run: `node scripts/ops/anon-boundary-probe.mjs` → **OK against production**, all 49 migration-declared tables — 47 yield 0 rows to anon, the 3 `hashtags` visibility predicates hold (`is_blocked`/`is_sensitive`/`status≠active` each 0 rows), all 12 probed `profiles` PII columns `401 42501`, `creator_earnings` execute denied, positive control (`profiles(id,handle)`) readable. Honest limits unchanged: anon-side only, so SYSTEM_RISK_MAP open findings #1 (`recipe_steps`/`recipe_ingredients` DML still granted to `authenticated`) and #4 (`comment_likes`/`reports`) are neither confirmed nor refuted this week — last live-verified 08-10, and nothing has touched grants since.

**⑥ Exposure drift — clean.** `/health` = `ok`, `problems: []`, `commit a2d477c` = HEAD at probe time, structure-only (booleans + modes: `paymentsKeyMode: live`, `cloudflareConfigured`, `moderationConfigured`, `emailConfigured`, `sentryConfigured`, `push: ok`), `stuckVideoBacklog` **0**, `parkedMediaDeletions` **0**, all five `cronAges` fresh — no secret-shaped values. Unauthenticated probes: `/admin/stats` **401**, `/admin/security-status` **401**, `/internal/finalize-videos` **401**, `/me` **401** (`{"error":{"code":"unauthorized"…}}`).

**Shipped this sweep** (explicit paths only; Branden's pre-existing uncommitted web WIP `App.tsx`/`lib/sentry.ts`/`main.tsx` untouched per rule 11): this log + a TD-21 annotation in `docs/engineering/technical-debt.md`. No control weakened, no dismissals, no code change was warranted — every open security item is already filed and deliberately parked in an attended/owner lane. `npm run secrets:check` before push; docs-only commit, so no API deploy to verify per the ship rule.

**For Branden — nothing P0/P1, no push notification sent.** The one thing this sweep could not do, for the second week running, is the advisor regression check on `password_hibp_enabled` + the 10-char floor — that stays dark until TD-21 gets option (a) (re-authorize the `.mcp.json` PAT) or (c) (allowlist the claude.ai connector tools). Everything else on the standing list is unchanged from this morning's daily sweep entry.
