/**
 * Seed script — populates realistic sample cooks + recipes.
 *
 * STUBBED for the Foundation+Auth slice. Implemented in the Phase 1 data slice,
 * where it will insert the cooks/recipes/ingredients/steps/video_assets from the
 * front-end's mock data (apps/web/src/data.ts) via the service-role client.
 */
async function main() {
  console.log('seed: not implemented yet — coming in the Phase 1 data slice.');
}

main().catch((err) => {
  console.error(err);
  process.exit(1);
});
